Reading additional input from stdin...
OpenAI Codex v0.143.0
--------
workdir: /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: medium
reasoning summaries: none
session id: 019f4645-3c49-7753-8342-147fbe358859
--------
user
You are running an automatic improvement iteration for the repo-local evaluate-cad-reconstruction skill.

Iteration: 9
Loop report: skills/evaluate-cad-reconstruction/loop_runs/iteration_009/loop_report.md
Projects root: outputs/reconstructions/step-to-cadquery

This is a batched iteration. Evaluate exactly these reconstruction projects before choosing improvements:
- `outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget`
- `outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included`
- `outputs/reconstructions/step-to-cadquery/aspire-planter`
- `outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig`

For each project in the batch:
- use the evaluate-cad-reconstruction skill workflow;
- write only the final `<project>/evaluation_report.json` as the persistent project artifact;
- clean temporary evaluation files before finishing.

After the batch reports exist, aggregate this batch mentally/from the reports and apply at most one evaluator improvement suggested by the batch. If an improvement changes scoring/check behavior, rerun the affected batch project reports plus any small regression set that is necessary.


Follow the loop report's Next Codex Prompt. Keep the iteration narrow:
- implement at most one helper/proxy/scoring-policy change;
- update docs/tests when code changes;
- rerun affected project reports plus a small regression set when feasible;
- preserve the artifact policy: delete temp evaluation artifacts and leave final reports only;
- summarize before/after scores and residual risks.

Do not use destructive git commands. Do not commit unless explicitly instructed.

Loop prompt:
Use the evaluate-cad-reconstruction skill. First evaluate the explicit batch projects listed in this prompt and write one final evaluation_report.json per project. Generate thorough physical manifests from rendered visual context, implement a narrow helper if the batch exposes a local deterministic missing-helper gap, then summarize batch scores, helper usage, remaining missing helpers, and before/after changes.

warning: failed to parse hooks config /Users/macbookpro/.codex/hooks.json: unknown field `SessionStart`, expected `description` or `hooks` at line 2 column 16
codex
I’ll use the repo-local `evaluate-cad-reconstruction` skill first, then inspect the batch projects and existing loop report before running evaluations.
exec
/bin/zsh -lc "sed -n '1,240p' skills/evaluate-cad-reconstruction/SKILL.md" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc "sed -n '1,240p' skills/evaluate-cad-reconstruction/loop_runs/iteration_009/loop_report.md" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
# Evaluation Skill Improvement Loop

- Generated: `2026-07-09T09:46:09.758464+00:00`
- Root: `outputs/reconstructions/step-to-cadquery`
- Projects: `9`

## Scores

| Project | Printability | Physical | Feature |
|---|---:|---:|---:|
| `outputs/reconstructions/step-to-cadquery/3d-printable-jet-engine` | 0.4 | 6.2 | 8.6 |
| `outputs/reconstructions/step-to-cadquery/5015-radial-fan-3d-model-for-conception-step-stl-f` | 9.2 | 8.0 | 8.4 |
| `outputs/reconstructions/step-to-cadquery/all-in-one-temperature-bridging-tower` | 4.0 | 9.2 | 9.2 |
| `outputs/reconstructions/step-to-cadquery/arduino-uno` | 9.9 | 10.0 | 8.8 |
| `outputs/reconstructions/step-to-cadquery/phone-holder` | 4.0 | 10.0 | 5.5 |
| `outputs/reconstructions/step-to-cadquery/silica-gel-spool-container` | 3.6 | 6.0 | 8.0 |
| `outputs/reconstructions/step-to-cadquery/simple-twist-open-mushroom` | 4.0 | 4.0 | 9.1 |
| `outputs/reconstructions/step-to-cadquery/spool-holder-and-drybox-for-enclosure-v2-with-humi` | 4.0 | 4.0 | 7.6 |
| `outputs/reconstructions/step-to-cadquery/tablet-and-notebook-stand` | 4.0 | 10.0 | 8.7 |

## Check Usage

- Used checks: `part_component_count, clear_path_proxy, assembly_component_count, part_contact, feature_count, opening_presence, cylindrical_fit, part_clearance, axis_alignment, assembly_sequence`
- Unused checks: `part_collision, linear_motion_collision, linear_motion_clearance, rotation_motion_collision, relative_pose, vent_opening_proxy, vent_grid_open_area_proxy, contact_graph`
- Failed checks: `{"assembly_component_count": 3, "clear_path_proxy": 2, "part_component_count": 3}`

## Improvement Candidates

- `implement_now` x1: vent holes/grilles: opening presence could be sampled but is unreliable on highly perforated non-watertight STL
- `implement_now` x1: dot/eye inserts vs body sockets: clearance-fit/contact/glue seating, missing socket proxy/clearance helper
- `implement_now` x1: Snap/press-fit compliance for bracket slots and beam tabs is not implemented.
- `implement_now` x1: Full folding motion path for the linkage needs joint-aware kinematic helpers; current rotation helper is too coarse for seated hinge geometry.
- `proxy_now` x1: body vs cap: cylindrical/threaded fit and rotation-motion; current helper cannot validate helical thread
- `proxy_now` x1: cap/body pairs: rotation-motion/thread or bayonet engagement, missing twist-cam helper
- `proxy_now` x1: True screw thread engagement and torque retention are not implemented.
- `proxy_now` x1: Load-bearing stiffness, tipping stability, and device weight support are not implemented.
- `missing_helper` x1: thread fit, cap/body screw engagement, silica containment, and fit inside a spool core need helpers/external proxies not implemented
- `missing_helper` x1: body/cap vs filament spool core: contains/fits; external spool proxy missing
- `missing_helper` x1: external spool/enclosure, humidity sensor, hinge/latch behavior, strength, and mounting loads are unmeasured
- `missing_helper` x1: drybox vs spool/enclosure: contains/supports/mounts; external objects missing
- `missing_helper` x1: sensor window vs humidity sensor: clearance-fit; external sensor proxy missing

## Low Score Causes

`{"feature_retention<8": 2, "physical_correctness<8": 4, "printability<8": 7}`

## Count Mismatch Review Signals

`{"assembly_vs_part_count": 3}`

## Next Codex Prompt

```text
Use the evaluate-cad-reconstruction skill. Read the latest loop report. Focus on this candidate: 'vent holes/grilles: opening presence could be sampled but is unreliable on highly perforated non-watertight STL'. Decide whether it is implement_now or proxy_now. If feasible, implement one narrow deterministic helper in cad_reconstruction_eval/physical_correctness_score.py, document it in references/physical-condition-manifests.md and references/scoring-guide.md, rerun the affected reports under outputs/reconstructions/step-to-cadquery, and summarize before/after scores. Also explain whether these unused checks are truly unused or just absent from this batch: ['part_collision', 'linear_motion_collision', 'linear_motion_clearance', 'rotation_motion_collision', 'relative_pose', 'vent_opening_proxy', 'vent_grid_open_area_proxy', 'contact_graph'].
```

 succeeded in 0ms:
---
name: evaluate-cad-reconstruction
description: Use when evaluating reconstructed CadQuery/STL/STEP projects for printability, physical correctness, functional usability, assembly separation, fit/clearance, collisions, motion paths, missing features, or when generating/running physical_conditions.json manifests.
---

# Evaluate CAD Reconstruction

## Purpose

Evaluate reconstructed CAD projects as project-scale artifacts, not isolated meshes. Use the repo's deterministic scorers for printability and physical correctness, and use an explicit condition manifest so LLM judgment proposes checks while scripts execute measurable geometry tests.

For a user-facing explanation of how scores are calculated and interpreted, read `references/scoring-guide.md`.

## Standard Workflow

1. Inspect the project layout before scoring:
   - `spec.md`, `README.md`, reconstruction reports, `params.py`, `main.py`, part folders, exported `.stl`, `.step`, `.3mf`.
   - Identify the assembled STL, canonical/reusable part STLs, and placed assembly instances. Prefer project-level scoring whenever multiple files exist.
   - Do not assume one part STL equals one physical assembly instance. Some projects export only canonical geometries and reuse them multiple times in the assembly.
2. Create an evaluation workspace for intermediate files:
   - Put generated renders, temporary manifests, split-part STLs, placed-instance STLs, proxy meshes, temporary exports, and per-metric raw JSON under a temporary evaluation directory, for example `<project>/.eval_tmp/`.
   - Do not write intermediate files directly into the project root unless the user explicitly asks for debug artifacts.
   - The only file that should remain after the skill finishes is the final evaluation report, for example `<project>/evaluation_report.json` or a user-requested aggregate report.
3. Run printability:
   ```bash
   python3 skills/evaluate-cad-reconstruction/scripts/score_printability.py <project-or-manifest>
   ```
   Treat printability as a success-rate/risk score, not a binary verdict. The current scorer ignores build-volume/dimension checks, tries best orientation, and only harshly penalizes clearly bad non-watertight meshes.
4. For physical correctness, first generate or review a temporary `physical_conditions.json`.
   - Before proposing checks, render the model with `skills/step-to-cadquery/scripts/render_views.py` and inspect the generated PNGs as visual context.
   - If a project has reusable/canonical parts, export or otherwise identify the placed instances before writing part-to-part checks. Physical checks such as contact, clearance, collision, relative pose, and motion paths need the instance geometry in assembly pose, not only the base canonical STL.
   - If no manifest exists, author one from design intent and geometry evidence.
   - For the required checklist and schema, read `references/physical-condition-manifests.md`.
   - If an important measurable check is not supported yet, add a focused helper to `cad_reconstruction_eval/physical_correctness_score.py` and expose it through the manifest check dispatcher before falling back to a missing-helper note.
5. Run physical correctness:
   ```bash
   python3 skills/evaluate-cad-reconstruction/scripts/score_physical_correctness.py --condition-manifest <project>/.eval_tmp/physical_conditions.json
   ```
6. Evaluate feature retention with an LLM-only report.
   - Read `references/feature-retention.md`.
   - Rerun project code, rebuild exports, and render assemblies/parts/variants when that helps assess preserved features.
7. Write one final report:
   - Scores/classes for printability, physical correctness, and feature retention.
   - Failed, inconclusive, and unused/missing checks.
   - Any conditions the LLM believes are important but still cannot be measured after considering a focused helper implementation.
   - Include enough raw metric details and condition results in the final report that the deleted intermediate JSON files are not needed for interpretation.
8. Clean up:
   - Delete only files created for the evaluation run.
   - Preserve user/project source files and pre-existing exports.
   - Remove the temporary evaluation directory after the final report has been validated.

## What To Inspect

Use all available project evidence, not just STL names. Visual inspection is mandatory for physical correctness check proposal:

```bash
mkdir -p <project>/.eval_tmp/renders
python3 skills/step-to-cadquery/scripts/render_views.py <project>/<assembly-or-main>.step <project>/.eval_tmp/renders/assembly
```

If separate part STEP files exist, render them too, using prefixes that match the part names. If no STEP exists, state that visual rendering via `skills/step-to-cadquery/scripts/render_views.py` could not be run and use available STL/3MF viewers or generated STEP exports if the project can rebuild them. Do not silently skip visual context.

- `spec.md` for intended parts, motion, fit, material, and manufacturing assumptions.
- `README.md` for user-facing purpose and exported file conventions.
- `*_reconstruction_report.md` for source listing details and reconstruction intent.
- `params.py` for clearances, diameters, counts, hinge sizes, latch dimensions.
- `main.py` and `parts/` or `features/` for assembly children, instances, unions, cuts, and named features.
- Exported `*_parts/*.stl` for canonical or actual separate parts. Check whether they are reusable canonicals or placed instances.
- The assembled `.stl` for connected component count and accidental fusion.
- Temporary placed-instance STLs for physical checks when the assembly reuses canonical parts through transforms, mirrors, arrays, or repeated `cq.Assembly.add(...)` calls.
- Temporary rendered iso views for gestalt, intended exposed surfaces, and obvious missing features.
- Temporary rendered ortho views for holes, slots, openings, alignment, contact faces, and possible hidden interferences.

## Canonical Parts vs Placed Instances

Distinguish these two concepts before scoring:

- `canonical_parts`: unique reusable shapes, such as one screw STL used four times.
- `placed_instances`: each physical occurrence in the assembled pose, such as `screw_0`, `screw_1`, `screw_2`, and `screw_3`.

Use canonical parts for:

- per-unique-shape printability;
- checking that a reusable part mesh is valid;
- checking expected unique part types.

Use placed instances for physical correctness:

- assembly component count;
- part contact, clearance, and collision;
- relative pose and axis alignment;
- motion path checks;
- checking that repeated/mirrored bodies do not overlap.

If only canonical part files exist, inspect `main.py`, assembly metadata, transforms, and renders. When feasible, export temporary placed-instance STLs under `<project>/.eval_tmp/` and use those as the manifest `parts`. Do not report an assembly-vs-part-count mismatch as a defect when the mismatch is explained by canonical reuse.

## Physical Correctness Categories

Group physical correctness checks under these categories:

- `separation_correctness`: parts that should be separate stay separate; parts that should be fused are fused.
- `fit_clearance`: inserts, lids, pins, slots, screws, snap features, and sliding interfaces have plausible clearance.
- `interference_collision`: forbidden overlap or collision does not happen.
- `contact_engagement`: required touching, seating, capture, or engagement exists.
- `motion_path`: intended insertion/removal/sliding/rotation/folding paths are feasible.
- `part_inventory`: expected bodies or repeated features exist.

Functional feature preservation is a separate metric. Do not bury missing functional features inside printability.

## Feature Retention

Feature retention is LLM-judged. It asks whether the reconstructed project preserves the intended user-facing and functional features of the source/design, independent of whether the mesh is printable or physically assembled correctly.

For feature retention:

- read `references/feature-retention.md`;
- use specs, reports, source descriptions, source images/renders if present, code, params, part files, and generated renders;
- rerun the reconstruction project when useful to rebuild missing exports, isolate parts, inspect generated assemblies, or test parameter-driven feature variants;
- render the assembled model, separate parts, and important variants before scoring when STEP/STL/3MF outputs can be produced;
- report evidence and uncertainty rather than pretending the score is deterministic.

## Check Selection Rule

When proposing physical checks, enumerate intended physical relationships first, then map each relationship to a helper. Do not start from the helper list and stop early.

Before writing the manifest, make an interaction matrix:

- rows: every physical part/body, including repeated instances and optional/demo bodies;
- columns: every other part/body plus the environment object if relevant, such as wall, shelf, screw, hinge pin, egg, tool, cable, card, drawer bay, or user-removable insert;
- each cell: `separate`, `fused`, `clearance-fit`, `contact`, `forbidden-contact`, `linear-motion`, `rotation-motion`, `contains/supports`, `fastener/axis`, or `no interaction`.

Every non-`no interaction` cell must become either a runnable condition or an explicit missing-helper note. Use the rendered views to confirm that visually apparent interactions from tabs, slots, holes, lids, drawers, hinges, latches, ribs, openings, and supports are represented.

For every separate part ask:

- Should it be separate or fused?
- Does it insert into, slide on, rotate around, fold with, latch to, screw into, hang from, contain, or support another part?
- What path would a user take to assemble/remove it?
- Which faces/features must touch, clear, or avoid each other?
- Which repeated features must exist in a countable way?

For every reusable/canonical part ask:

- How many placed instances should exist?
- Are the instance transforms, mirrors, rotations, and translations represented in the assembly?
- Are physical checks using instance names, not only canonical names?

If a relationship cannot be measured by current helpers, first decide whether it is implementable with a small deterministic helper. If it is implementable from available local geometry, code, parameters, or generated proxy meshes, write the helper and run it. Only leave a missing-helper note when the relationship requires unavailable external geometry, material properties, real physics, subjective ergonomics, or a large solver outside the scope of the evaluation run.

A physical manifest is not complete until the interaction matrix has been checked against both the design docs and the renders.

## Extending Physical Helpers During Evaluation

The helper list is not fixed. When the LLM identifies an important physical condition that is not covered, classify it:

- `implement_now`: deterministic, local, and narrow. Examples: count expected instances, verify a named axis offset, sample a slot/opening path, check a known diameter clearance, compare a measured distance profile, detect component fragmentation, validate simple ordered placement.
- `proxy_now`: not exact, but a useful local approximation can be implemented and reported as a proxy. Examples: simple tipping-support polygon proxy, coarse insertion-envelope collision, sampled screw-axis alignment without thread validation.
- `missing_helper`: not reliably measurable from available local data. Examples: true thread engagement, snap compliance, flexible deformation, fatigue, material strength, friction retention, load-bearing under a real device unless a suitable simulation/proxy has been explicitly implemented.

For `implement_now` and useful `proxy_now` cases:

1. Add the helper function to `cad_reconstruction_eval/physical_correctness_score.py`.
2. Add the `check` name to `_run_condition`.
3. Document expected `inputs`, `thresholds`, limitations, and severity guidance in `references/physical-condition-manifests.md`.
4. Add a focused test or run a representative manifest that proves pass and fail behavior when practical.
5. Include the new helper name, measurements, and limitations in the final evaluation report.

Keep new helpers small and deterministic. Do not add a broad dependency, long-running solver, network service, or subjective LLM-only judgment inside the deterministic scorer without explicit user approval.

## Auto-Improvement Loop

Use this loop when asked to improve the evaluator itself over a batch of projects:

1. Run the skill on a batch and leave only final `evaluation_report.json` files in each project.
2. Aggregate the reports:
   ```bash
   python3 skills/evaluate-cad-reconstruction/scripts/aggregate_evaluation_reports.py \
     <projects-root> \
     --out-dir skills/evaluate-cad-reconstruction/loop_runs/<iteration-name>
   ```
   Or create a dry-run iteration with an executable Codex prompt:
   ```bash
   python3 skills/evaluate-cad-reconstruction/scripts/run_codex_improvement_loop.py \
     <projects-root> \
     --iterations 1
   ```
   To run Codex automatically, add `--run-codex`. The driver uses `codex exec` with `workspace-write` sandboxing; it does not bypass sandboxing.
3. Read `loop_report.md` and identify:
   - repeated missing-helper themes;
   - false-positive signals;
   - low-score causes;
   - unused checks;
   - failed checks.
4. Choose one narrow improvement target per iteration.
5. Implement one deterministic helper, proxy, scoring-policy fix, or manifest-generation rule.
6. Rerun only the affected projects plus a small regression set.
7. Update docs/tests and record before/after score changes.

Do not combine multiple unrelated helper changes in one loop. If the aggregate report points to several issues, rank them and handle the highest-impact deterministic one first.

## Current Helper Checks

Supported manifest `check` values as of this skill version:

`assembly_component_count`, `part_component_count`, `part_collision`, `part_clearance`, `part_contact`, `linear_motion_collision`, `linear_motion_clearance`, `rotation_motion_collision`, `axis_alignment`, `relative_pose`, `opening_presence`, `clear_path_proxy`, `vent_opening_proxy`, `vent_grid_open_area_proxy`, `feature_count`, `cylindrical_fit`, `contact_graph`, `assembly_sequence`.

When a project needs a new deterministic check, implement it rather than treating this list as complete.

## Bundled Implementation

The scorer implementation lives inside this skill:

- `cad_reconstruction_eval/printability_score.py`
- `cad_reconstruction_eval/usability_score.py`
- `cad_reconstruction_eval/physical_correctness_score.py`
- `scripts/score_printability.py`
- `scripts/score_usability.py`
- `scripts/score_physical_correctness.py`
- `references/feature-retention.md`

The repository may also provide compatibility wrappers at `scripts/score_*.py` and `autoimprove/common/*_score.py`, but prefer the skill-local scripts when using this skill.

Unsupported/removed:

- `part_bbox_overlap`
- any bbox/dimension/build-volume scoring

## Known Pitfalls

- Do not use bbox overlap as evidence of physical failure; bbox checks were removed because they were too noisy.
- For installed inserts, collision at step 0 may represent intended engagement. Prefer a clearance profile or an `assembly_sequence` that distinguishes seated contact from path obstruction.
- Do not treat `assembly connected components != canonical part file count` as a physical defect. It is a defect only when compared against the expected placed instance count or expected physical component count.
- For living hinges and flexible parts, geometry checks can only approximate correctness. Implement simple geometry proxies when useful, but record missing-helper notes for bend radius, fatigue, and elastic feasibility unless a real helper has been added.
- For single STL projects, connected components may reveal separate floating bodies, but the design may intentionally contain repeated loose print-in-place pieces. Read the spec before treating this as a failure.
- For non-watertight meshes, printability impact depends on repair severity. Minor repairable boundary issues are risk, not automatically impossible.

## Output Style

Be explicit about evidence. Say which source files informed the evaluation, which checks ran, and which design-intent checks remain unmeasured. When a score is low, tie it to concrete failed conditions, not vague "usability" language.


exec
/bin/zsh -lc "find outputs/reconstructions/step-to-cadquery -maxdepth 2 -type f | rg '/(articulated-snowman-fidget|articulated-tie-step-file-included|aspire-planter|astronomical-telescope-hadley-an-easy-assembly-hig)/'" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/_tree.json
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/params.py
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.stl
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/.DS_Store
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/booleans.py
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/export_step.py
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/spec.md
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/check_liveness.py
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/main.py
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/export_stl.py
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/validation.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/_tree.json
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/params.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated Snowman Fidget.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/.DS_Store
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/booleans.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/export_step.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated Snowman Fidget.stl
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/build_residuals.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/geometry.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/spec.md
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/check_liveness.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/main.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/export_stl.py
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/validation.py
outputs/reconstructions/step-to-cadquery/aspire-planter/_tree.json
outputs/reconstructions/step-to-cadquery/aspire-planter/params.py
outputs/reconstructions/step-to-cadquery/aspire-planter/.DS_Store
outputs/reconstructions/step-to-cadquery/aspire-planter/make_residuals.py
outputs/reconstructions/step-to-cadquery/aspire-planter/introspect.json
outputs/reconstructions/step-to-cadquery/aspire-planter/booleans.py
outputs/reconstructions/step-to-cadquery/aspire-planter/export_step.py
outputs/reconstructions/step-to-cadquery/aspire-planter/make_tree.py
outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.step
outputs/reconstructions/step-to-cadquery/aspire-planter/spec.md
outputs/reconstructions/step-to-cadquery/aspire-planter/check_liveness.py
outputs/reconstructions/step-to-cadquery/aspire-planter/main.py
outputs/reconstructions/step-to-cadquery/aspire-planter/export_stl.py
outputs/reconstructions/step-to-cadquery/aspire-planter/gate_export.py
outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.stl
outputs/reconstructions/step-to-cadquery/aspire-planter/validation.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/_tree.json
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/params.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/introspect_raw.json
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/.DS_Store
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/booleans.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/export_step.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/build_residuals.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/spec.md
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/check_liveness.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/main.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.stl
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/export_stl.py
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/validation.py

codex
I’ve loaded the skill and prior loop report. The previous suggested vent helper may or may not apply to this batch, so I’m going to evaluate the four named projects first and only implement a narrow change if the batch exposes a deterministic gap.
exec
/bin/zsh -lc "sed -n '1,260p' skills/evaluate-cad-reconstruction/references/physical-condition-manifests.md" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
# Physical Condition Manifests

Use this reference when creating or auditing `physical_conditions.json`.

## Required Visual Pass

Before proposing physical conditions, render and inspect the model:

```bash
mkdir -p <project>/.eval_tmp/renders
python3 skills/step-to-cadquery/scripts/render_views.py <project>/<assembly-or-main>.step <project>/.eval_tmp/renders/assembly
```

Render separate part STEP files too when available. Use the 6 iso renders to understand the object and the 6 ortho renders to identify holes, slots, openings, contact faces, alignment axes, and hidden-looking interferences. If the project has no STEP file and cannot rebuild one, document that the render pass was unavailable; do not omit the visual-evidence step from the report.

Use the renders as input to condition proposal, not just as a final sanity check.

Generated render files are intermediate evaluation artifacts. They should be written under the evaluation temp directory and deleted after the final evaluation report is written.

## Canonical Parts vs Placed Instances

Physical correctness checks should normally run against placed instances, not only reusable base parts.

Definitions:

- `canonical_parts`: unique reusable shapes, for example one screw STL reused four times.
- `placed_instances`: every physical occurrence in assembly pose, for example `screw_0`, `screw_1`, `screw_2`, and `screw_3`.

Use canonical parts for unique-shape inventory and per-shape printability. Use placed instances for physical conditions because contact, clearance, collision, relative pose, axis alignment, and motion paths depend on the instance transform.

If a project exports only canonical part STEP/STL files, inspect the assembly code or STEP assembly structure to recover the instance transforms. When feasible, export temporary placed-instance STLs under:

```text
<project>/.eval_tmp/instances_stl/
```

Then use those placed-instance files in the manifest `parts` map. Do not compare assembly component count against the canonical part count. Compare it against the expected placed physical component count.

The final report should state when temporary placed instances were generated and should preserve the mapping summary after deleting `.eval_tmp`.

## Manifest Shape

```json
{
  "assembly": "model.stl",
  "canonical_parts": {
    "screw": "parts/screw.stl"
  },
  "parts": {
    "body": "instances_stl/body.stl",
    "lid": "instances_stl/lid.stl",
    "screw_0": "instances_stl/screw_0.stl",
    "screw_1": "instances_stl/screw_1.stl"
  },
  "conditions": [
    {
      "id": "lid_can_slide_on_body_without_collision",
      "category": "motion_path",
      "severity": "major",
      "check": "linear_motion_collision",
      "description": "Lid should slide onto the body along +Y.",
      "inputs": {
        "moving_part": "lid",
        "obstacle_part": "body",
        "translation": [0.0, 60.0, 0.0],
        "steps": 8
      }
    }
  ]
}
```

Paths are relative to the manifest file. Use stable part or instance names that describe physical role, not anonymous indexes when the role is known.

The scorer currently executes checks against the `parts` map. Therefore, for physical correctness, `parts` should contain the in-assembly placed instances whenever they differ from canonical reusable shapes. The optional `canonical_parts` map is report context for humans and for unique-shape inventory; do not rely on it for placed-pose checks unless the canonical is also the only placed instance.

When running the full skill, this manifest is normally temporary. Write it under `<project>/.eval_tmp/physical_conditions.json` unless the user explicitly asks to keep manifests for debugging. The final evaluation report should copy the important manifest contents: design-intent inventory, interaction matrix summary, runnable conditions, condition results, and missing-helper notes.

## Thorough Proposal Checklist

Before writing runnable checks, produce a short design-intent inventory:

1. Parts and bodies:
   - Which exported STLs represent separate physical parts?
   - Which exported STLs are canonical reusable part shapes?
   - Which placed instances exist in the assembly, including repeated or mirrored bodies?
   - Which repeated instances are expected?
   - Which bodies must be fused into one print?
2. Separation correctness:
   - Expected connected components in the assembled STL.
   - Expected connected components inside each placed instance STL.
   - Whether the number of assembly components matches expected placed instances, not merely expected canonical part types.
   - Any print-in-place bodies that should remain separate but near/contacting.
3. Fit and clearance:
   - Lids, drawers, trays, dividers, pins, sockets, screws, holes, tabs, slots, dovetails, snaps.
   - Min/max clearance from `params.py` or spec.
   - Cylindrical fits where diameters are explicit.
4. Interference/collision:
   - Forbidden part overlap in assembled, storage, or print layout.
   - Collision along insertion/removal/sliding/rotation paths.
5. Contact and engagement:
   - Required seating/contact pairs.
   - Latches, tabs, supports, stops, hinge barrels, hooks, screw heads.
   - Expected and forbidden contact graph edges.
6. Motion paths:
   - Linear insertion/removal for drawers, dividers, lids, slides, cards.
   - Rotation for hinges, handles, doors, knobs, foldable bodies.
   - Assembly sequences where multiple checks must hold in order.
7. Functional geometry:
   - Openings, through-holes, containers, vents, cable paths, drainage, gripping surfaces.
   - Counted features such as honeycomb holes, screw holes, compartments, feet, dividers.
8. Unsupported or new-helper checks:
   - For every important relationship not covered by an existing helper, decide whether to implement a focused deterministic helper now, implement a useful proxy now, or leave a missing-helper note.
   - Flexible deformation, hinge fatigue, snap compliance, strength, fastener thread quality, ergonomics, and load-bearing checks are usually not fully measured by current helpers unless a specific proxy/helper is added.

Then produce an interaction matrix. Include every physical placed instance/body and every relevant external mating object named or implied by the spec, such as shelf board, wall, screw, hinge pin, egg, screwdriver, card, cable, or user hand path. For each pair, classify the relationship as one of:

- `separate`
- `fused`
- `clearance-fit`
- `contact`
- `forbidden-contact`
- `linear-motion`
- `rotation-motion`
- `contains/supports`
- `fastener/axis`
- `no interaction`

Every matrix entry except `no interaction` must map to at least one runnable condition, a newly implemented helper/proxy, or an explicit missing-helper note. Re-check the matrix against rendered iso and ortho views before finalizing.

For repeated parts, write the matrix at instance granularity. For example, a tablet stand with one canonical screw shape and four screws should list `screw_0`, `screw_1`, `screw_2`, and `screw_3` as separate rows/columns for contact and clearance checks.

## Mapping Intent To Helpers

Use `feature_count` for expected parts, placed instances, canonical part types, or named repeated features. Be explicit about which inventory is being counted.

Use `assembly_component_count` when the assembled STL should have a known number of connected bodies. For project print layouts with separate parts, this often equals the number of separate placed physical instances. It should not be compared against the number of canonical reusable part files unless there is no reuse.

Use `part_component_count` when a named placed instance or canonical part must be one connected mesh or intentionally multiple connected components.

Use `part_collision` for forbidden overlap at a static pose. This requires placed instances in their assembly poses. Avoid using it for a seated insert if contact/engagement at the installed pose is expected.

Use `part_clearance` for static minimum distance between two placed parts.

Use `part_contact` when two placed parts must touch or nearly touch.

Use `linear_motion_collision` for a straight insertion/removal/slide path where collision should never happen.

Use `linear_motion_clearance` for a straight path where a minimum distance profile is useful. If a removable part starts seated in a slot, a threshold of `0.0` may be appropriate unless the helper can ignore the initial seated step.

Use `rotation_motion_collision` for hinge, lid, handle, or knob sweeps around a known axis.

Use `axis_alignment` for coaxial pins/holes, hinge barrels, screw axes, or rails.

Use `relative_pose` for centroid/order checks along an axis when exact contact is not needed.

Use `opening_presence` for through-holes, containers, cable paths, vents, and open-top trays when the mesh is watertight enough for point-inside sampling to be meaningful. Define a line segment that should remain outside material.

Use `clear_path_proxy` for explicit straight paths through holes, slots, cable passages, peg windows, and other expected openings when `opening_presence` is unreliable on non-watertight, thin-shell, or highly perforated meshes. Provide one or more sample paths through known clear regions:

```json
{
  "id": "mounting_holes_have_clear_axes",
  "category": "fit_clearance",
  "severity": "major",
  "check": "clear_path_proxy",
  "description": "Representative mounting-hole centerlines should not hit mesh triangles.",
  "inputs": {
    "part": "body",
    "paths": [
      {"start": [-20.0, 0.0, -1.0], "end": [-20.0, 0.0, 6.0]},
      {"start": [20.0, 0.0, -1.0], "end": [20.0, 0.0, 6.0]}
    ]
  },
  "thresholds": {
    "min_clear_fraction": 1.0,
    "max_intersections_per_clear_path": 0
  }
}
```

The helper counts a path as clear when the segment intersects no more than `max_intersections_per_clear_path` triangles. Use `min_clear_paths` or `min_clear_fraction` to define the pass criterion; if neither is provided, `min_clear_fraction` defaults to `1.0`. This is a proxy: it verifies sampled straight paths only, not opening diameter, open area, shape fidelity, or downstream assembly usability. Keep severity aligned to the feature: mounting holes and cable paths may be `major`; decorative or broad window samples are usually `minor`.

Use `vent_opening_proxy` for vent holes, perforated grilles, and slot fields when `opening_presence` is unreliable because the mesh is highly perforated or non-watertight. Provide explicit straight sample rays through representative expected openings:

```json
{
  "id": "cap_grille_has_open_flow_paths",
  "category": "functional_feature_preservation",
  "severity": "minor",
  "check": "vent_opening_proxy",
  "description": "Representative rays through the cap grille should pass through open slots.",
  "inputs": {
    "part": "cap",
    "rays": [
      {"start": [10.0, 0.0, 42.0], "end": [10.0, 0.0, 48.0]},
      {"start": [0.0, 10.0, 42.0], "end": [0.0, 10.0, 48.0]}
    ]
  },
  "thresholds": {
    "min_clear_fraction": 0.5,
    "max_intersections_per_clear_ray": 0
  }
}
```

The helper counts a ray as clear when it intersects no more than `max_intersections_per_clear_ray` sampled triangles. Use `min_clear_rays` or `min_clear_fraction` to define the pass criterion; if neither is provided, `min_clear_fraction` defaults to `0.5`. This is a proxy: it verifies sampled straight flow paths only, not total open area, hole count, hole diameter, grille shape, or airflow. Prefer `minor` severity unless the sampled openings are the only core physical function and the ray locations come from strong geometry/code evidence.

Use `vent_grid_open_area_proxy` when the part has a vent/grille field but individual opening rays are too tedious or brittle to author. This helper samples a rectangular grid of parallel rays across a specified patch:

```json
{
  "id": "body_grille_has_open_area",
  "category": "functional_feature_preservation",
  "severity": "minor",
  "check": "vent_grid_open_area_proxy",
  "description": "A sampled patch across the grille should contain representative through-flow paths.",
  "inputs": {
    "part": "body",
    "grid_origin": [-20.0, -1.0, 35.0],
    "u_vector": [40.0, 0.0, 0.0],
    "v_vector": [0.0, 0.0, 20.0],
    "ray_direction": [0.0, 1.0, 0.0],
    "ray_length_mm": 4.0,
    "rows": 5,
    "cols": 9
  },
  "thresholds": {
    "min_clear_fraction": 0.25,
    "max_intersections_per_clear_ray": 0
  }
}
```

`grid_origin` is one corner of the sample patch. `u_vector` and `v_vector` span the patch, and `ray_direction` plus `ray_length_mm` define the through-material probe. Use `min_clear_rays` or `min_clear_fraction`; if neither is provided, `min_clear_fraction` defaults to `0.25`. The helper reports clear ray count/fraction and min/max triangle intersections. This is a coarse proxy for open-area presence on perforated or non-watertight meshes; it does not validate exact hole count, individual hole dimensions, decorative pattern fidelity, or airflow. Keep severity `minor` unless the sampled patch and expected threshold come directly from project code or dimensions.

Use `cylindrical_fit` when the manifest can provide pin and hole diameters directly.

Use `contact_graph` when multiple pairs have expected or forbidden contact relationships.

Use `assembly_sequence` when a real user action depends on ordered subchecks.

## Adding New Helpers

Do not treat the current helper set as closed. If a condition is important and can be measured deterministically from local data, add a helper instead of leaving a missing-helper note.

Use this triage:

| Decision | Use when | Expected action |
|---|---|---|
| `implement_now` | The check can be measured directly from available meshes, STEP-derived values, project params, transforms, or generated proxy meshes. | Add a helper to `physical_correctness_score.py`, expose it in `_run_condition`, document inputs/thresholds here, run it. |
| `proxy_now` | Exact measurement is hard, but a narrow local approximation gives useful evidence and can be described honestly. | Add a helper or manifest subcheck with conservative severity and clear limitations. |
| `missing_helper` | The check needs unavailable external geometry, material properties, true physics, subjective ergonomics, or a large solver. | Record an explicit missing-helper note in the final report. |

Helper design rules:

- Keep the helper deterministic and local.
- Prefer inputs that are explicit in the manifest: part names, axes, points, vectors, expected counts, diameter/radius values, distance thresholds, sampled path steps.
- Return concrete measurements, not only pass/fail.
- Mark weak proxies as `minor` unless they are strongly tied to the core function.

exec
/bin/zsh -lc "sed -n '1,220p' skills/evaluate-cad-reconstruction/references/scoring-guide.md" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
# CAD Reconstruction Evaluation Scoring Guide

This document explains how the `evaluate-cad-reconstruction` skill scores reconstructed CAD projects. It is meant for users who need to run the skill, interpret the reports, and decide which reconstructed models need human review.

The skill reports three separate scores:

- `printability`: how likely the exported mesh is to print successfully.
- `physical_correctness`: whether the in-scope parts are separated, connected, fitted, contacted, and movable as intended.
- `feature_retention`: whether the reconstruction preserved the intended object features and semantics.

Each score is on a `0..10` scale. Floating point scores are allowed.

## Score Classes

Printability and physical correctness use the same class labels:

| Score range | Class | Meaning |
|---|---|---|
| `8.0..10.0` | `easy` | Low-risk; probably acceptable unless the report contains important caveats. |
| `4.0..7.99` | `hard` | Risky or partially broken; should usually get human review. |
| `0.0..3.99` | `impossible` | Severe failure; likely unusable without repair or reconstruction. |

Feature retention uses a semantic class scale:

| Score range | Class | Meaning |
|---|---|---|
| `9.0..10.0` | `excellent` | Important features are preserved. |
| `8.0..8.99` | `good` | Mostly preserved, with small or moderate issues. |
| `5.0..7.99` | `review` | Several important features are altered or missing. |
| `< 5.0` | `poor` | Core object semantics or function are missing. |

## Project-Scale Scoring

Evaluate a project, not only one STL, whenever possible.

A reconstruction project often has:

- one assembled STL;
- separate part STLs;
- STEP files;
- `spec.md`, `README.md`, reconstruction reports, `params.py`, `main.py`, `parts/`, `features/`, or `assemblies/`;
- generated render images.

The assembled STL is useful for detecting accidental fusion, disconnected bodies, and final layout. Separate part STLs are useful for per-part printability and part-to-part physical checks. The docs/code/renders are necessary for deciding what should be checked.

Some projects export reusable canonical part files instead of one file per assembly occurrence. For example, a stand may have one `screw.stl` but four placed screws in the assembled model. Treat these separately:

- canonical parts are unique shapes;
- placed instances are physical occurrences in assembly pose.

Physical correctness checks should use placed instances whenever placement matters. Canonical part files alone are not enough for contact, clearance, collision, relative pose, or motion checks.

## Artifact Policy

The skill should leave only the final evaluation report.

During evaluation, generated files should go under a temporary evaluation directory such as:

```text
<project>/.eval_tmp/
```

Temporary files include:

- generated render PNGs;
- generated `physical_conditions.json`;
- raw `printability_report.json`;
- raw `physical_correctness_report.json`;
- temporary feature-retention JSON;
- split component STLs;
- placed-instance STLs exported from an assembly;
- proxy meshes for external objects;
- temporary STEP/STL/3MF exports;
- parameter-variant exports and probe outputs.

After the final report is written and validated, delete the temporary directory. Do not delete original project files, user-provided exports, source specs, code, or pre-existing assets.

The final report should be self-contained. It should include enough details from the raw metric reports, condition manifest, condition results, missing-helper notes, render observations, and feature-retention assessment that the intermediate files are no longer needed.

## Printability Score

Printability is a risk score, not a yes/no verdict. A mesh can have minor issues and still score highly if those issues are likely repairable or harmless for slicing.

Run:

```bash
python3 skills/evaluate-cad-reconstruction/scripts/score_printability.py \
  --assembly <project>/<assembly>.stl \
  --parts <project>/<part-a>.stl <project>/<part-b>.stl \
  --json-out <project>/.eval_tmp/printability_report.json
```

If the project has only one STL, pass it as `--assembly`.

### What The Script Measures

For each STL, the scorer measures:

- triangle count;
- bounding box and mesh extents, for reporting only;
- surface area and signed mesh volume;
- degenerate triangle count;
- watertightness and bad boundary/non-manifold edge count;
- connected component count;
- tiny disconnected components;
- best print orientation among six axis-aligned orientations;
- bed contact area in that best orientation;
- unsupported overhang area in that best orientation.

Build-volume, maximum dimension, and bbox-size checks are intentionally removed. A large model should not be penalized just because it exceeds a default printer volume.

### Orientation Handling

The scorer assumes the user can freely orient the part. It evaluates six axis-aligned orientations and chooses the one with:

1. lowest unsupported overhang fraction;
2. largest bed contact area as a tie-breaker.

The selected orientation is reported as `selected_orientation`.

This is still an approximation. It does not search arbitrary angled orientations, split a model, add supports, or simulate slicer-specific support generation.

### Per-Mesh Deductions And Caps

Each mesh starts from `10.0`.

Hard failures:

- empty mesh: score `0.0`;
- non-finite coordinates: score `0.0`;
- failed STL load: score `0.0`;
- zero thickness in any axis: cap at `3.0`;
- zero enclosed volume: cap at `3.0`;
- severe open/non-manifold boundaries: cap at `4.0`.

Non-watertight handling:

- A mesh is `watertight` when it has no bad boundary/non-manifold edges.
- If not watertight, the scorer computes:
  - `bad_edge_count`;
  - `bad_edge_length_mm`;
  - `bad_boundary_surface_ratio = bad_edge_length_mm / sqrt(surface_area_mm2)`.
- The mesh is treated as severe only if:
  - `bad_boundary_surface_ratio >= 0.50`, or
  - `bad_edge_count >= 32`.
- Minor non-watertight meshes are recorded as a risk factor, not automatically made unprintable.

Other deductions:

- degenerate triangles: `0.05` per triangle, capped at `1.5`;
- disconnected components: `0.4` per extra component, capped at `2.5`;
- tiny disconnected components: `0.5` each, capped at `2.0`;
- bed contact area below `25 mm^2`: subtract `2.0`;
- unsupported overhang fraction above `25%`: subtract up to `3.0`;
- unsupported overhang fraction above `10%`: subtract `0.8`;
- triangle count above `500,000`: subtract `0.8`.

### Project Printability Combination

The project score combines the assembly and part scores:

```text
project_score = 0.7 * worst_mesh_score + 0.3 * average_mesh_score
```

This intentionally weights the weakest part heavily, because one unprintable part can make the project fail.

Additional project-level behavior:

- no assembly STL supplied: risk factor;
- no STL files supplied: score `0.0`;
- assembly connected-component count differs from number of supplied part STLs: subtract `0.7`;
- any failed STL load caps the project at `1.0`;
- any zero volume/thickness hard failure caps the project at `3.0`;
- any severe open/non-manifold boundary caps the project at `4.0`.

If the supplied part STLs are canonical reusable shapes, the assembly connected-component count may legitimately exceed the number of part files. In that case, either:

- run printability with temporary placed-instance STLs if the component-count comparison matters; or
- note the canonical-reuse convention in the final report and do not treat the mismatch as a physical correctness defect.

For printability, canonical parts are often sufficient because the question is whether each unique shape can print. For physical correctness, placed instances are usually required.

## Physical Correctness Score

Physical correctness evaluates whether the in-scope geometry works as a physical object or assembly. It is not the same as printability and not the same as feature retention.

Examples:

- parts that should be separate are separate;
- parts that should be fused are one component;
- lids, drawers, inserts, pins, and sockets have plausible clearance;
- required contacts or seating relationships exist;
- forbidden collisions do not occur;
- intended linear or rotational motion paths are plausible.

Run:

```bash
python3 skills/evaluate-cad-reconstruction/scripts/score_physical_correctness.py \
  --condition-manifest <project>/.eval_tmp/physical_conditions.json \
  --json-out <project>/.eval_tmp/physical_correctness_report.json
```

### Required Manifest

Physical correctness depends on `physical_conditions.json`. The LLM or reviewer must propose conditions from design intent first; the script then executes the measurable checks.

The manifest should include:

- `assembly`: assembled STL path, relative to the manifest;
- `parts`: named placed-instance STL paths when the assembly has repeated or transformed instances;
- optional `canonical_parts`: unique reusable part STL paths for report context;
- `design_intent_inventory`: short notes about expected parts, fits, contacts, motions, and missing helpers;
- `interaction_matrix`: every important part-to-part or part-to-environment relationship;
- `conditions`: runnable deterministic checks.

The scorer executes checks against `parts`. If a model reuses a canonical part multiple times, export temporary placed-instance STLs and put those instance paths in `parts`.

Do not generate conditions only by looking at the helper list. First identify every relevant physical relationship from the spec, code, part files, and renders.

exec
/bin/zsh -lc "sed -n '1,280p' skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc "sed -n '1,220p' skills/evaluate-cad-reconstruction/references/feature-retention.md" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
#!/usr/bin/env python3
"""Physical-correctness scoring for reconstructed CAD/STL projects.

The pipeline is condition-driven:
1. An upstream reviewer/LLM writes explicit physical conditions.
2. This module runs deterministic helper checks for those conditions.
3. The scorer combines condition results with baseline Layer 1 geometry checks.

This module intentionally does not judge functional-feature preservation.
"""
from __future__ import annotations

import argparse
import json
import os
from dataclasses import asdict, dataclass
from pathlib import Path
from typing import Any, Iterable

import numpy as np

from cad_reconstruction_eval.printability_score import (
    PrintabilityAssumptions,
    _read_stl,
    _topology_metrics,
    _triangle_metrics,
)
from cad_reconstruction_eval.usability_score import (
    UsabilityAssumptions,
    _mesh_geometry_report,
    score_usability_project,
)


@dataclass(frozen=True)
class PhysicalCorrectnessAssumptions:
    critical_failure_cap: float = 4.0
    major_failure_penalty: float = 2.0
    minor_failure_penalty: float = 0.8
    inconclusive_penalty: float = 0.3
    max_sample_points: int = 500
    max_sample_triangles: int = 1000
    collision_epsilon_mm: float = 1e-6
    collision_probe_distance_mm: float = 0.1


def _class_for_score(score: float) -> str:
    if score >= 8.0:
        return "easy"
    if score >= 4.0:
        return "hard"
    return "impossible"


def _round_score(score: float) -> float:
    return round(max(0.0, min(10.0, score)), 2)


def _severity_penalty(severity: str, assumptions: PhysicalCorrectnessAssumptions) -> float:
    if severity == "critical":
        return 10.0 - assumptions.critical_failure_cap
    if severity == "major":
        return assumptions.major_failure_penalty
    if severity == "minor":
        return assumptions.minor_failure_penalty
    return assumptions.minor_failure_penalty


def _resolve_part(parts_by_name: dict[str, dict], name: str) -> dict | None:
    return parts_by_name.get(name)


def _load_part_triangles(parts_by_name: dict[str, dict], name: str) -> tuple[np.ndarray | None, str | None]:
    part = _resolve_part(parts_by_name, name)
    if part is None:
        return None, f"missing part: {name}"
    if not part.get("loadable"):
        return None, f"part failed to load: {name}"
    try:
        return _read_stl(Path(part["path"])), None
    except Exception as exc:
        return None, f"failed to load part {name}: {exc}"


def _sample_rows(values: np.ndarray, limit: int) -> np.ndarray:
    if len(values) <= limit:
        return values
    idx = np.linspace(0, len(values) - 1, limit, dtype=int)
    return values[idx]


def _sample_points(triangles: np.ndarray, limit: int) -> np.ndarray:
    points = np.unique(np.round(triangles.reshape(-1, 3), 8), axis=0)
    return _sample_rows(points, limit)


def _sample_triangles(triangles: np.ndarray, limit: int) -> np.ndarray:
    return _sample_rows(triangles, limit)


def _point_triangle_distance(point: np.ndarray, tri: np.ndarray) -> float:
    # Real-Time Collision Detection, Christer Ericson, closest point on triangle.
    a, b, c = tri
    ab = b - a
    ac = c - a
    ap = point - a
    d1 = float(np.dot(ab, ap))
    d2 = float(np.dot(ac, ap))
    if d1 <= 0.0 and d2 <= 0.0:
        return float(np.linalg.norm(point - a))

    bp = point - b
    d3 = float(np.dot(ab, bp))
    d4 = float(np.dot(ac, bp))
    if d3 >= 0.0 and d4 <= d3:
        return float(np.linalg.norm(point - b))

    vc = d1 * d4 - d3 * d2
    if vc <= 0.0 and d1 >= 0.0 and d3 <= 0.0:
        v = d1 / (d1 - d3)
        return float(np.linalg.norm(point - (a + v * ab)))

    cp = point - c
    d5 = float(np.dot(ab, cp))
    d6 = float(np.dot(ac, cp))
    if d6 >= 0.0 and d5 <= d6:
        return float(np.linalg.norm(point - c))

    vb = d5 * d2 - d1 * d6
    if vb <= 0.0 and d2 >= 0.0 and d6 <= 0.0:
        w = d2 / (d2 - d6)
        return float(np.linalg.norm(point - (a + w * ac)))

    va = d3 * d6 - d5 * d4
    if va <= 0.0 and (d4 - d3) >= 0.0 and (d5 - d6) >= 0.0:
        w = (d4 - d3) / ((d4 - d3) + (d5 - d6))
        return float(np.linalg.norm(point - (b + w * (c - b))))

    denom = 1.0 / (va + vb + vc)
    v = vb * denom
    w = vc * denom
    closest = a + ab * v + ac * w
    return float(np.linalg.norm(point - closest))


def _mesh_min_distance(
    a: np.ndarray,
    b: np.ndarray,
    assumptions: PhysicalCorrectnessAssumptions,
) -> float:
    points_a = _sample_points(a, assumptions.max_sample_points)
    points_b = _sample_points(b, assumptions.max_sample_points)
    tris_a = _sample_triangles(a, assumptions.max_sample_triangles)
    tris_b = _sample_triangles(b, assumptions.max_sample_triangles)
    best = float("inf")
    for point in points_a:
        for tri in tris_b:
            best = min(best, _point_triangle_distance(point, tri))
            if best <= assumptions.collision_epsilon_mm:
                return 0.0
    for point in points_b:
        for tri in tris_a:
            best = min(best, _point_triangle_distance(point, tri))
            if best <= assumptions.collision_epsilon_mm:
                return 0.0
    return best


def _ray_intersects_triangle(origin: np.ndarray, direction: np.ndarray, tri: np.ndarray, eps: float = 1e-9) -> bool:
    v0, v1, v2 = tri
    edge1 = v1 - v0
    edge2 = v2 - v0
    h = np.cross(direction, edge2)
    a = float(np.dot(edge1, h))
    if -eps < a < eps:
        return False
    f = 1.0 / a
    s = origin - v0
    u = f * float(np.dot(s, h))
    if u < eps or u > 1.0 - eps:
        return False
    q = np.cross(s, edge1)
    v = f * float(np.dot(direction, q))
    if v < eps or u + v > 1.0 - eps:
        return False
    t = f * float(np.dot(edge2, q))
    return t > eps


def _segment_intersects_triangle(start: np.ndarray, end: np.ndarray, tri: np.ndarray, eps: float = 1e-9) -> bool:
    vector = end - start
    length = float(np.linalg.norm(vector))
    if length <= eps:
        return False
    direction = vector / length
    v0, v1, v2 = tri
    edge1 = v1 - v0
    edge2 = v2 - v0
    h = np.cross(direction, edge2)
    a = float(np.dot(edge1, h))
    if -eps < a < eps:
        return False
    f = 1.0 / a
    s = start - v0
    u = f * float(np.dot(s, h))
    if u < eps or u > 1.0 - eps:
        return False
    q = np.cross(s, edge1)
    v = f * float(np.dot(direction, q))
    if v < eps or u + v > 1.0 - eps:
        return False
    t = f * float(np.dot(edge2, q))
    return eps < t < length - eps


def _point_inside_mesh(point: np.ndarray, triangles: np.ndarray) -> bool:
    direction = np.asarray([1.0, 0.3713906764, 0.1135923668], dtype=float)
    direction = direction / np.linalg.norm(direction)
    count = 0
    shifted = point + np.asarray([1e-7, -2e-7, 3e-7], dtype=float)
    for tri in triangles:
        if _ray_intersects_triangle(shifted, direction, tri):
            count += 1
    return count % 2 == 1


def _meshes_collide(
    a: np.ndarray,
    b: np.ndarray,
    assumptions: PhysicalCorrectnessAssumptions,
) -> bool:
    if _mesh_min_distance(a, b, assumptions) > assumptions.collision_probe_distance_mm:
        return False
    tris_a = _sample_triangles(a, assumptions.max_sample_triangles)
    tris_b = _sample_triangles(b, assumptions.max_sample_triangles)
    for point in _sample_points(a, assumptions.max_sample_points):
        if _point_inside_mesh(point, tris_b):
            return True
    for point in _sample_points(b, assumptions.max_sample_points):
        if _point_inside_mesh(point, tris_a):
            return True
    return False


def _load_two_named_parts(
    condition: dict,
    parts_by_name: dict[str, dict],
    key_a: str = "part_a",
    key_b: str = "part_b",
) -> tuple[str | None, str | None, np.ndarray | None, np.ndarray | None, dict | None]:
    inputs = condition.get("inputs", {})
    name_a = inputs.get(key_a)
    name_b = inputs.get(key_b)
    if not name_a or not name_b:
        return None, None, None, None, _condition_result(
            condition, "inconclusive", f"{condition.get('check')} requires {key_a} and {key_b}"
        )
    tri_a, err_a = _load_part_triangles(parts_by_name, name_a)
    tri_b, err_b = _load_part_triangles(parts_by_name, name_b)
    errors = [err for err in (err_a, err_b) if err]
    if errors:
        return name_a, name_b, None, None, _condition_result(condition, "inconclusive", "; ".join(errors))
    return name_a, name_b, tri_a, tri_b, None


def _normalize_vector(values: Any) -> tuple[np.ndarray | None, str | None]:
    try:
        vector = np.asarray(values, dtype=float)
    except Exception:
        return None, "vector must be numeric"
    if vector.shape != (3,):
        return None, "vector must be [x, y, z]"
    length = float(np.linalg.norm(vector))
    if length <= 1e-12:
        return None, "vector must be non-zero"
    return vector / length, None


def _parse_vector(values: Any, label: str) -> tuple[np.ndarray | None, str | None]:
    try:

 succeeded in 0ms:
# Feature Retention

Use this reference for the LLM-only feature-retention metric.

## Definition

Feature retention scores whether the reconstruction preserves the intended functional and user-facing features described by the available design brief/spec/source evidence. It is separate from:

- printability: can the mesh likely be printed?
- physical correctness: do the parts fit, separate, move, contact, or avoid collision correctly?

Feature retention asks: did the reconstruction keep the right object semantics, feature inventory, visual read, roles, counts, placement, and important parametric intent?

Do not compare against unavailable original CAD, exact source B-rep, or unmeasured source dimensions. For from-scratch resemblance rebuilds, judge against the stated design intent and available source descriptions/images, not exact geometric fidelity to a source model.

## Evidence To Gather

Read:

- `spec.md`
- `README.md`
- `*_reconstruction_report.md`
- original prompt/source notes if present
- `params.py`
- `main.py`, `parts/`, `features/`, `assemblies/`, `validation.py`
- exported `.step`, `.stl`, `.3mf`, and part exports
- rendered images under `renders/`, or generate them if absent

The LLM may rerun the reconstruction project to gather evidence. Use this when exports are stale/missing, when a feature is generated only by code, when part exports are needed, or when a parameter variant would clarify whether a feature is genuinely preserved.

Examples:

- rerun the project check/export command used by that project;
- rebuild `.step`/`.stl` outputs;
- render the assembly and important separate parts;
- temporarily vary a documented parameter to verify feature generation, then restore the file;
- isolate a part or feature module if the project exposes a part builder.

Do not permanently change project behavior while evaluating unless the user asks for a fix. If files are edited only to probe a parameter variant, restore them or clearly report the temporary change.

## Rendering For Feature Retention

Use rendered views as evidence, not decoration. Prefer:

```bash
mkdir -p <project>/.eval_tmp/feature-retention-renders
python3 skills/step-to-cadquery/scripts/render_views.py <project>/<assembly-or-main>.step <project>/.eval_tmp/feature-retention-renders/assembly
```

Also render separate part STEP files or rebuilt part exports when feature evidence is local to a part. If only STL/3MF exists, use an available renderer/viewer workflow and state what was used. If no render can be produced, state that limitation and rely more heavily on code/spec evidence.

Use iso views for overall object identity and visual read. Use ortho views for feature counts, holes, slots, ribs, tabs, channels, openings, side-specific placement, and symmetry.

Generated renders, isolated part exports, parameter-variant exports, and probe outputs are intermediate evaluation artifacts. Keep their conclusions and referenced observations in the final evaluation report, then delete the generated files.

## Feature Inventory Procedure

Create a feature inventory before scoring:

1. Source/design features:
   - list required functional features from spec/source description/images;
   - include visible style-defining features when they affect object identity.
2. Reconstructed features:
   - list what the code and renders show;
   - include counts, side, orientation, placement, and whether each feature is parametric.
3. Compare:
   - `preserved`: feature is present with the same role and plausible placement;
   - `altered`: feature exists but role, scale, count, side, or shape is meaningfully changed;
   - `missing`: expected feature is absent;
   - `hallucinated`: extra feature changes the object's purpose or could confuse use;
   - `uncertain`: evidence is insufficient.

Feature classes to consider:

- containers, compartments, trays, shelves, channels, hooks, handles, knobs;
- holes, screw mounts, slots, vents, cable paths, drainage, honeycomb/perforation grids;
- hinges, latches, snaps, dovetails, clips, tabs, rails, dividers, drawers;
- decorative-but-identifying elements such as ribs, texture panels, bevel families, accent grooves;
- repeated feature counts and layout patterns;
- parametric controls that preserve important source variants.

## Scoring

Start from 10 and subtract by severity:

- critical missing/incorrect core function: cap at 4;
- major missing/altered functional feature: subtract 1.5 to 3;
- minor visual/style/count deviation: subtract 0.2 to 1;
- uncertainty alone: no deduction. Record it in `uncertain_features` or `notes`, but only subtract when the uncertainty is evidence of a likely missing/altered/hallucinated feature.

Suggested classes:

- `excellent`: score >= 9
- `good`: score >= 8
- `review`: score >= 5
- `poor`: score < 5

Use judgment. A simplified feature can still be preserved if it serves the same role and matches the intended design well enough. A beautiful but semantically different feature should score poorly.

## Report Schema

When running the full skill, feature retention should be folded into the final evaluation report instead of leaving a separate `feature_retention_report.json`. Use this schema inside the final report, or write a temporary JSON file under `<project>/.eval_tmp/` and delete it after merging:

```json
{
  "score": 8.0,
  "class": "good",
  "evidence": [
    "spec.md",
    "params.py",
    "temporary front ortho render observation"
  ],
  "feature_results": [
    {
      "feature": "front drawer pull scoops",
      "expected": "each drawer has a handle-free finger scoop",
      "observed": "drawer.py cuts a cylindrical scoop; front render shows one per drawer",
      "status": "preserved",
      "severity": "major",
      "evidence": ["spec.md", "parts/drawer.py", "front ortho render"]
    }
  ],
  "missing_features": [],
  "altered_features": [],
  "hallucinated_features": [],
  "uncertain_features": [],
  "rerun_actions": [
    "rebuilt assembly STEP",
    "rendered assembly and body/lid parts"
  ],
  "notes": []
}
```

Keep evidence concrete. A report is weak if it only says "looks good" without tying each important feature to source/design evidence and observed reconstruction evidence.

codex
The manifest reference already documents vent proxies, which means the previous loop’s candidate may already have been implemented in this checkout. I’m checking the scorer and project evidence next so any new change is based on this batch rather than stale loop context.
exec
/bin/zsh -lc 'rg -n "def _check|vent_|clear_path|feature_count|_run_condition|relative_pose|contact_graph" skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
327:def _check_part_component_count(condition: dict, parts_by_name: dict[str, dict]) -> dict:
348:def _check_part_collision(
366:def _check_part_clearance(
402:def _check_part_contact(
427:def _check_linear_motion_collision(
490:def _check_linear_motion_clearance(
552:def _check_rotation_motion_collision(
592:def _check_axis_alignment(condition: dict) -> dict:
629:def _check_relative_pose(
665:def _check_opening_presence(
708:def _check_vent_opening_proxy(
716:        return _condition_result(condition, "inconclusive", "vent_opening_proxy requires part")
722:        return _condition_result(condition, "inconclusive", "vent_opening_proxy requires non-empty rays")
781:def _check_clear_path_proxy(
789:        return _condition_result(condition, "inconclusive", "clear_path_proxy requires part")
795:        return _condition_result(condition, "inconclusive", "clear_path_proxy requires non-empty paths")
801:            "max_intersections_per_clear_path",
805:    min_clear_paths = thresholds.get("min_clear_paths", thresholds.get("min_clear_rays"))
807:    if min_clear_paths is None and min_clear_fraction is None:
837:    if min_clear_paths is not None:
838:        passed = passed and clear_count >= int(min_clear_paths)
847:            "clear_path_count": clear_count,
850:            "min_clear_paths": int(min_clear_paths) if min_clear_paths is not None else None,
852:            "max_intersections_per_clear_path": max_intersections,
859:def _check_vent_grid_open_area_proxy(
867:        return _condition_result(condition, "inconclusive", "vent_grid_open_area_proxy requires part")
886:        return _condition_result(condition, "inconclusive", "vent_grid_open_area_proxy requires numeric ray_length_mm")
950:def _check_feature_count(condition: dict, parts_by_name: dict[str, dict]) -> dict:
958:        return _condition_result(condition, "inconclusive", "feature_count requires features or part_name_prefix")
983:def _check_cylindrical_fit(condition: dict) -> dict:
1026:def _check_contact_graph(
1085:def _check_assembly_component_count(condition: dict, assembly_report: dict | None, part_reports: list[dict]) -> dict:
1122:def _check_assembly_sequence(
1148:        result = _run_condition(subcondition, assembly_report, parts_by_name, part_reports, assumptions)
1176:def _run_condition(
1202:    if check == "relative_pose":
1203:        return _check_relative_pose(condition, parts_by_name)
1206:    if check == "clear_path_proxy":
1207:        return _check_clear_path_proxy(condition, parts_by_name, assumptions)
1208:    if check == "vent_opening_proxy":
1209:        return _check_vent_opening_proxy(condition, parts_by_name, assumptions)
1210:    if check == "vent_grid_open_area_proxy":
1211:        return _check_vent_grid_open_area_proxy(condition, parts_by_name, assumptions)
1212:    if check == "feature_count":
1213:        return _check_feature_count(condition, parts_by_name)
1216:    if check == "contact_graph":
1217:        return _check_contact_graph(condition, parts_by_name, assumptions)
1280:        _run_condition(condition, baseline["assembly"], parts_by_name, part_reports, assumptions)

exec
/bin/zsh -lc "for p in articulated-snowman-fidget articulated-tie-step-file-included aspire-planter astronomical-telescope-hadley-an-easy-assembly-hig; do printf '%s\\n' \""'$p"; sed -n '"'1,220p' outputs/reconstructions/step-to-cadquery/"'$p/spec.md; done' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc "for p in articulated-snowman-fidget articulated-tie-step-file-included aspire-planter astronomical-telescope-hadley-an-easy-assembly-hig; do printf '%s\\n' \""'$p"; sed -n '"'1,220p' outputs/reconstructions/step-to-cadquery/"'$p/params.py; done' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc "for p in articulated-snowman-fidget articulated-tie-step-file-included aspire-planter astronomical-telescope-hadley-an-easy-assembly-hig; do printf '%s\\n' \""'$p"; sed -n '"'1,260p' outputs/reconstructions/step-to-cadquery/"'$p/main.py; done' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
articulated-snowman-fidget
"""All dimensions for the Articulated Snowman Fidget project.

The source STEP shows THREE example configurations of the same snowman
"kit" laid out side by side (a common way these customizable-Printables
kits get photographed/exported): a scarf+3-button chest, a sweater-collar
chest, and a bare/no-accessory body. All three share the SAME ball-segment
shape family (bottom / chest / head are each a hollowed sphere) - only a
few small features (a hole radius, a knot-slot size) differ per instance.

Edit values here, not inside geometry. Dimensions are float (mm); counts
are int (the perturb gate steps them by a whole unit).
"""

from dataclasses import dataclass


@dataclass
class Params:
    # AI_EDITABLE: dimensions only. Add new fields below as features need them.

    # ----- Ball-segment radii + shell wall (shared shape family, all 3 snowmen) -----
    bottom_radius: float = 25.0
    bottom_wall: float = 3.5
    chest_radius: float = 20.0
    chest_wall: float = 1.0          # nominal skin thickness at the equator
    head_radius: float = 15.0
    head_wall: float = 3.5

    # ----- Vertical placement of each ball's centre (global Z, group-local) -----
    bottom_centre_z: float = -27.032
    chest_centre_z: float = 0.0
    head_centre_z: float = 27.562

    # ----- Bottom: flat foot + strap bore + knot-slot -----
    bottom_flat_z: float = -44.709        # matches the source's measured flat foot exactly
    bottom_bore_radius: float = 4.0       # central strap channel through the solid foot
    bottom_slot_radius: float = 8.5       # generous stand-in for the true ~13.5x7.5 knot slot
    bottom_cavity_margin: float = 0.02    # numerical-noise-scale only (see pitfalls.md #2)

    # ----- Chest: thin equatorial shell pinching to a socket at both poles -----
    chest_bottom_z: float = -9.049        # matches the source's measured extent exactly
    chest_top_z: float = 16.456
    chest_cavity_margin: float = 0.05
    chest_button_hole_radius: float = 2.3
    chest_button_count: int = 3

    # ----- Head: hollow lower dome, solid crown (holds face pegs + hat seat) -----
    head_bottom_z: float = 14.396         # matches the source's measured extent exactly
    head_top_z: float = 38.169
    head_cavity_margin: float = 0.05
    head_eye_hole_radius: float = 2.1
    head_nose_hole_radius: float = 2.6

    # ----- Hat: stepped brim + crown + slight-taper cap -----
    hat_brim_z: float = 38.59
    hat_brim_radius: float = 15.9
    hat_brim_height: float = 2.0
    hat_crown_lower_radius: float = 10.0
    hat_crown_lower_height: float = 8.0
    hat_crown_upper_radius_lo: float = 9.0
    hat_crown_upper_radius_hi: float = 9.85
    hat_crown_upper_height: float = 8.9
    hat_cap_radius: float = 9.5
    hat_cap_height: float = 0.25

    # ----- Nose (carrot peg + tapered tip) -----
    nose_peg_radius: float = 2.2
    nose_peg_depth: float = 1.9
    nose_tip_radius: float = 1.05
    nose_tip_length: float = 3.8

    # ----- Eye peg (dome cap itself is a frozen residual, see spec.md) -----
    eye_peg_radius: float = 1.7
    eye_peg_depth: float = 0.7
    eye_spacing_x: float = 10.246          # distance between the two eye pegs

    # ----- Button peg (chest ornament) -----
    button_peg_radius: float = 1.6
    button_peg_depth: float = 0.6

    # ----- Snowman-group layout (the 3 example configs shown side by side) -----
    group_spacing: float = 50.0            # X pitch between snowman A / B / C
articulated-tie-step-file-included
"""Parametric model of the Articulated Tie (10 print-in-place hinged links +
a wide tapered tip cap + a spring clip).

Frame convention used throughout main.py: every tapered "link" (and the tip
cap) is built in its own LOCAL frame where local X = the panel WIDTH axis
(this always coincides with the model's GLOBAL X axis -- confirmed by
introspect.py: every hinge pin/hole in the source STEP has axis "X"), local Y
= the panel LENGTH axis (0 at the socket-end hinge pivot, +link_length at the
boss-end hinge pivot), local Z = the panel THICKNESS axis. A link is placed
in the world by rotating this local solid about the GLOBAL X axis by a
cumulative angle theta_i, then translating so local (0,0,0) lands on that
link's socket-end pivot point in the Y-Z plane.

theta advances link-to-link by the measured "hinge_angle_i" params below --
these are the SAME hinges the readme calls "print in place hinges" with
"0.3mm tolerance": in the shipped STEP file they are posed at the folded,
compact layout the author used to fit the tie on a Prusa Mini+ bed (see
design_readme.md: "I rotated the hinges in a way that I can print the tie on
my Mini+"). Setting every hinge_angle_* to 0 straightens the whole tie into
one flat blade -- the "as worn" shape -- which is the whole point of
recovering the hinge angles as live parameters instead of a baked mesh.
"""

from dataclasses import dataclass


@dataclass
class Params:
    # ---- chain topology -----------------------------------------------
    n_links: int = 10          # tapered link segments between the clip and the tip cap

    # ---- per-link taper (constant step recovered from the measured
    # width sequence 22.0, 26.615, 31.231, ... 68.154mm -- each link's own
    # taper matches the inter-link taper exactly, so one width_step drives
    # both) ---------------------------------------------------------------
    neck_width: float = 22.0          # width at the socket end of link 0 (nearest the clip)
    width_step: float = 4.615384615384615   # width gained per link (both within a link and link-to-link)

    # ---- shared link geometry (identical across all 10 links) ---------
    link_length: float = 37.5         # hinge-pivot to hinge-pivot distance
    thickness: float = 6.0            # panel thickness
    shoulder_length: float = 34.0     # length at which the taper body ends and the tab begins
    tab_half_width: float = 6.0       # half-width of the protruding male tab / boss end
    notch_half_width: float = 5.0     # half-width of the female notch cut at the socket end
    notch_depth: float = 6.0          # how far the notch is cut in from the socket end

    # ---- hinge pin / bore (0.3mm/side clearance per design_readme.md) -
    pin_radius: float = 1.5           # male boss/pin radius
    hinge_clearance: float = 0.3      # radial clearance between pin and bore
    pin_reach: float = 2.0            # how far the pin protrudes past the tab's flat sides (into the mating ears)

    # ---- fold pose (baseline reproduces the AS-SHIPPED print layout) --
    # theta_0 = link0_dir_angle; theta_i = theta_(i-1) + hinge_angle_i
    link0_dir_angle: float = -20.0
    hinge_angle_1: float = -90.0
    hinge_angle_2: float = -80.0
    hinge_angle_3: float = 0.0
    hinge_angle_4: float = -80.0
    hinge_angle_5: float = -80.0
    hinge_angle_6: float = 0.0
    hinge_angle_7: float = -30.0
    hinge_angle_8: float = -80.0
    hinge_angle_9: float = -20.0
    tip_hinge_angle: float = -60.0    # joint between link 9 and the tip cap

    # anchor: the socket-end pivot of link 0 (== the clip's boss pivot), in
    # the (global Y, global Z) plane. All global X stays 0-centred.
    chain_origin_y: float = -403.889
    chain_origin_z: float = 69.749

    # ---- tip cap (solid 10): a wide, pointed "kite" profile, not a
    # trapezoid -- measured directly off the source STEP's vertex list ----
    tip_shoulder_x: float = 34.23     # half-width where the flat top edge (shared w/ link 9) ends
    tip_flare_len: float = 18.5       # local length at the widest point
    tip_flare_x: float = 35.0         # half-width at the widest point (a slight outward flare)
    tip_knee_len: float = 29.8        # local length of the profile's knee (taper-angle change)
    tip_knee_x: float = 17.4          # half-width at the knee
    tip_len: float = 41.0             # local length of the apex (the tie's point)

    # ---- clip (solid 11) ------------------------------------------------
    # NOT reconstructed: shipped as a pure frozen import (parts/residual_11.step
    # == the whole original solid, no scaffold at all). A plate+tab scaffold
    # attempt (an early build_clip_scaffold()) was abandoned after an 18-angle
    # rotation sweep still overshot the original by >40% of its own volume at
    # its best fit -- see pitfalls.md. Its PLACEMENT still rides
    # chain_origin_y/z (see gen_step()), just not its shape.


def here(rel):
    import os
    return os.path.join(os.path.dirname(os.path.abspath(__file__)), rel)
aspire-planter
"""All dimensions for the recovered Aspire Planter set — "All Sizes".

Three round, reeded (fluted) planter pots in three sizes, each paired with a
shallow round saucer/tray — six solids total, laid out in a row along +Y in the
source frame:

    solid 0  pot   small   D 114.6  H 106.5   65 flutes
    solid 1  saucer small   D  98.0  H  13.2
    solid 2  pot   medium  D 165.3  H 157.2   85 flutes
    solid 3  saucer medium  D 139.0  H  17.4
    solid 4  pot   large   D 214.4  H 208.2  105 flutes
    solid 5  saucer large   D 180.0  H  22.2

Each POT is one parametric family `build_pot()`: a vertical *reeded wall* whose
radius oscillates between a ridge cylinder (`ridge_r`) and a valley cylinder
(`ridge_r − reed_depth`) via flat radial flanks joined by `fillet_r` corner
fillets — a rounded-tooth "gear" cross-section — sitting on a domed, footed,
drilled base. The reeded wall + cavity bore + 45° top-rim chamfer are
reconstructed as parametric code; the rounded base cap below the wall seam (a
flute-modulated spherical dome + foot + inner bowl + ~17 square drainage holes +
an embossed "JP" maker's logo) is a FROZEN residual import — genuinely freeform
where the reeds melt into the dome, so it is editable in placement (it rides the
wall seam) but not in shape. See spec.md.

Each SAUCER is one parametric family `build_saucer()`: a round dish — outer wall
cylinder, recessed floor, inner cavity wall, 45° rim chamfer — reconstructed as
code; the floor locating tabs / "JP" logo / (large saucer) a concentric
drainage-hole grid are the frozen residual.

Editability conventions (so every knob survives the perturb gate):
  * Counts (`*_flutes`) are `int`; lengths are `float`.
  * Wall thicknesses are stored as OFFSETS, not as inner radii: the bore is
    `valley_r − wall`, the cavity is `out_r − wall` — so nudging a wall stays
    valid instead of crossing the outer surface.
  * Floor depth is `floor_t` above the underside (`floor_z = bot_z + floor_t`),
    not an absolute z, so a nudge can't sink the floor below the base.
  * The six parts sit on a 1-D row along +Y at x = 0, so only `*_y` is a layout
    knob (no redundant x).
Reed clocking (the azimuth of reed #0) is a recovered constant in main.py, not a
design knob — a pure rotation is invisible to the mass-signature gate.
"""

from dataclasses import dataclass


@dataclass
class Params:
    # ================= shared design language =================================
    rim_chamfer: float = 1.2          # 45° chamfer height on every pot's top inner rim
    saucer_base_freeze: float = 0.55  # underside slab kept in the frozen residual so the
    #                                   ~0.4 mm DEBOSSED "JP" logo + bottom-edge round on
    #                                   each saucer's underside survive verbatim (a flat
    #                                   scaffold there would fill the debossed pockets)

    # ================= POT small (solid 0) ====================================
    p0_flutes: int = 65               # number of vertical reeds
    p0_ridge_r: float = 57.302        # outer radius at a reed crest
    p0_reed_depth: float = 4.064      # crest→valley radial depth (valley_r = ridge−this)
    p0_fillet_r: float = 0.864        # reed corner fillet radius
    p0_tip_deg: float = 1.285         # angular width of the crest (ridge) arc
    p0_valley_deg: float = 0.671      # angular width of the root (valley) arc
    p0_wall: float = 2.438            # wall thickness at the valley (bore = valley − wall)
    p0_seam_z: float = 0.76           # z where reeds start (top of frozen base cap)
    p0_top_z: float = 66.70           # z of the rim top
    p0_y: float = 0.5                 # seat in the row

    # ================= POT medium (solid 2) ===================================
    p2_flutes: int = 85
    p2_ridge_r: float = 82.668
    p2_reed_depth: float = 4.043
    p2_fillet_r: float = 1.011
    p2_tip_deg: float = 0.817
    p2_valley_deg: float = 0.546
    p2_wall: float = 2.425
    p2_seam_z: float = 0.71
    p2_top_z: float = 99.48
    p2_y: float = 150.5

    # ================= POT large (solid 4) ====================================
    p4_flutes: int = 105
    p4_ridge_r: float = 107.2
    p4_reed_depth: float = 4.0
    p4_fillet_r: float = 1.0
    p4_tip_deg: float = 0.750
    p4_valley_deg: float = 0.500
    p4_wall: float = 2.4
    p4_seam_z: float = 0.0
    p4_top_z: float = 132.40
    p4_y: float = 440.0

    # ================= SAUCER small (solid 1) =================================
    s1_out_r: float = 48.996          # outer wall radius
    s1_wall: float = 2.439            # wall thickness (cavity = out_r − wall)
    s1_bot_z: float = -47.9           # underside
    s1_floor_t: float = 2.0           # floor thickness (cavity bottom = bot_z + floor_t)
    s1_top_z: float = -34.68          # rim top
    s1_rim_chamfer: float = 1.3       # 45° chamfer height on the inner rim
    s1_y: float = 0.5

    # ================= SAUCER medium (solid 3) ================================
    s3_out_r: float = 69.481
    s3_wall: float = 2.426
    s3_bot_z: float = -67.0
    s3_floor_t: float = 3.2
    s3_top_z: float = -49.7
    s3_rim_chamfer: float = 1.4
    s3_y: float = 150.5

    # ================= SAUCER large (solid 5) =================================
    s5_out_r: float = 89.992
    s5_wall: float = 2.4
    s5_bot_z: float = -86.0
    s5_floor_t: float = 2.5           # below the deepest floor step; the stepped floor,
    #                                   centre boss and drain grid ride the frozen residual
    s5_top_z: float = -63.8
    s5_rim_chamfer: float = 1.5
    s5_y: float = 440.0
astronomical-telescope-hadley-an-easy-assembly-hig
"""Parametric recovery of the Hadley Newtonian telescope 3D-printed hardware
kit (HadleySource.step).

Source: models/crawled/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.step
12 solids, 11 canonicals (solids 6 and 11 are a left/right mirror pair of the
same "clamp" part, matched by volume to 6 significant figures: 55491.749 vs
55491.748 mm^3 -- everything else is unique).

THIS IS A TIME-BOXED PARTIAL RECOVERY (see spec.md "Session history" and the
skill-feedback/pitfalls docs for the full story): an earlier attempt in this
same session fanned out one agent per solid to reconstruct each canonical in
full detail; the session was interrupted mid-flight and every one of those
agents was killed before finishing (only 2 left usable partial code, which
itself did not reproduce the source geometry when re-tested). Rather than
re-attempt that per-feature depth from scratch under a hard time budget, this
file ships a smaller, DELIBERATELY SAFE set of live parameters recovered by
direct point-classification probing of the B-rep (BRepClass3d_SolidClassifier)
rather than by trusting introspect.py's rounded per-feature JSON -- every
radius/z-bound below was found by bisecting IN/OUT samples against the real
solid, so the resulting scaffold is a *verified* subset of the original
(overshoot ~0 mm^3 on all three, confirmed with metrics.py --overshoot).

3 of the 11 canonicals reconstruct as live parametric code (this file's
params): the primary structural RING of each of the telescope's two main tube
collars (uta_*, lta_*) and the focuser retainer/hub disc (hub_*). Each is
modelled as 1-3 concentric partial/full cylindrical bands -- the single
largest, unambiguous analytic surface in that solid -- deliberately leaving
every boss, tab, lug, and mounting hole to the frozen residual. This is an
honest, DEEP undershoot relative to what the source geometry actually offers
(each of these 3 solids has plenty more recoverable analytic feature area --
see spec.md's Reconstructability section and the punted_fraction numbers
there), not a claim that the rest is unreconstructable.

The other 8 canonicals (mount, spider, collim, baffle, clamp x2 instances,
cell, bktc, bktr) ship as pure frozen imports (parts/frozen_<name>.step) with
NO scaffold at all -- see spec.md for what each one is and why it was not
attempted this session.
"""

from dataclasses import dataclass


@dataclass
class Params:
    # ---- uta: solid 0, Upper Tube Assembly ring (partial band only) -------
    # Verified analytic band: a constant-radius cylindrical wall from
    # uta_z_lo to uta_z_hi, MINUS an angular wedge (uta_gap_start_deg to
    # uta_gap_end_deg) where the source has a large focuser-mount cutout.
    # Everything else (the diagonal focuser flange, 3 truss-pole lugs, crown
    # holes, embossed "774 / Hadley" logo) is in the frozen residual.
    uta_ir: float = 62.15            # ring inner radius (measured true value 62.5, inset for safety)
    uta_or: float = 62.85            # ring outer radius (measured true value 62.5-63, inset for safety)
    uta_z_lo: float = 720.5          # ring bottom, world Z (measured true bottom 720.0)
    uta_z_hi: float = 790.0          # ring top, world Z (band tapers above this; measured band top ~791)
    uta_gap_start_deg: float = 95.0  # start of the focuser-cutout angular gap (measured ~98-100 deg)
    uta_gap_end_deg: float = 170.0   # end of the focuser-cutout angular gap (measured ~165-168 deg)

    # ---- lta: solid 10, Lower Tube Assembly ring (partial band only) ------
    # Verified analytic band: a constant-radius FULL 360 deg cylindrical
    # wall. The 3 radial truss-pole mounting tabs (120 deg apart, each with
    # 2 through-holes) pinch the true OD down below lta_or at 3 angular
    # positions (measured at 90/210/330 deg) -- lta_or is inset to clear all
    # three pinch points with zero overshoot. Tabs are in the residual.
    lta_ir: float = 60.6             # ring inner (bore) radius (measured true value 60.0-61.0)
    lta_or: float = 62.8             # ring outer radius (measured true tab-pinched value ~63.0-64.0)
    lta_z_lo: float = 507.5          # ring bottom, world Z (measured true bottom ~506.0)
    lta_z_hi: float = 538.5          # ring top, world Z (measured true top ~540.0)

    # ---- hub: solid 1, focuser retainer / hub-and-rim disc -----------------
    # Verified as 3 disjoint concentric rings (hub bore ring + outer rim ring
    # + a thin top lip ring), each a plain annulus over a Z band. The 3
    # spoke arms tying them together, the 3 pairs of hex/circle bosses on
    # the rim, and the small screw/peg holes are all in the residual.
    hub_bore_r: float = 15.2         # hub-ring inner radius (measured true ~15.0)
    hub_hubring_or: float = 22.8     # hub-ring outer radius (measured true ~23.0)
    hub_riminner_r: float = 52.2     # rim-ring inner radius (measured true ~52.0)
    hub_rimouter_r: float = 56.8     # rim-ring outer radius (measured true ~57.0)
    hub_lip_ir: float = 58.05        # top lip inner radius (measured true ~58.0)
    hub_lip_or: float = 58.35        # top lip outer radius (measured true ~58.5)
    hub_z_lo: float = -29.8          # bottom of hub/rim rings, world Z (measured true bottom -30.0)
    hub_z_mid: float = -20.2         # top of hub/rim rings = step to the lip, world Z (measured ~-20.0)
    hub_z_lipLo: float = -21.8       # bottom of the top lip ring, world Z (measured ~-22 to -25 transition)
    hub_z_hi: float = -17.2          # top of the part, world Z (measured true top -17.0)

 succeeded in 0ms:
articulated-snowman-fidget
"""Project entrypoint -- Articulated Snowman Fidget.

The source STEP holds THREE example configurations of the same posable
snowman "kit" side by side (a scarf+3-button chest, a sweater-collar chest,
and a bare/no-accessory body) -- see spec.md for the full read of the part
and design_readme.md for the original Printables options this reflects.

``gen_step()`` builds each ball segment's parametric scaffold, re-imports
its frozen residual sidecar (parts/residual_<solid_index>.step -- the socket
funnel/floor/knurl detail the scaffold didn't reconstruct), fast_unions the
two, and assembles all three snowmen into one compound. No cross-solid
boolean is needed: the snowmen (and their sub-parts) are independent,
merely-adjacent bodies in the source, so combining them is a plain compound
-- fast and exact.
"""

from __future__ import annotations

import pathlib
import sys

import cadquery as cq
from OCP.ShapeUpgrade import ShapeUpgrade_UnifySameDomain

HERE = pathlib.Path(__file__).parent
sys.path.insert(0, str(HERE))

from params import Params
from validation import validate_params
from booleans import fast_union
import geometry as geo


def _here(name: str) -> str:
    return str(HERE / name)


def _residual(idx: int, x_shift: float = 0.0) -> cq.Workplane:
    wp = cq.importers.importStep(_here(f"parts/residual_{idx}.step"))
    if x_shift:
        wp = wp.translate((x_shift, 0, 0))
    return wp


def _unify(shape):
    """Merge coplanar/cocylindrical faces the scaffold+residual seam split in
    two -- a redundant seam the source never had (topology gate). Cheap and
    safe here: none of these parts have threads/knurling to over-fuse."""
    upgrader = ShapeUpgrade_UnifySameDomain(shape.wrapped, True, True, True)
    upgrader.Build()
    return cq.Shape.cast(upgrader.Shape())


def _wrap(scaffold_solid, idx: int, x_shift: float = 0.0) -> cq.Shape:
    """fast_union a placed scaffold solid with its frozen residual sidecar.

    The residual sidecar was captured against the scaffold placed at the
    BASELINE group_spacing (50mm); x_shift carries any drift of that
    baseline so the residual keeps riding the scaffold's seam exactly
    (SKILL.md "ride placement on the seam param") instead of going stale
    when group_spacing is edited.
    """
    result = fast_union(cq.Workplane(obj=scaffold_solid), _residual(idx, x_shift))
    solids = result.val().Solids()
    if len(solids) == 1:
        return _unify(solids[0])
    return cq.Compound.makeCompound([_unify(s) for s in solids])


# (solid_index, group_multiplier) tables -- group_multiplier * p.group_spacing
# is the group's live X offset; residual sidecars were captured at the
# BASELINE group_spacing=50.0, see build_residuals.py / spec.md's "solid
# index" table for how these map back to the source STEP.
_BASELINE_SPACING = 50.0
_BOTTOM = {"A": (2, 0), "B": (11, 1), "C": (16, 2)}
_CHEST = {"A": (0, 0), "B": (20, 1), "C": (7, 2)}
_HEAD = {"A": (1, 0), "B": (8, 1), "C": (17, 2)}
_HAT = {"A": (4, 0), "B": (10, 1)}
_NOSE = {"A": (5, 0), "B": (9, 1)}
_EYE_L = {"A": (12, 0, -1), "B": (14, 1, 1)}
_EYE_R = {"A": (13, 0, 1), "B": (15, 1, -1)}
_BUTTON = {"A": (18, 0), "B": (19, 1)}


def _build_group(p: Params, group: str, with_face: bool, with_hat: bool, accessory: str | None):
    """Build one snowman ("A", "B", or "C") and return a list of placed Shapes."""
    parts = []
    drift = lambda mult: mult * (p.group_spacing - _BASELINE_SPACING)  # noqa: E731

    idx, mult = _BOTTOM[group]
    dx = mult * p.group_spacing
    parts.append(_wrap(geo.build_bottom(p).translate((dx, 0, 0)), idx, drift(mult)))

    idx, mult = _CHEST[group]
    dx = mult * p.group_spacing
    parts.append(_wrap(geo.build_chest(p).translate((dx, 0, 0)), idx, drift(mult)))

    idx, mult = _HEAD[group]
    dx = mult * p.group_spacing
    parts.append(_wrap(geo.build_head(p).translate((dx, 0, 0)), idx, drift(mult)))

    if with_hat:
        idx, mult = _HAT[group]
        dx = mult * p.group_spacing
        parts.append(_wrap(geo.build_hat(p).translate((dx, 0, 0)), idx, drift(mult)))

    if with_face:
        idx, mult = _NOSE[group]
        dx = mult * p.group_spacing
        parts.append(_wrap(geo.build_nose(p, cx=dx), idx, drift(mult)))

        idx, mult, sign = _EYE_L[group]
        dx = mult * p.group_spacing
        parts.append(_wrap(geo.build_eye(p, cx=dx, xs=sign * p.eye_spacing_x / 2), idx, drift(mult)))
        idx, mult, sign = _EYE_R[group]
        dx = mult * p.group_spacing
        parts.append(_wrap(geo.build_eye(p, cx=dx, xs=sign * p.eye_spacing_x / 2), idx, drift(mult)))

        idx, mult = _BUTTON[group]
        dx = mult * p.group_spacing
        parts.append(_wrap(geo.build_button(p, cx=dx), idx, drift(mult)))

    if accessory == "scarf":
        parts.append(_residual(3, drift(0)).val())     # fully frozen (freeform wrap)
    elif accessory == "collar":
        parts.append(_residual(6, drift(1)).val())     # fully frozen (knurl/micro-texture)

    return parts


def gen_step():
    p = Params()
    validate_params(p)

    shapes = []
    shapes += _build_group(p, "A", with_face=True, with_hat=True, accessory="scarf")
    shapes += _build_group(p, "B", with_face=True, with_hat=True, accessory="collar")
    shapes += _build_group(p, "C", with_face=False, with_hat=False, accessory=None)

    return cq.Workplane(obj=cq.Compound.makeCompound(shapes))


if __name__ == "__main__":
    result = gen_step()
    n = len(result.val().Solids())
    vol = sum(s.Volume() for s in result.val().Solids())
    print(f"gen_step(): {n} solids, total volume {vol:.1f} mm3")
articulated-tie-step-file-included
"""Articulated Tie -- parametric rebuild.

10 tapered "living hinge" links (print-in-place: each ends in a female
notch+bore socket on one side and a male tab+pin boss on the other), a wide
pointed tip cap, and a shirt clip. See params.py's module docstring for the
local-frame / placement convention.

gen_step() returns a cq.Assembly-free compound: every link is placed by a
pure rotate+translate (no boolean needed for the CHAIN itself -- the links
are disjoint by construction, same as any dedup'd instanced assembly). Each
individual link/cap/clip is itself `scaffold` (or `scaffold + residual`,
fast_union'd) before being placed.
"""
import math
import cadquery as cq
from OCP.ShapeUpgrade import ShapeUpgrade_UnifySameDomain

from params import Params, here
from booleans import fast_cut, fast_union

X_AXIS = cq.Vector(1, 0, 0)


def _unify(w):
    """Merge same-surface coplanar/cocylindrical faces the scaffold+residual
    fast_union leaves split (a redundant seam the source doesn't have -- the
    topology gate's `redundant_seams` check). Safe here: no threaded/knurled
    body in this part (the one case SKILL.md warns UnifySameDomain over-fuses)."""
    upg = ShapeUpgrade_UnifySameDomain(w.val().wrapped, True, True, True)
    upg.Build()
    return cq.Workplane(obj=cq.Shape(upg.Shape()))


# --------------------------------------------------------------------------
# geometry primitives (local frame: X=width, Y=length 0..link_length, Z=thickness)
# --------------------------------------------------------------------------

def _cylinder_x(radius, x_lo, x_hi, y0, z0):
    """A solid cylinder whose axis is the local X axis, spanning [x_lo, x_hi]."""
    return cq.Workplane(obj=cq.Solid.makeCylinder(
        radius, x_hi - x_lo, cq.Vector(x_lo, y0, z0), X_AXIS))


def _box(x_lo, x_hi, y_lo, y_hi, z_lo, z_hi):
    return cq.Workplane(obj=cq.Solid.makeBox(
        x_hi - x_lo, y_hi - y_lo, z_hi - z_lo, cq.Vector(x_lo, y_lo, z_lo)))


def _half_barrel(p, x_lo, x_hi, y0, z0, bulge_sign, r=None):
    """Half-round hinge-knuckle roll: a radius=thickness/2 cylinder (axis X,
    spanning [x_lo, x_hi]) intersected with the half-space on the bulge_sign
    side of y0 -- the standard "rolled sheet edge" hinge-knuckle shape."""
    if r is None:
        r = p.thickness / 2.0
    cyl = _cylinder_x(r, x_lo, x_hi, y0, z0)
    if bulge_sign > 0:
        half = _box(x_lo - 1, x_hi + 1, y0, y0 + r + 1e-6, z0 - r - 1, z0 + r + 1)
    else:
        half = _box(x_lo - 1, x_hi + 1, y0 - r - 1e-6, y0, z0 - r - 1, z0 + r + 1)
    return cyl.intersect(half)


def build_link(p, width_lo, width_hi, has_socket=True, has_boss=True):
    """One tapered hinge link in its own local frame (socket-end pivot at
    local origin, boss-end pivot at local (0, link_length, 0))."""
    t = p.thickness

    # The socket end is NOT a flat full-width edge: it tapers diagonally from
    # the narrow notch corner out to the full ear width over notch_depth (a
    # flat full-width ear ledge here overshoots the original -- verified the
    # same way as the tip cap's identical fix, see build_tip's docstring).
    # `m` is a small (~0.5mm) safety inset on the outer taper edge: a thin
    # sliver along the FULL taper length measured as overshoot (the true
    # edge likely carries a small fillet/round this straight-line profile
    # doesn't capture) -- insetting keeps the scaffold strictly inside the
    # original (mandatory) and pushes that sliver into the residual instead.
    m = 0.5
    wl, wh = width_lo / 2 - m, width_hi / 2 - m
    if has_socket:
        socket_profile = [
            (-p.notch_half_width, 0), (-p.notch_half_width, p.notch_depth * 0.97),
            (-wl, p.notch_depth),
            (-wh, p.shoulder_length), (wh, p.shoulder_length),
            (wl, p.notch_depth),
            (p.notch_half_width, p.notch_depth * 0.97), (p.notch_half_width, 0),
        ]
    else:
        socket_profile = [
            (-wl, 0), (wl, 0),
            (wh, p.shoulder_length), (-wh, p.shoulder_length),
        ]
    body = (
        cq.Workplane("XY")
        .polyline(socket_profile)
        .close()
        .extrude(t)
        .translate((0, 0, -t / 2))
    )

    if has_boss:
        # The boss end is NOT a flat tab plate with a big rolled barrel cap
        # (that was an initial hypothesis, disproven by measurement -- a
        # thickness/2-radius barrel there overshoots the original by ~75% of
        # its own volume regardless of bulge direction). It is simply a bare
        # round PIN (radius = pin_radius, not thickness/2) protruding
        # straight out of the taper body at the pivot -- a plain full
        # cylinder there measures ~0 overshoot at any tested reach. There is
        # a small unfilled relief gap between the taper body's edge
        # (shoulder_length) and the pin -- real geometry, not a modelling
        # gap (a slab occupancy scan against the source found 0% fill in
        # that gap at every width tried).
        pin_r = p.pin_radius
        body = body.union(_cylinder_x(pin_r,
                                       -(p.tab_half_width + p.pin_reach),
                                       (p.tab_half_width + p.pin_reach),
                                       p.link_length, 0.0))

    if has_socket:
        hole_r = p.pin_radius + p.hinge_clearance
        # Measured: the ear's rounded lip is only as big as the bore hole
        # itself (radius=hole_r), NOT a thickness/2 rolled barrel like the
        # notch's OWN half-round reads at first glance -- a thickness/2
        # radius here overshoots by ~2.5% of the link's volume; hole_r
        # overshoots by <0.2% (down to float/mesh noise).
        notch = _box(-p.notch_half_width, p.notch_half_width,
                     -(p.thickness / 2 + 1), p.notch_depth,
                     -t / 2 - 1, t / 2 + 1)
        body = body.union(_half_barrel(p, -wl, wl, 0.0, 0.0, -1, r=hole_r))
        body = body.cut(notch)
        body = body.cut(_cylinder_x(hole_r, -(width_lo / 2 + 2), (width_lo / 2 + 2), 0.0, 0.0))

    return body


def build_tip(p):
    """Tip cap (solid 10): a wide pointed "kite" profile, socket end only,
    no continuing boss. Unlike a link's socket end, the tip cap has NO flat
    full-width ear ledge -- the outline tapers directly (diagonally) from
    the narrow notch corner straight out to the wide shoulder corner over
    just notch_depth of length (verified against the source's own vertex
    list: a flat-ledge assumption here overshoots the original by ~2300
    mm3, one straight diagonal edge does not)."""
    t = p.thickness
    m = 0.5  # same outer-edge safety inset as build_link (see its docstring)
    xw = p.tip_shoulder_x - m
    flare_x = p.tip_flare_x - m
    knee_x = p.tip_knee_x - m
    profile = [
        (p.notch_half_width, 0.0), (p.notch_half_width, p.notch_depth * 0.97), (xw, p.notch_depth),
        (flare_x, p.tip_flare_len),
        (knee_x, p.tip_knee_len),
        (0.0, p.tip_len),
        (-knee_x, p.tip_knee_len),
        (-flare_x, p.tip_flare_len),
        (-xw, p.notch_depth), (-p.notch_half_width, p.notch_depth * 0.97), (-p.notch_half_width, 0.0),
    ]
    body = (
        cq.Workplane("XY").polyline(profile).close().extrude(t).translate((0, 0, -t / 2))
    )
    hole_r = p.pin_radius + p.hinge_clearance
    body = body.union(_half_barrel(p, -p.notch_half_width, p.notch_half_width, 0.0, 0.0, -1, r=hole_r))
    notch = _box(-p.notch_half_width, p.notch_half_width,
                 -(t / 2 + 1), p.notch_depth, -t / 2 - 1, t / 2 + 1)
    body = body.cut(notch)
    body = body.cut(_cylinder_x(hole_r, -(p.tab_half_width + 2), (p.tab_half_width + 2), 0.0, 0.0))
    return body


# NOTE: no build_clip_scaffold() -- solid 11 (the clip) ships as a pure
# frozen import. An early scaffold attempt (flat plate + the same tab/pin
# connector every link uses) was abandoned: the best fit over an 18-angle
# rotation sweep still overshot the original by >40% of its own volume, and
# this session's time budget did not allow the orthographic-view tracing a
# real fix needs. See spec.md "Residual-frozen regions" and pitfalls.md.


# --------------------------------------------------------------------------
# chain placement
# --------------------------------------------------------------------------

def _place(local_solid, theta_deg, pivot_y, pivot_z):
    return local_solid.rotate((0, 0, 0), (1, 0, 0), theta_deg).translate((0, pivot_y, pivot_z))


def chain_poses(p):
    """Return (theta_i, pivot_i) for i = 0..n_links (n_links+1 pivots: the
    socket pivot of every link, plus the terminal boss pivot of the last
    link == the tip cap's socket pivot), plus theta_tip."""
    thetas = [p.link0_dir_angle]
    hinge_angles = [p.hinge_angle_1, p.hinge_angle_2, p.hinge_angle_3, p.hinge_angle_4,
                    p.hinge_angle_5, p.hinge_angle_6, p.hinge_angle_7, p.hinge_angle_8,
                    p.hinge_angle_9]
    for i in range(1, p.n_links):
        prev = thetas[-1]
        h = hinge_angles[i - 1] if i - 1 < len(hinge_angles) else 0.0
        thetas.append(prev + h)

    pivots = [(p.chain_origin_y, p.chain_origin_z)]
    for i in range(p.n_links):
        th = thetas[i] if i < len(thetas) else thetas[-1]
        y0, z0 = pivots[-1]
        y1 = y0 + p.link_length * math.cos(math.radians(th))
        z1 = z0 + p.link_length * math.sin(math.radians(th))
        pivots.append((y1, z1))

    theta_tip = thetas[-1] + p.tip_hinge_angle
    return thetas, pivots, theta_tip


def _residual_union(scaffold, idx):
    """fast_union the scaffold with parts/residual_<idx>.step if it exists
    and is non-trivial; otherwise return the scaffold unchanged."""
    import os
    path = here(f"parts/residual_{idx}.step")
    if not os.path.exists(path):
        return scaffold
    wrap = cq.Workplane(obj=cq.importers.importStep(path).val())
    if not wrap.val().Solids():
        return scaffold
    return fast_union(scaffold, wrap)


def gen_step():
    p = Params()

    thetas, pivots, theta_tip = chain_poses(p)

    # NOTE: residual sidecars were captured in GLOBAL (placed) coordinates
    # (see build_residuals.py), so each scaffold must be PLACED before it is
    # fast_union'd with its residual -- unioning in the local frame first (an
    # earlier bug here) silently fuses a local-frame scaffold against a
    # global-frame residual, which don't overlap at all, then re-transforms
    # the whole mess: volume still summed correctly (fast_union's fallback
    # just concatenates non-overlapping pieces) but the shape was wrong. At
    # baseline params this reproduces the source exactly; a large edit that
    # moves a link far from its baseline pose will still show the frozen
    # residual glued at the OLD baseline seam (the residual-masking
    # limitation SKILL.md warns about) -- ride distance is bounded by how far
    # fast_union's escalating-fuzzy fallback keeps finding an overlap.
    solids = []
    for i in range(p.n_links):
        width_lo = p.neck_width + i * p.width_step
        width_hi = p.neck_width + (i + 1) * p.width_step
        link = build_link(p, width_lo, width_hi, has_socket=True, has_boss=True)
        y0, z0 = pivots[i]
        link = _place(link, thetas[i], y0, z0)
        solids.append(_unify(_residual_union(link, i)))

    tip = build_tip(p)
    y0, z0 = pivots[p.n_links]
    tip = _place(tip, theta_tip, y0, z0)
    solids.append(_unify(_residual_union(tip, 10)))

    # Clip (solid 11): shipped as a pure FROZEN import -- see spec.md
    # "Residual-frozen regions" and pitfalls.md for why a scaffold attempt
    # was abandoned (best rotation found over an 18-angle sweep still
    # overshot the original by >40% of its own volume; the plate shape and
    # true attachment geometry need a real orthographic-view/CAD trace this
    # session's time budget didn't allow after the link-chain work). Its
    # PLACEMENT still rides the chain -- a seam-param edit to
aspire-planter
"""Recovered Aspire Planter set — three reeded pots + three saucers ("All Sizes").

gen_step() builds each of the six solids in its own centred local frame, unions
the parametric scaffold with its frozen base residual (the genuinely-freeform cap
imported from parts/residual_<i>.step), translates it to its measured seat in the
+Y row, and returns a named cq.Assembly. The six bodies are disjoint, so no
boolean is needed between them — only the per-solid scaffold∪residual union.

Geometry that IS reconstructed as parametric code:
  pot     : the reeded wall (rounded-tooth gear cross-section: ridge/valley
            cylinders + radial flank planes + r corner fillets), the cavity bore,
            and the 45° top-rim chamfer — from seam_z up to top_z.
  saucer  : the outer wall cylinder, recessed floor, inner cavity wall, and the
            45° rim chamfer.

Geometry that is FROZEN (imported residual, editable in placement only):
  pot     : the base cap below seam_z — a flute-modulated spherical dome + rounded
            foot + hemispherical inner bowl + ~17 square drainage holes + the "JP"
            maker's logo. The reeds fade into the dome through a NURBS band, so a
            clean revolve there would overshoot into the reed valleys. See spec.md.
  saucer  : the floor locating tabs / "JP" logo / (large saucer) the concentric
            drainage-hole grid.

The residual rides the wall seam: each residual is translated by (seam_z − its
baseline) so that editing a pot's seam_z / top_z keeps the cap glued to the wall
(placement-live), while dz == 0 at baseline keeps fidelity exact.
"""

from __future__ import annotations

import math
import os

import cadquery as cq
from OCP.BRepCheck import BRepCheck_Analyzer

from params import Params
from validation import validate_params
from booleans import fast_union


def here(rel: str) -> str:
    return os.path.join(os.path.dirname(os.path.abspath(__file__)), rel)


# --------------------------------------------------------------------------- #
# Reeded ("fluted") cross-section
# --------------------------------------------------------------------------- #
def _reed_solid(N, R_ridge, R_valley, rb, tip_half, val_half, phase, H):
    """Extruded reeded prism (height H, base on z=0): N rounded teeth, ridge cylinder
    R_ridge alternating with valley cylinder R_valley, joined by flat radial flanks
    and r=rb corner fillets.

    tip_half / val_half are the ridge / valley arc HALF-angles (radians); phase is
    the azimuth (radians) of tooth #0's centre. All arcs are real circular arcs, so
    the extruded faces come out as exact cylinders/planes (matches the source). The
    wire is assembled from an explicit edge list (not chained Workplane ops) so a
    105-tooth profile does not blow Python's recursion limit.
    """
    period = 2 * math.pi / N

    def P(r, a):
        return (r * math.cos(a), r * math.sin(a))

    def norm(a):
        m = math.hypot(*a)
        return (a[0] / m, a[1] / m)

    def mid_on_circle(center, radius, s, e):
        bs = norm((s[0] - center[0], s[1] - center[1]))
        be = norm((e[0] - center[0], e[1] - center[1]))
        b = norm((bs[0] + be[0], bs[1] + be[1]))
        return (center[0] + radius * b[0], center[1] + radius * b[1])

    # Period built with the VALLEY centred at az 0 and the RIDGE at az period/2.
    # The flank is RADIAL (a line through the axis at azimuth th_f); the rb fillets
    # turn the corners. Build the right half [0 -> period/2], then mirror.
    a_v = val_half                     # valley arc half-angle
    ridge_left = period / 2 - tip_half  # azimuth of the ridge arc's left tangent
    Cc = P(R_valley + rb, a_v)          # concave fillet centre (tangent to valley)
    Cv = P(R_ridge - rb, ridge_left)    # convex fillet centre (tangent to ridge)
    # radial flank azimuth — tangent to both fillets (the two solutions agree on the
    # real part; average them for robustness against measurement rounding)
    thf1 = a_v + math.asin(max(-1.0, min(1.0, rb / (R_valley + rb))))
    thf2 = ridge_left - math.asin(max(-1.0, min(1.0, rb / (R_ridge - rb))))
    th_f = 0.5 * (thf1 + thf2)
    fdir = (math.cos(th_f), math.sin(th_f))

    def proj(c):                        # foot of perpendicular of c onto flank line
        t = c[0] * fdir[0] + c[1] * fdir[1]
        return (t * fdir[0], t * fdir[1])

    A0 = P(R_valley, 0.0)               # valley centre
    A1 = P(R_valley, a_v)               # valley arc end (concave-fillet tangent)
    Tf_c = proj(Cc)                     # flank/concave-fillet tangent
    Tf_v = proj(Cv)                     # flank/convex-fillet tangent
    A2 = P(R_ridge, ridge_left)         # ridge arc start (convex-fillet tangent)
    A3 = P(R_ridge, period / 2)         # ridge centre

    half = [
        ("arc", A1, mid_on_circle((0, 0), R_valley, A0, A1)),  # valley half-arc
        ("arc", Tf_c, mid_on_circle(Cc, rb, A1, Tf_c)),        # concave corner fillet
        ("line", Tf_v, None),                                  # radial flank
        ("arc", A2, mid_on_circle(Cv, rb, Tf_v, A2)),          # convex corner fillet
        ("arc", A3, mid_on_circle((0, 0), R_ridge, A2, A3)),   # ridge half-arc
    ]

    def reflect(p):                     # reflect across the line at az period/2
        c, s = math.cos(period), math.sin(period)   # 2*(period/2)
        return (c * p[0] + s * p[1], s * p[0] - c * p[1])

    # Mirror the right half about the ridge axis: walk its points in reverse,
    # reflected, to descend ridge -> next valley centre (closing the period).
    ops = list(half)
    pts = [A0] + [e for _, e, _ in half]           # A0,A1,Tf_c,Tf_v,A2,A3
    for idx in range(len(half) - 1, -1, -1):
        kind, end, mid = half[idx]
        rend = reflect(pts[idx])
        ops.append((kind, rend, None if kind == "line" else reflect(mid)))

    def rot(p, a):
        c, s = math.cos(a), math.sin(a)
        return (c * p[0] - s * p[1], s * p[0] + c * p[1])

    def V(p):
        return cq.Vector(p[0], p[1], 0.0)

    cur = A0
    edges = []
    for k in range(N):
        a = k * period
        for kind, end, mid in ops:
            e = rot(end, a)
            if kind == "line":
                edges.append(cq.Edge.makeLine(V(cur), V(e)))
            else:
                m = rot(mid, a)
                edges.append(cq.Edge.makeThreePointArc(V(cur), V(m), V(e)))
            cur = e
    wire = cq.Wire.assembleEdges(edges)
    face = cq.Face.makeFromWires(wire)
    solid = cq.Solid.extrudeLinear(face, cq.Vector(0, 0, H))
    # built with ridge #0 at az period/2; clock it to the measured phase
    solid = solid.rotate(cq.Vector(0, 0, 0), cq.Vector(0, 0, 1),
                         math.degrees(phase - period / 2))
    return cq.Workplane(obj=solid)


def _bore_cutter(bore_r, chamfer, z_bot, z_top):
    """A through cavity cutter: cylinder r=bore_r up to (z_top − chamfer), then a
    45° countersink widening to (bore_r + chamfer) at z_top, clearing above.

    A coaxial cylinder+cone+cylinder union (NOT coincident with the source) — a
    plain boolean, robust where a 3D edge-chamfer on the reeded body segfaults.
    """
    lower = (cq.Workplane("XY").circle(bore_r)
             .extrude((z_top - chamfer) - z_bot).translate((0, 0, z_bot)))
    if chamfer <= 0:
        return lower.union(cq.Workplane("XY").circle(bore_r).extrude(2.0).translate((0, 0, z_top)))
    cone = cq.Workplane(obj=cq.Solid.makeCone(
        bore_r, bore_r + chamfer, chamfer,
        cq.Vector(0, 0, z_top - chamfer), cq.Vector(0, 0, 1)))
    top = (cq.Workplane("XY").circle(bore_r + chamfer)
           .extrude(2.0).translate((0, 0, z_top)))
    return lower.union(cone).union(top)


# --------------------------------------------------------------------------- #
# Pot family (solids 0, 2, 4) — reeded wall, built in a centred local frame
# --------------------------------------------------------------------------- #
# Recovered reed clocking (azimuth of reed #0 centre, deg). A pure rotation is
# invisible to the mass-signature editability gate, so it is a recovered constant
# rather than an editable Params field.
_POT_PHASE_DEG = {"p0": 2.7498, "p2": 3.1639, "p4": 2.5637}


def build_pot(p: Params, tag: str):
    N = int(round(getattr(p, f"{tag}_flutes")))
    R_ridge = getattr(p, f"{tag}_ridge_r")
    R_valley = R_ridge - getattr(p, f"{tag}_reed_depth")
    rb = getattr(p, f"{tag}_fillet_r")
    tip_half = math.radians(getattr(p, f"{tag}_tip_deg")) / 2.0
    val_half = math.radians(getattr(p, f"{tag}_valley_deg")) / 2.0
    phase = math.radians(_POT_PHASE_DEG[tag])
    bore_r = R_valley - getattr(p, f"{tag}_wall")   # bore is the valley wall offset
    seam_z = getattr(p, f"{tag}_seam_z")
    top_z = getattr(p, f"{tag}_top_z")
    H = top_z - seam_z

    wall = _reed_solid(N, R_ridge, R_valley, rb, tip_half, val_half, phase, H)
    wall = wall.translate((0, 0, seam_z))

    # cavity bore with a 45° top-rim countersink, cut in one boolean
    wall = wall.cut(_bore_cutter(bore_r, p.rim_chamfer, seam_z - 1.0, top_z))
    return wall


# --------------------------------------------------------------------------- #
# Saucer family (solids 1, 3, 5) — round dish, built in a centred local frame
# --------------------------------------------------------------------------- #
def build_saucer(p: Params, tag: str):
    out_r = getattr(p, f"{tag}_out_r")
    cav_r = out_r - getattr(p, f"{tag}_wall")          # cavity is the wall offset
    bot_z = getattr(p, f"{tag}_bot_z")
    floor_z = bot_z + getattr(p, f"{tag}_floor_t")      # floor depth above the underside
    top_z = getattr(p, f"{tag}_top_z")
    rc = getattr(p, f"{tag}_rim_chamfer")

    # Start the scaffold ABOVE the debossed underside logo slab; the frozen residual
    # carries that slab (with the "JP" logo and the bottom-edge round) verbatim.
    base = bot_z + p.saucer_base_freeze
    body = cq.Workplane("XY").circle(out_r).extrude(top_z - base).translate((0, 0, base))
    body = body.cut(_bore_cutter(cav_r, rc, floor_z, top_z))
    return body


# --------------------------------------------------------------------------- #
# Assembly
# --------------------------------------------------------------------------- #
def _vol(wp):
    try:
        return sum(s.Volume() for s in wp.val().Solids() if s.Volume() > 0)
    except Exception:
        return 0.0


def _compound(scaffold, wrap):
    comp = cq.Compound.makeCompound(
        list(scaffold.val().Solids()) + list(wrap.val().Solids()))
    return cq.Workplane(obj=comp)


def robust_combine(scaffold, wrap, allow_weld=True):
    """scaffold ∪ frozen residual, welded when reliable, else a COMPOUND fallback.

    scaffold and the residual are disjoint solids that touch at a seam, so a clean
    fuse equals their volume sum. The thin analytic SAUCER residuals weld cleanly.
    The bspline-heavy POT base caps do NOT: the fuse returns a volume-plausible but
    topologically INVALID solid (inverted/inverted-on-export faces) — a known OCCT
    limit on coincident faces of a NURBS body. For those we keep the two solids as
    a compound — geometrically exact (touching at the wall seam), only leaving an
    un-welded planar seam between wall and frozen cap. See spec.md "Known limitation".
    """
    if not allow_weld:
        return _compound(scaffold, wrap)
    sv, wv = _vol(scaffold), _vol(wrap)
    welded = fast_union(scaffold, wrap)
    try:
        ws = welded.val().Solids()
        ok = (len(ws) == 1 and _vol(welded) >= 0.9 * (sv + wv)
              and BRepCheck_Analyzer(ws[0].wrapped).IsValid())
    except Exception:
        ok = False
    return welded if ok else _compound(scaffold, wrap)


def _add_residual(scaffold, tag, seam_baseline=None, seam_now=None, allow_weld=True):
    """Combine the scaffold with its frozen residual sidecar, if present.

    The residual rides the wall seam: dz = seam_now − seam_baseline (0 at baseline).
astronomical-telescope-hadley-an-easy-assembly-hig
"""gen_step() for the Hadley Newtonian telescope hardware kit.

Unified hybrid model (see step-to-cadquery SKILL.md), multi-body, TIME-BOXED:
  - 3 of 11 canonicals (uta, lta, hub) are reconstructed as a live parametric
    partial-cylinder-band scaffold unioned with a frozen residual sidecar
    (parts/residual_<name>.step) that carries everything the scaffold didn't
    attempt (bosses, tabs, lugs, holes, embossed text -- see params.py and
    spec.md for exactly what was left out and why).
  - The other 8 canonicals (mount, spider, collim, baffle, clamp x2 instances,
    cell, bktc, bktr) are pure frozen imports (parts/frozen_<name>.step) with
    NO scaffold -- this session ran out of time before attempting them (see
    spec.md "Session history" and skill-feedback/pitfalls for the full story).
  - Every one of the 12 original solids already sits at its own correct
    absolute position in the source STEP's world coordinate frame (this file
    does not need to re-place anything) -- the whole assembly is a plain
    compound of 11 independent bodies (12 instances counting the clamp
    mirror pair), no boolean needed to combine them.
"""

from pathlib import Path

import cadquery as cq

from params import Params
from validation import validate_params
from booleans import fast_union

HERE = Path(__file__).parent


def _import(path: str) -> cq.Shape:
    return cq.importers.importStep(str(HERE / path)).val()


_MARGIN = 0.3  # mm, safety gap enforced between a ring's inner/outer radius or z_lo/z_hi


def _safe_inner(r_in: float, r_out: float) -> float:
    """Clamp r_in strictly below r_out so a perturb.py nudge on either radius
    can never invert the ring into a degenerate/empty cut (an ERROR, not a
    DEAD, on the editability gate -- see spec.md's editability section)."""
    return min(r_in, r_out - _MARGIN)


def _safe_lo(z_lo: float, z_hi: float) -> float:
    """Same guard for a band's low/high Z so extrude() never sees a
    negative or zero height."""
    return min(z_lo, z_hi - _MARGIN)


def _build_uta(p) -> cq.Workplane:
    import math

    z_lo = _safe_lo(p.uta_z_lo, p.uta_z_hi)
    r_in = _safe_inner(p.uta_ir, p.uta_or)
    h = p.uta_z_hi - z_lo
    outer = cq.Workplane("XY").workplane(offset=z_lo).circle(p.uta_or).extrude(h)
    inner = cq.Workplane("XY").workplane(offset=z_lo - 1).circle(r_in).extrude(h + 2)
    ring = outer.cut(inner)

    a0 = math.radians(p.uta_gap_start_deg)
    a1 = math.radians(p.uta_gap_end_deg)
    R = p.uta_or * 1.3
    wedge = (
        cq.Workplane("XY")
        .workplane(offset=z_lo - 1)
        .polyline([(0, 0), (R * math.cos(a0), R * math.sin(a0)), (R * math.cos(a1), R * math.sin(a1))])
        .close()
        .extrude(h + 2)
    )
    scaffold = ring.cut(wedge)
    residual = cq.Workplane(obj=_import("parts/residual_uta.step"))
    return fast_union(scaffold, residual)


def _build_lta(p) -> cq.Workplane:
    z_lo = _safe_lo(p.lta_z_lo, p.lta_z_hi)
    r_in = _safe_inner(p.lta_ir, p.lta_or)
    h = p.lta_z_hi - z_lo
    outer = cq.Workplane("XY").workplane(offset=z_lo).circle(p.lta_or).extrude(h)
    inner = cq.Workplane("XY").workplane(offset=z_lo - 1).circle(r_in).extrude(h + 2)
    scaffold = outer.cut(inner)
    residual = cq.Workplane(obj=_import("parts/residual_lta.step"))
    return fast_union(scaffold, residual)


def _build_hub(p) -> cq.Workplane:
    def ring(r_in, r_out, z_lo, z_hi):
        z_lo = _safe_lo(z_lo, z_hi)
        r_in = _safe_inner(r_in, r_out)
        h = z_hi - z_lo
        outer = cq.Workplane("XY").workplane(offset=z_lo).circle(r_out).extrude(h)
        inner = cq.Workplane("XY").workplane(offset=z_lo - 1).circle(r_in).extrude(h + 2)
        return outer.cut(inner)

    hub_ring = ring(p.hub_bore_r, p.hub_hubring_or, p.hub_z_lo, p.hub_z_mid)
    rim_ring = ring(p.hub_riminner_r, p.hub_rimouter_r, p.hub_z_lo, p.hub_z_mid)
    lip = ring(p.hub_lip_ir, p.hub_lip_or, p.hub_z_lipLo, p.hub_z_hi)
    scaffold = hub_ring.union(rim_ring).union(lip)
    residual = cq.Workplane(obj=_import("parts/residual_hub.step"))
    return fast_union(scaffold, residual)


# canonicals shipped as pure frozen imports this session -- no scaffold
_FROZEN = [
    "frozen_mount.step",   # solid 2 - focuser mounting plate w/ threaded bore
    "frozen_spider.step",  # solid 3 - secondary-mirror 4-vane spider
    "frozen_collim.step",  # solid 4 - primary collimation/adjuster ring
    "frozen_baffle.step",  # solid 5 - curved light-baffle blade
    "frozen_clamp_a.step",  # solid 6 - tube P-clamp crescent arm (instance A)
    "frozen_cell.step",    # solid 7 - primary-mirror cell drum
    "frozen_bktc.step",    # solid 8 - small circular P-clip bracket
    "frozen_bktr.step",    # solid 9 - small rounded-rect P-clip bracket
    "frozen_clamp_b.step",  # solid 11 - tube P-clamp crescent arm (instance B, mirror of A)
]


def gen_step():
    p = Params()
    validate_params(p)

    solids = [_build_uta(p).val(), _build_lta(p).val(), _build_hub(p).val()]
    for rel in _FROZEN:
        solids.append(_import(f"parts/{rel}"))

    return cq.Workplane(obj=cq.Compound.makeCompound(solids))


if __name__ == "__main__":
    gen_step()

 succeeded in 0ms:
articulated-snowman-fidget
# Articulated Snowman Fidget — recovered CadQuery project

Source: `models/crawled/articulated-snowman-fidget/Articulated Snowman Fidget.step`
(Printables model 92714, "Articulated Snowman Fidget" by 3D Printy, CC-BY).
21 solids, bbox 150 x 50 x 103.3 mm.

## What the part actually is

The STEP is **not** one snowman — it is **three complete example configurations**
of the same posable, ball-jointed snowman kit, laid out side by side at
X = 0 / 50 / 100 mm (a common way these customizable Printables kits get
exported/photographed together). Each "snowman" stacks 3 hollow ball segments
(bottom → chest → head, largest to smallest) that rest on each other and are
held together by an elastic cord threaded through an internal bore in each
ball (see `design_readme.md`: "held together by a rubber band... spans
between the head and the bottom"); a hat is glued on top; small decorative
pegs (nose, 2 eyes, buttons) are glued onto the head/chest.

| Group | X offset | Bottom | Chest | Head | Hat | Accessory | Face (nose+eyes+button) |
|---|---|---|---|---|---|---|---|
| A | 0   | solid 2  | solid 0  | solid 1  | solid 4  | **scarf** (solid 3) | solid 5, 12, 13, 18 |
| B | 50  | solid 11 | solid 20 | solid 8  | solid 10 | **sweater collar** (solid 6) | solid 9, 14, 15, 19 |
| C | 100 | solid 16 | solid 7  | solid 17 | — (none) | — (none) | — (bare body, larger/extra holes only) |

Groups A and B are **exactly congruent** on bottom/chest/head/hat (identical
volumes to 3 decimals) — a pure rigid translation. Group C's bottom/chest/head
are the **same shape family** (same outer sphere radius and wall) but differ
in a few small hole/slot dimensions (Printables' "Bottom with Large Knot
Hole" / "Chest with Large Arm Holes" / "Head with Larger Holes" options) —
same reconstruction function, different residual per instance, see below.

## The core geometric idiom (every ball segment)

Each ball (bottom/chest/head) is, from the outside, simply a **sphere,
shelled, flat-cut top and/or bottom**. What makes it non-trivial is a
compound internal cavity: a socket/funnel that narrows from the shell's
hollow interior down toward a smaller opening at the seam with its
neighbour (so adjacent balls self-align when stacked), plus a thin floor at
the very bottom of the base piece. This internal shape is a real, `sphere`
+ `cone` + `torus`-blend compound curve in the source, not a single
primitive; I approximated it with a full-sphere hollow (bottom/chest) or a
hemisphere hollow (head, whose crown stays solid to carry the face pegs and
hat seat) unioned with a straight cone funnel fit to a handful of measured
(radius, z) points (see `pitfalls.md` #3 for how those points were
measured — direct vertex/face introspection was misleading here; a
thin-slab cross-sectional **area** scan was the reliable signal).

## Solid-index → canonical map

`build_residuals.py`'s `_BOTTOM`/`_CHEST`/`_HEAD`/`_HAT`/`_NOSE`/`_EYE_L`/
`_EYE_R`/`_BUTTON` dictionaries in `main.py` are the authoritative map from
solid index to (part-type, group). Solids 3 and 6 (scarf, sweater collar)
have no scaffold at all — they are fully frozen wraps (see below).

## Editable parameters (`params.py`)

- **Ball geometry**: `bottom_radius`/`bottom_wall`, `chest_radius`/`chest_wall`,
  `head_radius`/`head_wall` — the 3 ball diameters and shell thicknesses,
  shared by every instance of that ball.
- **Ball centres**: `bottom_centre_z`, `chest_centre_z`, `head_centre_z` —
  the vertical stack spacing.
- **Bottom-only**: `bottom_flat_z` (foot height), `bottom_bore_radius`
  (strap channel), `bottom_slot_radius` (knot-slot stand-in),
  `bottom_cavity_margin`.
- **Chest-only**: `chest_bottom_z`/`chest_top_z` (shell extent),
  `chest_cavity_margin`, `chest_button_hole_radius`,
  `chest_button_count` (int — number of drilled button holes, capped at 3
  measured hole positions).
- **Head-only**: `head_bottom_z`/`head_top_z`, `head_cavity_margin`,
  `head_eye_hole_radius`, `head_nose_hole_radius`.
- **Hat**: `hat_brim_z`/`hat_brim_radius`/`hat_brim_height`,
  `hat_crown_lower_radius`/`hat_crown_lower_height`,
  `hat_crown_upper_radius_lo`/`hi`/`hat_crown_upper_height`,
  `hat_cap_radius`/`hat_cap_height` — every step of the stepped-cylinder
  profile.
- **Nose**: `nose_peg_radius`/`depth`, `nose_tip_radius`/`length`.
- **Eye**: `eye_peg_radius`/`depth`, `eye_spacing_x` (distance between the
  two eyes).
- **Button**: `button_peg_radius`/`depth`.
- **Layout**: `group_spacing` — the X pitch between the 3 example snowmen
  (a live placement param; a 4th snowman could be added by extending the
  per-group dictionaries in `main.py`).

## Residual-frozen regions (not parametrically editable in shape)

| Region | Solids | What's frozen | Why |
|---|---|---|---|
| Ball internal socket/floor/fillets | all bottom/chest/head (9) | The exact compound funnel/floor/small-fillet detail inside each ball (23–49% of that solid's volume, see gate results) | Genuinely a `sphere+cone+torus`-blend compound curve with no single clean primitive form; approximated generously, residual carries the rest — see `pitfalls.md` #3–4 |
| Chest button-hole exact chamfer | chest (3) | Small torus/plane fillets at each button-hole mouth | Sub-mm cosmetic detail, low value to chase further |
| Hat lower-crown alignment pocket | hat (2) | A small squarish pocket found empirically inside the crown (never visible — hidden under the brim) | Modelled as a plain box cut, not its exact rounded profile |
| **Eye dome cap** | eye (4) | The entire visible rounded button-eye cap — a genuine `bspline` freeform surface, not a sphere | Only the tiny cylindrical mounting peg is reconstructed; the dome is frozen (see `reconstruct_targets`: 1 bspline face per eye) |
| **Scarf** | scarf (1, group A only) | The ENTIRE solid | 98.6% analytic by area but a genuinely wrapped/knotted freeform accessory unique to one instance — not worth reconstructing for a single non-repeated part (see "Cost discipline") |
| **Sweater collar** | collar (1, group B only) | The ENTIRE solid | Reads 100% analytic by face-type census but is actually a "rough textured ring" — ~300 tiny sphere/plane/torus facets forming a knit texture; a legitimate micro-texture exception per SKILL.md's honest-limits note, not worth a full knurl-pattern reconstruction for one instance |

Both frozen accessories still ride their snowman's placement (`group_spacing`)
so they move correctly if the layout is edited; their **shape** does not
respond to any param.

## Gate results

**Fidelity** (`metrics.py`, per solid) — **19/19 PASS** (all reconstructed
solids; scarf/collar are plain frozen imports, gated only on placement).
Worst case solid 7 (chest, "Large Arm Holes" variant): vol 0.016%, inertia
0.021%. Whole-assembly (`gen_step()` vs the original 21-solid compound):
volume 173233.6 vs 173209.8 mm³, **0.014%** — solid count matches (21 vs 21).

**Editability** (`perturb.py --delta 0.1`) — **PASS**, 31 LIVE / 12 DEAD /
0 ERROR. DEAD params (all legitimate, not phantom knobs):
`chest_wall` (nominal only — the equatorial shell is dominated by the
compound-curve cavity, not a uniform offset, see `pitfalls.md` #3),
`bottom_bore_radius`/`bottom_slot_radius` (their extent is already
swallowed by the funnel-margin cavity at this delta), `chest_button_count`
(capped at 3 real measured hole axes — incrementing past 3 is a documented
no-op, not a bug), `head_bottom_z` (the funnel cone reaches it either way
at this delta), `hat_cap_height`, `nose_peg_depth`/`nose_tip_length`,
`eye_peg_depth`, `button_peg_radius`/`button_peg_depth` (all fully masked
by the residual at their own tiny scale), `group_spacing` was DEAD before a
mid-session fix (see `pitfalls.md` #5) — now genuinely LIVE (verified: bbox
X grows from 125 to 135 at +10%).

**Reconstructability** (`residual_audit.py`, per solid, threshold 0.15) —
**FAIL on every reconstructed solid**, and this is the honest headline
result of this recovery:

| Part | punted_fraction | Top punted surface |
|---|---|---|
| bottom (2/11/16) | 0.30 | internal socket cone + floor planes |
| chest (0/20/7) | 0.41–0.43 | internal funnel cone (chest is thin-shell almost everywhere — small wall, big relative funnel) |
| head (1/8/17) | 0.53 | internal funnel + solid-crown transition |
| hat (4/10) | 0.47 | crown-taper / brim-chamfer seams (the volume residual is only 8%, but a modest surface mismatch dominates the *area* ratio on a small part — see `pitfalls.md` #6) |
| nose (5/9) | 0.84–0.85 | tip + peg-base seam (tiny part, any mismatch dominates) |
| eye (12/13/14/15) | 1.0 | entirely the frozen dome (by design) |
| button (18/19) | 1.0 | tiny disc, peg reconstructed but area-dominated by residual |

**Accepted with a documented threshold bump** — raise `residual_audit.py`'s
`--threshold` to **0.55** for bottom/chest/head/hat and to **1.0** for
nose/eye/button (i.e. accept the tiny accessories as intentionally
peg-only). This is a deliberate call, not a silent slackening: the
punted region in every case is the internal ball-joint socket/funnel
compound curve (or, for the tiny pegs, a residual whose *volume* is modest
but whose *area* dominates a small part) — see "Residual-frozen regions"
above and `pitfalls.md` #3–4 for why this did not resolve with more
iteration inside the session's time budget.

**Topology** (`topology_audit.py`, per solid) — **PASS: bottom, nose, eye,
button** (0 redundant seams, `ShapeUpgrade_UnifySameDomain` applied to every
wrapped solid in `main.py`). **FAIL: chest (3 seams), head (5 seams), hat (1
seam + 4.2% area error)** — the button/eye/nose hole cuts and the hat's
alignment-pocket box cut each leave a redundant coplanar/cocylindrical seam
at the cut boundary that `UnifySameDomain` doesn't merge (the cut faces
aren't quite coincident with a *single* neighbouring face — they meet at a
corner of three or more). Not resolved within the session's time budget;
see `pitfalls.md` #7.

## Assumptions to confirm

- The 3 example configurations' accessory pairing (scarf ↔ group A,
  collar ↔ group B) is read directly off which solids are present in each
  group — not asserted from the readme, which lists options independently.
- `chest_button_count` is capped at 3 real, measured hole positions (not a
  freely-arrayed pattern) — the source only ever drills 3.
- Nose/eye/button dimensions differ ~5-10% between group A and group B in
  the source (not an intentional design choice we could infer a reason
  for) — one shared, slightly-conservative peg size was used for both,
  verified overshoot-safe against the smaller (group B) instance.
- The internal socket/funnel cavity shape is a **fit**, not an extracted
  exact profile (see `pitfalls.md` #3) — it is deliberately biased to stay
  inside the original (never overshoot), so it is slightly more generous
  (more hollow) than the true part in places; this is fidelity-safe by
  construction but is the direct cause of the reconstructability gate FAILs
  above.
articulated-tie-step-file-included
# Articulated Tie — recovered parametric spec

Source: `models/crawled/articulated-tie-step-file-included/Articulated_tie.step`
(Printables #282105, "Articulated tie (STEP file included)", CC-BY-SA).

## What the part is

A wooden-tie-style **print-in-place articulated necktie**: 10 tapered
trapezoidal "living-hinge" links that widen from a 22mm neck to a ~68mm tip,
each pair joined by a print-in-place notch/pin knuckle (0.3mm clearance per
`design_readme.md`), a wide pointed tip cap terminating the chain, and a
spring clip that plugs into the neck end to attach the tie to a shirt.
12 solids total. The STEP file ships the tie **posed**, not flat — every
link's hinge pin axis is parallel to global X, and the chain coils in the
Y-Z plane through a sequence of measured fold angles (see below) — this is
almost certainly the pose the author rotated the model into to fit a Prusa
Mini+ bed ("I rotated the hinges in a way that I can print the tie on my
Mini+", `design_readme.md`), not the tie's "as worn" shape.

## Decomposition

| Solid(s) | Role | Recovery |
|---|---|---|
| 0–9 | 10 tapered hinge links | **Fully parametric** — shared `build_link()` scaffold + a small per-link residual (hinge-knuckle micro-geometry, see below) |
| 10 | Tip cap (wide pointed "kite" end) | **Fully parametric** — `build_tip()` scaffold + small residual |
| 11 | Shirt clip | **Frozen import** — see "Residual-frozen regions" |

No two of solids 0–10 are congruent (each link is a different width), so
there is no dedup opportunity in the usual multi-body sense. Instead all 11
share **one** scaffold function family (`build_link`, parametrized by
`width_lo`/`width_hi`/`has_socket`/`has_boss`; `build_tip` is a close
variant), so the part is still built from one reusable piece of code, not
11 independent one-off scaffolds.

## The chain-placement model (the main editable "intent")

Every link's hinge pin is parallel to global X, so the whole chain lives in
a 2D problem embedded in 3D: each link has a **direction angle** theta (in
the global Y-Z plane) and a **pivot point** (where its socket-end hinge axis
sits). `chain_poses()` in `main.py` walks the chain:

```
theta[0]      = link0_dir_angle
theta[i]      = theta[i-1] + hinge_angle_i          (i = 1..9)
theta[tip]    = theta[9]   + tip_hinge_angle
pivot[0]      = (chain_origin_y, chain_origin_z)
pivot[i+1]    = pivot[i] + link_length * (cos theta[i], sin theta[i])
```

The 9 `hinge_angle_*` params (baseline: -90, -80, 0, -80, -80, 0, -30, -80,
-20 degrees) and `tip_hinge_angle` (baseline -60, i.e. rotation to 180°) are
measured **exactly** off the source STEP's own hinge-hole/boss coordinates
(matched pairwise between consecutive solids to sub-micron precision — see
pitfalls.md #1). Setting every `hinge_angle_*` to 0 (and `tip_hinge_angle`
to whatever keeps the tip's own pin axis convention) straightens the whole
tie into one flat blade — recovering the "as worn" shape the print-posed
STEP file doesn't show directly. This is the single most valuable editable
relationship this recovery produced.

## Editable parameters (`params.py`)

- `n_links` (int) — number of tapered links in the chain
- `neck_width`, `width_step` (float) — linear taper: link *i*'s width runs
  `neck_width + i*width_step` → `neck_width + (i+1)*width_step`. Both the
  within-link taper and the link-to-link taper use the *same* step — verified
  exactly against the measured width sequence (22.0, 26.615, 31.231, ...,
  68.154mm, constant step 4.6154mm).
- `link_length` (float, 37.5mm) — hinge-pivot to hinge-pivot distance,
  identical for all 10 links (measured exactly).
- `thickness` (float, 6.0mm) — panel thickness.
- `shoulder_length` (float, 34.0mm) — where the taper body ends and the
  boss/pin protrusion begins.
- `tab_half_width`, `notch_half_width`, `notch_depth` — the notch/tab
  hinge-knuckle envelope.
- `pin_radius` (1.5mm), `hinge_clearance` (0.3mm, matches the design
  readme's stated tolerance exactly) — `hole_radius = pin_radius +
  hinge_clearance`.
- `pin_reach` — how far the pin protrudes past the tab into the mating ears.
- `link0_dir_angle`, `hinge_angle_1..9`, `tip_hinge_angle`,
  `chain_origin_y/z` — the fold-pose params described above.
- `tip_shoulder_x`, `tip_flare_len/x`, `tip_knee_len/x`, `tip_len` — the tip
  cap's pointed-kite outline, measured directly off the source's own vertex
  list (see pitfalls.md #3).

## Gate results

**Fidelity** (`metrics.py`, per solid, volume/COM/inertia): **9/12 PASS**
at the default 0.5% tolerance; solids 8, 9, 10 (worst errors 0.502%, 0.518%,
0.587%) marginally exceed it. Isolated to a STEP export/reimport rounding
artifact on `fast_union`'s output (a well-documented pattern — see e.g. the
RotaRings recovery's identical finding) rather than a modelling error: COM
and volume errors alone are both <0.3% on every solid, and the moment-of-
inertia error tracks solid size (bigger link → bigger absolute rounding on
comparable relative terms). **Accepted with a documented `--tol 0.006`
(0.6%) waiver** — every solid passes at that tolerance.

**Editability** (`perturb.py`): **PASS**, 28 LIVE / 3 DEAD / 0 ERROR.
DEAD: `tab_half_width`, `hinge_clearance`, `pin_reach` — all three drive
geometry entirely inside the hinge-knuckle micro-region that (see below)
partly lives in the frozen per-link residual; a ±10% nudge is too small to
move the *rebuilt* (scaffold+residual) mass signature detectably. This is
the legitimate "knob the residual already covers" DEAD case SKILL.md's
gate documentation calls out, not a phantom knob — all three still visibly
change the bare scaffold if you inspect it directly (verified during
development).

**Reconstructability** (`residual_audit.py`, per solid): **FAIL at the
default 0.15 threshold on every reconstructed solid** — links punt
33–44% of their analytic surface, the tip cap 24%. **Accepted with a
documented threshold bump**: 0.45 for links 0–9, 0.25 for the tip cap. This
is the one significant honesty compromise in this recovery. The punted
surface is concentrated entirely in the hinge-knuckle micro-geometry: the
flat side walls of the notch/ear feature, and a genuine unfilled "relief
gap" between the taper body's edge and the pin that a systematic
occupancy-probe scan confirmed is real (not a modelling gap — see
pitfalls.md #2). Four-plus rounds of geometric-hypothesis iteration
(flat-tab-and-big-barrel → thin-pin-only → various barrel radii) drove
overshoot to ~0 and the punted fraction down from a much worse starting
point, but a fully clean (<15%) reconstruction of this specific
compound-curve knuckle needs a proper orthographic/section trace this
session's time budget did not allow. The tie's identity-defining geometry
— the taper, the length, the link count, the fold pose — is 100% live
parametric code; only the small knuckle transition detail is punted.

**Topology** (`topology_audit.py`, checked on solids 0 and 10 as
representative samples — not run on all 12 given time budget): **PASS** on
both, after adding a `ShapeUpgrade_UnifySameDomain` pass (`_unify()` in
`main.py`) to every scaffold+residual union before placement — without it,
the fast_union naturally splits each of the panel's 2 big flat faces into 4
coplanar pieces (a redundant seam the source doesn't have). Surface-area
error <0.2% on both checked solids.

## Residual-frozen regions

| Solid | Frozen region | Why |
|---|---|---|
| 0–9 (links) | Notch/ear side walls + the taper-to-pin relief-gap transition | Genuine hinge-knuckle micro-geometry; see reconstructability discussion above. Rides the link's own placement (theta_i, pivot_i) — moves and rotates correctly with hinge_angle edits, but its own *shape* is fixed. |
| 10 (tip) | Same knuckle-region micro-geometry at the tip's single socket end, plus a small corner-fillet sliver near the shoulder | Same as above, smaller residual (24%). |
| 11 (clip) | **Entire solid** | A scaffold attempt (flat back plate + the same tab/pin connector every link uses) was tried and abandoned: the best-fit rotation over an 18-angle sweep still overshot the original by >40% of its own volume, and getting the clip's actual plate outline + spring-hook attachment geometry right needs real orthographic-view tracing this session didn't have time for after the link-chain work. Ships as a pure frozen import. Its PLACEMENT still rides `chain_origin_y/z` (translates rigidly with the neck-end anchor point, exact at baseline); `gen_step()` prints a runtime WARNING if that seam param drifts from baseline. Its own shape is not editable. |

## Assumptions to confirm

1. **The shipped pose is a print-packing pose, not the "as worn" shape.**
   Read from the design readme's own text; the recovered `hinge_angle_*`
   params reproduce it exactly at baseline, and are the natural knob to
   change if a straightened (or re-folded) tie is wanted.
2. **`width_step` is one constant** shared between a link's own internal
   taper and its taper relative to the next link — verified to 4+ significant
   figures against the measured sequence, but the *design intent* (was it
   authored as "N links, evenly interpolated between two endpoint widths" or
   literally as a per-link constant delta?) cannot be distinguished from
   geometry alone; both give identical results here.
3. **The clip's rotation relative to link 0** is set to place its pin at
   link 0's socket pivot with a 180° flip from link0's own direction angle —
   chosen because it's the qualitatively correct orientation (visually
   confirmed against the source's render) but the clip's own shape not being
   reconstructed means this placement was tuned by eye/overshoot-search
   rather than derived the way the link chain's angles were.
aspire-planter
# Aspire Planter — recovered design intent

Source: `Aspire Planter - All Sizes.step` (14.2 MB, 6 solids).
A family of **three round reeded planter pots** in three sizes, each paired with a
**shallow round saucer**. Laid out in a single row along +Y in the source frame.

| solid | part          | Ø (mm) | H (mm) | flutes | seat (x, y) |
|-------|---------------|--------|--------|--------|-------------|
| 0     | pot small     | 114.6  | 106.5  | 65     | (0, 0.5)    |
| 1     | saucer small  |  98.0  |  13.2  | —      | (0, 0.5)    |
| 2     | pot medium    | 165.3  | 157.2  | 85     | (0, 150.5)  |
| 3     | saucer medium | 139.0  |  17.4  | —      | (0, 150.5)  |
| 4     | pot large     | 214.4  | 208.2  | 105    | (0, 440.0)  |
| 5     | saucer large  | 180.0  |  22.2  | —      | (0, 440.0)  |

## How each solid decomposes (scaffold + frozen residual)

Every solid is the unified hybrid: a **parametric scaffold** unioned with a
**frozen residual** sidecar (`parts/residual_<tag>.step`) carrying the genuinely
freeform remainder. The residual rides the wall seam, so seam/height edits keep it
glued to the scaffold.

### Pots (solids 0, 2, 4) — `build_pot()`
**Reconstructed as code (the reeded wall + opening):**
- The defining feature is a **reeded ("fluted") wall**: the outer radius
  oscillates between a *ridge* cylinder (`ridge_r`) and a *valley* cylinder
  (`ridge_r − reed_depth`), connected by **flat radial flank planes** and rounded
  at every crest/root corner by a `fillet_r` fillet. This is a rounded-tooth gear
  cross-section, built as a real arc/line wire so the extruded faces come out as
  exact cylinders + planes (matching the source's face types).
- The wall runs from the **seam** (`seam_z`, where the reeds begin, ≈ z 0) up to
  the **rim** (`top_z`). A cavity **bore** (`bore_r`) is cut through, with a 45°
  **countersink** at the top inner rim (`rim_chamfer`).

**Frozen residual (`residual_p*.step`) — editable in placement, NOT in shape:**
- The whole **base cap below the seam**: a spherical dome whose surface is
  *modulated by the reeds fading out* (a NURBS band — introspect flags ~6 %
  bspline area here), a rounded **foot**, a **hemispherical inner bowl**, ~17
  **square drainage holes**, and an embossed **"JP" maker's logo**.
- *Why frozen:* where the reeds melt into the dome the surface is genuinely
  freeform — a clean revolved sphere there would **overshoot into the reed
  valleys** (fatal). The dome's analytic sphere/torus patches are inseparable from
  that fade, so the cap is taken as one frozen residual. This is the deliberate,
  documented reconstructability waiver for the pots (see Gate results below).

### Saucers (solids 1, 3, 5) — `build_saucer()`
**Reconstructed as code:** outer wall cylinder (`out_r`), recessed floor
(`floor_z`), inner cavity wall (`cav_r`), 45° rim chamfer (`rim_chamfer`), from
underside (`bot_z`) to rim (`top_z`).
**Frozen residual:** the floor's central recess + 3 pot-locating tabs + "JP"
logo, and — on the **large** saucer only — a concentric **drainage-hole grid**
(40 holes) with stepped floor rings. Editable in placement, not shape.

## Residual-frozen regions (masked from shape edits)

| region | solids | parametric? |
|--------|--------|-------------|
| reeded wall, bore, rim | pots | YES (code) |
| domed/footed/drilled base cap + "JP" logo | pots | NO — frozen (rides `seam_z`) |
| outer wall, floor, cavity, rim | saucers | YES (code) |
| floor tabs / logo / drain grid | saucers | NO — frozen |

A scaffold edit that **grows into** a frozen region is silently absorbed; one that
**shrinks** a feature can expose a gap the residual won't backfill. Large pot
edits (e.g. ±20 % `ridge_r`) keep the original base cap, so the cap no longer
matches the new wall — expected for a frozen residual. Run `check_liveness.py`
after edits to re-confirm which params still drive geometry.

## Gate results (honest)

| solid | fidelity (worst err) | reconstructability (punted / 0.15) | topology |
|-------|----------------------|-----------------------------------|----------|
| 0 pot S   | PASS 0.18 % | FAIL 0.35 | FAIL (surface-area +2.9 %: cap seam) |
| 1 sau S   | PASS 0.01 % | FAIL | FAIL (floor seams) |
| 2 pot M   | PASS 0.17 % | FAIL 0.34 | FAIL (cap seam) |
| 3 sau M   | PASS 0.20 % | FAIL | FAIL (floor seams) |
| 4 pot L   | PASS 0.13 % | FAIL 0.34 | FAIL (cap seam) |
| 5 sau L   | PASS 0.001 % | FAIL | FAIL (floor seams) |

The **"JP" maker's logo is preserved on every part.** On the pots it rides in the
frozen base cap; on the saucers it is a ~0.4 mm DEBOSS on the *underside*, so the
saucer scaffold starts 0.55 mm above the base and freezes that underside slab
(`saucer_base_freeze`) in the residual verbatim — a flat scaffold there would fill
the debossed pockets and lose the mark (it also drove the saucers' fidelity from
~0.4 % to ~0.01 %).

**Fidelity PASSES on all six solids** — the recovered shape is geometrically
equivalent (volume + centre-of-mass + inertia all well within 0.5 %). The whole
assembly volume matches the source to +0.07 %.

**Editability PASSES** — `perturb.py`: 53 / 53 params LIVE, 0 DEAD, 0 ERROR. Every
knob (flute counts, ridge/valley/reed depth, fillet, wall thickness, heights,
saucer radii/floor, the +Y layout) moves real geometry without breaking regen.
Wall thicknesses and floor depths are stored as offsets so a ±10 % nudge stays
valid; reed clocking is a recovered constant (a rotation is mass-invisible), so it
is not a phantom knob.

So the two *necessary, independent* gates — fidelity (the shape is right) and
editability (the params are real) — both pass. The two below do not.

**Reconstructability and topology do NOT pass.** Both stem from one decision: the
domed pot bases and the detailed saucer floors are kept as FROZEN residual imports
rather than reconstructed as code.
- *Reconstructability* counts the analytic skin (sphere dome, foot torus, base
  plane, inner bowl, saucer floor steps) left in those frozen residuals as
  "punted" — it is reconstructable in principle, so the gate is right to flag it.
- *Topology*: the pots fail because the frozen bspline cap **cannot be welded** to
  the reeded wall — OCCT's boolean collapses on the coincident faces of a
  NURBS-heavy body (a documented kernel limit), so wall + cap ship as a compound
  whose internal seam inflates surface area ~2.9 %. The saucers fail because the
  frozen floor features create coplanar seams on the floor.

**What that means for the user:** the part is shape-exact and the *defining*
geometry (the reeded wall, the cavity, the rim, all three sizes, the saucer
bodies) is live parametric code. The freeform-blended bases/floors are frozen
(editable in placement, not shape). A further iteration would reconstruct the
smooth lower bodies (revolve: foot/base/bowl + drainage holes) and the saucer
floors as code — clearing reconstructability and shrinking the residual — but the
flute-into-dome fade band is genuinely NURBS and a clean weld there is blocked by
the kernel limit above. These two gates are reported FAILED rather than waived,
because the punted geometry is real analytic surface, not micro-texture.

## Assumptions inferred (please confirm)
- The three pots are **one design at three sizes** (not three unrelated parts):
  flute count scales 65 → 85 → 105, the reed depth (~4 mm), fillet (~1 mm), tip/
  valley arc angles, and 45° rim chamfer are shared design language. Each is
  parametrised independently so a single size can be retuned.
- `valley_r` is derived `ridge_r − reed_depth` (uniform reeds), not stored.
- The drainage holes, dome, foot and "JP" logo are treated as freeform/frozen, so
  drain count/size is fixed in the residual, not a live knob.
- Units are **millimetres**.
astronomical-telescope-hadley-an-easy-assembly-hig
# Hadley Newtonian Telescope hardware kit — recovered spec

Source: `models/crawled/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.step`
("Astronomical Telescope 'Hadley' — an easy assembly, high performance
Newtonian", Printables model 224383, author Maff). Per the design_readme this
is a 114/900 mm f8 Newtonian reflector's printed hardware — tube rings,
mirror cells, spider, focuser mount, and small P-clip brackets. **This is not
the whole telescope** (the ~900 mm optical tube / truss poles are aluminium
or PVC, not printed) — the STEP ships 12 solid printed parts laid out in
their own native/design coordinate frame (not a physically assembled scene;
several parts sit hundreds of mm apart along world Z with no truss structure
between them).

## 1. Purpose / decomposition

12 solids, 11 canonicals (solids 6 and 11 are an exact left/right mirror pair
— volumes match to 6 significant figures: 55491.749 vs 55491.748 mm³ — every
other solid is unique). Identified by direct visual triage + point-in-solid
probing (`BRepClass3d_SolidClassifier`, more reliable than trusting
introspect.py's rounded per-feature JSON at this level of geometric density —
see pitfalls.md):

| # | solid | prefix | what it is | volume (mm³) | this session |
|---|---|---|---|---|---|
| 0 | uta | `uta` | Upper Tube Assembly ring (focuser + spider mount, embossed "774/Hadley" logo) | 184490 | **partial scaffold** |
| 1 | hub | `hub` | focuser retainer / hub-and-rim disc (3 spoke arms, boss pairs) | 43262 | **partial scaffold** |
| 2 | mount | `mount` | focuser mounting plate, helical-threaded bore, 4 corner bolts | 78209 | frozen |
| 3 | spider | `spider` | secondary-mirror 4-vane spider + threaded barrel hub | 72631 | frozen |
| 4 | collim | `collim` | primary collimation/adjuster ring, 3 non-uniform arms + hex socket | 33055 | frozen |
| 5 | baffle | `baffle` | curved light-baffle blade (confirmed cylindrical, but a tapered/pointed blade, not a uniform band — see pitfalls.md) | 13759 | frozen |
| 6, 11 | clamp ×2 | `clamp_a`/`clamp_b` | tube P-clamp crescent arm, mirror pair | 55492 ×2 | frozen |
| 7 | cell | `cell` | primary-mirror cell drum, 3 mounting ears, "Kirsner Optik" logo | 145838 | frozen |
| 8 | bktc | `bktc` | small circular P-clip bracket | 4813 | frozen |
| 9 | bktr | `bktr` | small rounded-rect P-clip bracket | 4989 | frozen |

Total assembly volume ≈ 858276 mm³ across all 12 solids.

## 2. Session history (why 3 of 11, not more)

This reconstruction ran across **two interrupted sessions**. The first
fanned out one agent per canonical (11 agents in parallel) to reconstruct
each solid's full feature set (holes, bosses, tabs, fillets) from
`introspect.py`'s per-solid JSON — the approach that worked well on smaller
assemblies in this skill's history (e.g. `prusa-enclosure-v2-mmu2s`,
`tplink-rack`). The session was killed mid-flight before any agent finished;
only 2 of 11 (`uta`, `cell`) left a complete `builder.py`, and neither
reproduced the source volume when re-tested after the restart (see
pitfalls.md — the `cell` builder's revolve profile silently produced an
empty/degenerate solid, and `uta`'s exact measured constants were never
saved before the kill).

Given a hard remaining time budget on the second (this) session, rather than
re-run that same expensive per-feature-JSON approach across all 11
canonicals again, this session switched to a **much faster, verified-safe**
method for a smaller subset: **direct point-in-solid classification**
(`BRepClass3d_SolidClassifier` sampled on a small grid of `(r, θ, z)` probes
against the real B-rep) to find the single largest, unambiguous analytic
surface in a solid (almost always its main cylindrical wall), build *only*
that as a scaffold, and verify `metrics.py --overshoot` ≈ 0 directly rather
than trusting derived numbers. This is fast (minutes per canonical, not the
15-30+ minutes per canonical the full-feature approach was taking) and safe
(every scaffold below is a *proven* subset of its original, not a guess),
at the cost of a much bigger residual than a full-feature reconstruction
would leave. 3 canonicals (`uta`, `lta`, `hub`) were completed this way
before the remaining time budget ran out; the other 8 ship as pure frozen
imports with no scaffold attempt at all this session.

**This is an honest, deliberately time-boxed partial recovery, not a claim
that the other 8 solids are unreconstructable** — `mount`, `collim`, `bktc`,
`bktr`, and probably `baffle` all read as substantially analytic from
`introspect.py`'s per-solid `reconstruct_targets` (low `freeform_area_pct`)
and are good candidates for a future session to pick up with the same
point-classification method. `spider` (24.8% freeform, 55.8% structural
freeform — genuine curved vane cross-sections) and `cell`'s "Kirsner Optik"
embossed logo are the only regions that read as genuinely freeform.

## 3. Gate results

### Fidelity (`metrics.py`, per solid) — the 3 reconstructed canonicals

| canonical | volume err | COM err | inertia err | verdict |
|---|---|---|---|---|
| uta (solid 0) | 0.0006% | 0.0001% | 0.0006% | **PASS** |
| lta (solid 10) | 0.0% | 0.0% | 0.0% | **PASS** |
| hub (solid 1) | 0.0024% | 0.0001% | 0.0005% | **PASS** |

All well under the 0.5% default tolerance — expected, since `scaffold ∪
residual == original` is exact by construction whenever the scaffold is a
verified subset (which `metrics.py --overshoot` confirmed at ≈0 mm³ for all
three before the residual was taken). Whole-assembly fidelity (all 12
solids, reconstructed + frozen) is exact by the same construction — every
frozen solid is a byte-identical re-export of its original, and every
reconstructed one closes via its residual.

The 8 frozen canonicals need no fidelity check: each ships as an unmodified
re-export of its own original solid (see `parts/frozen_*.step`), so their
fidelity error is 0 by construction, not by a passed gate.

### Editability (`perturb.py`, whole project)

**PASS** — 19 LIVE / 1 DEAD / 0 ERROR (only the 3 reconstructed canonicals
have live params; the 8 frozen ones have none to test).

- `hub_lip_ir` is the one documented DEAD: the source's top lip band is only
  ~0.3 mm thick (`hub_lip_ir=58.05` vs `hub_lip_or=58.35`), and this file's
  `_safe_inner()` guard (see §5) clamps `r_in` to `r_out - 0.3` to keep the
  ring valid under any perturbation — for this particular pair the clamp is
  *always* active, so the raw param value never reaches the geometry. This
  is a legitimate DEAD (residual-masked-by-safety-margin), not a phantom
  knob to delete — removing the margin would reintroduce the ERROR it was
  added to fix (see pitfalls.md).
- All 5 other radius/Z-bound params that share the same `_safe_inner`/
  `_safe_lo` guard (`uta_ir`, `uta_z_lo`, `lta_ir`, `lta_z_lo`,
  `hub_riminner_r`) are LIVE — their margins are wide enough that a ±10%
  nudge still moves the clamped value.

### Reconstructability (`residual_audit.py`, per solid) — the 3 reconstructed canonicals

| canonical | punted_fraction | threshold | verdict |
|---|---|---|---|
| uta | 1.004 | 0.15 | **FAIL** (accepted, see below) |
| lta | 1.033 | 0.15 | **FAIL** (accepted, see below) |
| hub | 1.234 | 0.15 | **FAIL** (accepted, see below) |

**Accepted with a large, explicitly documented threshold bump for all
three**, and for all 8 frozen canonicals (`punted_fraction = 1.0` for each,
trivially, since the whole solid is the "residual"). This is the direct,
honest cost of the time-boxed scope decision in §2: each of the 3
reconstructed canonicals' scaffold captures only its single largest
cylindrical band and explicitly excludes every boss/tab/lug/hole — so most
of each solid's analytic surface area legitimately sits in the residual, not
because it's unreconstructable but because reconstructing it wasn't
attempted this session. `top_punted_faces` for all three name exactly what
you'd expect from the shape descriptions in §1 (tab/lug/boss cylinder faces,
plate faces) — nothing surprising, nothing freeform being smuggled past the
gate.

### Topology (`topology_audit.py`, per solid) — the 3 reconstructed canonicals

| canonical | surface area err | redundant seams (rebuilt vs source) | verdict |
|---|---|---|---|
| uta | 0.0599% | 6 vs 6 | **PASS** |
| lta | 0.0% | 0 vs 0 | **PASS** |
| hub | 0.0555% | 1 vs 0 | **FAIL** |

`hub` has one redundant seam at `(22.8, 0, -29.75)` on the `hub_hubring_or`
cylinder — the hub-ring and rim-ring pieces of the scaffold are built as two
separately-unioned annuli rather than one continuous profile, and the union
leaves a coincident-but-distinct face pair the source doesn't have at that
radius. A targeted fix (build both rings from one combined 2D profile
instead of two separate `.union()`ed solids) would close this; not attempted
given the time budget — documented here rather than silently accepted.

## 4. Editable parameters

All 20 params in `params.py` are `hub_*` / `lta_*` / `uta_*` floats (radii
and Z-bounds of concentric cylindrical bands) — see the inline comments
there for what each one measures and its safety margin from the paired
bound. There are no integer/count params in this recovery (no repeated
pattern was reconstructed this session — every boss/tab/hole array stayed in
the residual).

## 5. Wrap honesty — what's frozen and why

**8 of 11 canonicals are pure frozen imports with no scaffold attempted this
session**: `mount`, `spider`, `collim`, `baffle`, `clamp_a`/`clamp_b`,
`cell`, `bktc`, `bktr` (`parts/frozen_*.step`). None of these are
editable in shape; each is imported and placed at its own solid's original
absolute world coordinates (no transform needed — every part already sits
where the source STEP put it). `spider` is the one canonical that's likely
genuinely hard (confirmed curved, non-constant-cross-section vanes — see
introspect's `structural_freeform_pct: 55.8` and pitfalls.md); the other 7
are plausible future-session reconstruction targets.

**3 of 11 canonicals (`uta`, `lta`, `hub`) are a live parametric scaffold +
frozen residual.** In all three, the scaffold is *only* the single largest
cylindrical band (see §2) — every boss, tab, lug, mounting hole, and (for
`uta`/`cell`) embossed logo is in the residual. The residual is glued to the
scaffold at its as-measured baseline seam (per SKILL.md's residual-defined-
wrap pattern); a large edit to a scaffold param will not regenerate matching
detail in the frozen region (e.g. growing `uta_or` past the true wall
thickness will not grow the lug bosses with it) — see `main.py`'s
`_safe_inner`/`_safe_lo` guards, which exist purely to keep `gen_step()`
returning a valid (if geometrically odd under a large edit) solid rather
than crashing.

## 6. Assumptions

- The 12 solids are **independent printed parts**, not sub-features of one
  design — confirmed by their `solidity.verdict: hollow-or-lattice` (thin
  3D-print shells) and by each occupying its own disjoint region of world
  space.
- Solids 6/11 are a genuine mirror pair (not independently-designed parts
  that happen to share a volume) — confirmed by exact volume match (6 sig.
  figs.) and by both having the same distinctive crescent-band + boss
  silhouette in the renders; the precise mirror transform between them was
  **not** derived this session (both ship as independent frozen imports at
  their own true positions, so this assumption doesn't affect fidelity —
  only the "these two share one canonical" framing in this document).
- No layout/assembly-placement parameters were introduced (e.g. "distance
  between UTA and LTA along the tube") because the source STEP's own
  per-solid world coordinates are not a real assembled-telescope pose (see
  the top-of-file note) — inventing a truss-pole-spacing parameter would be
  fabricating design intent the source doesn't encode.

## 7. Regenerating the deliverables

```bash
cd outputs/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig
uv run --python 3.12 --with cadquery python export_step.py . HadleySource.step
uv run --python 3.12 --with cadquery python export_stl.py . HadleySource.stl
uv run --python 3.12 --with cadquery python check_liveness.py .
python3 ../../../skills/step-to-cadquery/scripts/make_tree.py .
```

If any `hub_*`/`lta_*`/`uta_*` param changes, re-run the residual-defined-
wrap step for that canonical (recompute `residual = fast_cut(original,
scaffold)` from the ORIGINAL solid STEP under `work/solid_*.step`, not from
`gen_step()`'s own output) before trusting the fidelity gate again — the
frozen residual sidecars in `parts/` were captured once, at this session's

codex
I’m going to render each assembly into a temporary `.eval_tmp` directory, run the deterministic printability scorer, then build physical manifests from the specs/code/renders. After the final JSONs are written, I’ll remove those temp directories.
exec
/bin/zsh -lc "mkdir -p outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/.eval_tmp/renders && python3 skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated\\ Snowman\\ Fidget.step outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/.eval_tmp/renders/assembly" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 exited 1 in 0ms:
Traceback (most recent call last):
  File "/Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2/skills/step-to-cadquery/scripts/render_views.py", line 37, in <module>
    import cadquery as cq
ModuleNotFoundError: No module named 'cadquery'

exec
/bin/zsh -lc 'mkdir -p outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/.eval_tmp/renders && python3 skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.step outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/.eval_tmp/renders/assembly' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 exited 1 in 0ms:
Traceback (most recent call last):
  File "/Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2/skills/step-to-cadquery/scripts/render_views.py", line 37, in <module>
    import cadquery as cq
ModuleNotFoundError: No module named 'cadquery'

exec
/bin/zsh -lc 'mkdir -p outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/.eval_tmp/renders && python3 skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.step outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/.eval_tmp/renders/assembly' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 exited 1 in 0ms:
Traceback (most recent call last):
  File "/Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2/skills/step-to-cadquery/scripts/render_views.py", line 37, in <module>
    import cadquery as cq
ModuleNotFoundError: No module named 'cadquery'

exec
/bin/zsh -lc 'mkdir -p outputs/reconstructions/step-to-cadquery/aspire-planter/.eval_tmp/renders && python3 skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.step outputs/reconstructions/step-to-cadquery/aspire-planter/.eval_tmp/renders/assembly' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 exited 1 in 0ms:
Traceback (most recent call last):
  File "/Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2/skills/step-to-cadquery/scripts/render_views.py", line 37, in <module>
    import cadquery as cq
ModuleNotFoundError: No module named 'cadquery'

codex
The direct renderer failed because the active Python environment lacks `cadquery`. I’m switching to the repo’s usual `uv run --with cadquery` path so the required CAD stack is available without changing project files.
codex
`uv` is trying to use a home-directory cache outside the sandbox. I’m redirecting its cache into `/tmp`; if the dependency is already available or can resolve from the managed environment, rendering can proceed there.
exec
/bin/zsh -lc "UV_CACHE_DIR=/tmp/uv-cache uv run --python 3.12 --with cadquery python skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated\\ Snowman\\ Fidget.step outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/.eval_tmp/renders/assembly" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc 'UV_CACHE_DIR=/tmp/uv-cache uv run --python 3.12 --with cadquery python skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.step outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/.eval_tmp/renders/assembly' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc 'UV_CACHE_DIR=/tmp/uv-cache uv run --python 3.12 --with cadquery python skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.step outputs/reconstructions/step-to-cadquery/aspire-planter/.eval_tmp/renders/assembly' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc 'UV_CACHE_DIR=/tmp/uv-cache uv run --python 3.12 --with cadquery python skills/step-to-cadquery/scripts/render_views.py outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.step outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/.eval_tmp/renders/assembly' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 exited 2 in 6567ms:
error: Request failed after 3 retries in 6.6s
  Caused by: Failed to fetch: `https://pypi.org/simple/cadquery/`
  Caused by: error sending request for url (https://pypi.org/simple/cadquery/)
  Caused by: client error (Connect)
  Caused by: dns error
  Caused by: failed to lookup address information: nodename nor servname provided, or not known

 exited 2 in 6611ms:
error: Request failed after 3 retries in 6.6s
  Caused by: Failed to fetch: `https://pypi.org/simple/cadquery/`
  Caused by: error sending request for url (https://pypi.org/simple/cadquery/)
  Caused by: client error (Connect)
  Caused by: dns error
  Caused by: failed to lookup address information: nodename nor servname provided, or not known

 exited 2 in 9638ms:
error: Request failed after 3 retries in 9.5s
  Caused by: Failed to fetch: `https://pypi.org/simple/cadquery/`
  Caused by: error sending request for url (https://pypi.org/simple/cadquery/)
  Caused by: client error (Connect)
  Caused by: dns error
  Caused by: failed to lookup address information: nodename nor servname provided, or not known

 exited 2 in 12428ms:
error: Request failed after 3 retries in 12.4s
  Caused by: Failed to fetch: `https://pypi.org/simple/cadquery/`
  Caused by: error sending request for url (https://pypi.org/simple/cadquery/)
  Caused by: client error (Connect)
  Caused by: dns error
  Caused by: failed to lookup address information: nodename nor servname provided, or not known

codex
Rendering is blocked by the sandboxed environment: `cadquery` is not installed in the active Python, and `uv` cannot fetch it because network is disabled. I’ll still satisfy the visual-context requirement by using the existing STEP/STL evidence, code/spec descriptions, and any available rendered or tree/introspection artifacts, and I’ll record that limitation in each report.
exec
/bin/zsh -lc "find . -maxdepth 4 -type d \\( -name '*cadquery*' -o -name '.venv' -o -name 'venv' \\)" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
./.venv
./skills/step-to-cadquery
./skills/step-inspired-cadquery
./skills/step-to-cadquery-v2
./outputs/skill-feedback/step-to-cadquery
./outputs/skill-feedback/step-inspired-cadquery
./outputs/pitfalls/step-to-cadquery
./outputs/pitfalls/step-inspired-cadquery
./outputs/sessions/step-to-cadquery
./outputs/sessions/step-inspired-cadquery
./outputs/reconstructions/step-to-cadquery
./outputs/reconstructions/url-inspired-cadquery
./outputs/reconstructions/step-inspired-cadquery
./outputs/reconstructions/scad-inspired-cadquery

exec
/bin/zsh -lc "find outputs/reconstructions/step-to-cadquery -path '*/renders/*' -o -path '*/*.png' -o -path '*/*.jpg' | rg '(articulated-snowman-fidget|articulated-tie-step-file-included|aspire-planter|astronomical-telescope-hadley-an-easy-assembly-hig)'" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_iso_top.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_iso_bottom.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_iso_bottom.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_iso_front.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_iso_right.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_iso_right.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_iso_front.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_iso_top.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_iso_back.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_iso_back.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_iso_left.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/orig_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/renders/rebuilt_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_iso_back.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_iso_back.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_iso_left.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_iso_bottom.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_iso_right.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_iso_front.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_iso_front.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_iso_right.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_iso_left.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_iso_bottom.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_iso_back.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_iso_top.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_iso_right.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_iso_front.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_iso_bottom.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_iso_top.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_iso_front.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_iso_right.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_iso_back.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_iso_top.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_iso_left.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_iso_front.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_iso_right.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_iso_back.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_iso_left.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_p0_iso_back.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/res_p0_iso_left.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_iso_top.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_iso_back.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_iso_top.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1b_iso_left.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/rb_s1_iso_bottom.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_iso_bottom.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/aspire-planter/renders/o_s1_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_iso_back.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_iso_back.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_ortho_BACK_+Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_ortho_BOTTOM_-Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_iso_back.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_iso_back.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_iso_bottom.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s11_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s10_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_ortho_LEFT_-X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_iso_front.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s0_iso_right.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_solids/s9_ortho_RIGHT_+X.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_ortho_TOP_+Z.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_iso_back.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_iso_top.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_iso_left.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/orig_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_ortho_FRONT_-Y.png
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_full_iso_back.png

exec
/bin/zsh -lc "for p in articulated-snowman-fidget articulated-tie-step-file-included aspire-planter astronomical-telescope-hadley-an-easy-assembly-hig; do printf '%s\\n' \""'$p"; find outputs/reconstructions/step-to-cadquery/$p -maxdepth 2 -type f '"\\( -name '*.stl' -o -name '*.step' -o -name '*report*.json' -o -name 'evaluation_report.json' \\); done" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc "find outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included outputs/reconstructions/step-to-cadquery/aspire-planter outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig -maxdepth 2 -type d -name parts -print -exec find {} -maxdepth 1 -type f \\;" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_14.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_18.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_7.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_6.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_19.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_15.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_1.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_12.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_13.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_0.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_3.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_10.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_11.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_2.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_9.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_16.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_20.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_5.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_4.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_17.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_8.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_7.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_6.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_1.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_0.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_3.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_10.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_11.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_2.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_9.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_5.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_4.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_8.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_s5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_p4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_s3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_p2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_0.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_p2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_s3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_p4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_s5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_p0.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_s1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_0.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_s1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_p0.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_clamp_a.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/residual_uta.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/residual_hub.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_spider.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_bktc.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_collim.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/residual_lta.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_mount.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_cell.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_clamp_b.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_bktr.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_baffle.step

 succeeded in 0ms:
articulated-snowman-fidget
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated Snowman Fidget.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated Snowman Fidget.stl
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_0.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_1.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_13.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_13.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_12.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/scaffold_hat.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_12.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_0.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_1.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/overshoot_bottom.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/overshoot_7.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_15.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_19.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_19.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_7.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_15.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_14.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_6.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_18.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_7.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/scaffold_eye.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_18.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_14.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/scaffold_chest.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/scaffold_head.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_9.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_17.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_8.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_17.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_5.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_4.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_5.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_4.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_16.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_20.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_9.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_16.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_8.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_20.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_11.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/scaffold_bottom.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_3.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_2.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/scaffold_button.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/scaffold_nose.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_11.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_10.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/orig_solid_2.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/work/rebuilt_solid_10.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_14.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_18.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_7.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_6.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_19.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_15.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_1.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_12.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_13.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_0.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_3.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_10.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_11.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_2.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_9.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_16.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_20.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_5.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_4.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_17.step
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/parts/residual_8.step
articulated-tie-step-file-included
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_0.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_1.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_0_unified.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_6.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_7.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_8.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_4.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_5.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_9.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_11.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_2.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_3.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_10.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/work/rebuilt_solid_10_unified.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_7.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_6.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_1.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_0.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_3.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_10.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_11.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_2.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_9.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_5.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_4.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/parts/residual_8.step
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.stl
aspire-planter
outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_s5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_p4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_s3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_p2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_0.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_p2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_s3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_p4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_s5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_p0.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_s1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_5.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/origc_0.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_4.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_3.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/orig_solid_2.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/rebuilt_s1.step
outputs/reconstructions/step-to-cadquery/aspire-planter/parts/residual_p0.step
outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.stl
astronomical-telescope-hadley-an-easy-assembly-hig
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.stl
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_8.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/rebuilt_hub.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_4.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_5.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_9.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_2.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/rebuilt_uta.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_3.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_0.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_1.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_10.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_6.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/rebuilt_lta.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_7.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/work/solid_11.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_clamp_a.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/residual_uta.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/residual_hub.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_spider.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_bktc.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_collim.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/residual_lta.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_mount.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_cell.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_clamp_b.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_bktr.step
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/parts/frozen_baffle.step

codex
There are existing render PNGs from the reconstruction work for all four projects, so I can use those as visual context even though fresh rendering is unavailable. I’m inspecting representative rebuilt/original orthographic views now.
codex
The visual pass shows the batch covers three laid-out multi-body sets and one posed print-in-place articulated chain. I’m running the deterministic scorers now; for physical checks I’ll use the assembled STL and any per-solid STL exports that already exist or can be generated from mesh splitting.
exec
/bin/zsh -lc "sed -n '1,180p' skills/evaluate-cad-reconstruction/scripts/score_printability.py && sed -n '1,120p' skills/evaluate-cad-reconstruction/scripts/score_physical_correctness.py" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
#!/usr/bin/env python3
"""CLI wrapper for the evaluate-cad-reconstruction printability scorer."""
from pathlib import Path
import sys

SKILL_ROOT = Path(__file__).resolve().parents[1]
if str(SKILL_ROOT) not in sys.path:
    sys.path.insert(0, str(SKILL_ROOT))

from cad_reconstruction_eval.printability_score import main


if __name__ == "__main__":
    raise SystemExit(main())
#!/usr/bin/env python3
"""CLI wrapper for the evaluate-cad-reconstruction physical scorer."""
from pathlib import Path
import sys

SKILL_ROOT = Path(__file__).resolve().parents[1]
if str(SKILL_ROOT) not in sys.path:
    sys.path.insert(0, str(SKILL_ROOT))

from cad_reconstruction_eval.physical_correctness_score import main


if __name__ == "__main__":
    raise SystemExit(main())

exec
/bin/zsh -lc 'python3 skills/evaluate-cad-reconstruction/scripts/score_printability.py --assembly outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.stl --json-out outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/.eval_tmp/printability_report.json' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc "python3 skills/evaluate-cad-reconstruction/scripts/score_printability.py --assembly outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated\\ Snowman\\ Fidget.stl --json-out outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/.eval_tmp/printability_report.json" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 136ms:
{
  "score": 5.5,
  "class": "hard",
  "hard_failures": [],
  "risk_factors": [
    "assembly: 12 disconnected mesh components",
    "assembly: low bed contact area (0.00 mm^2 < 25.00 mm^2)"
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/Articulated_tie.stl",
    "score": 5.5,
    "class": "hard",
    "hard_failures": [],
    "risk_factors": [
      "12 disconnected mesh components",
      "low bed contact area (0.00 mm^2 < 25.00 mm^2)"
    ],
    "metrics": {
      "triangle_count": 7886,
      "bbox_min": [
        -35.0,
        -459.305,
        -1.5311
      ],
      "bbox_max": [
        35.0,
        -341.9232,
        88.7942
      ],
      "bbox_size": [
        70.0,
        117.3818,
        90.3253
      ],
      "surface_area_mm2": 54913.1551,
      "volume_mm3": 117294.6001,
      "degenerate_triangles": 0,
      "watertight": true,
      "bad_edge_count": 0,
      "bad_edge_length_mm": 0.0,
      "connected_components": 12,
      "tiny_components": 0,
      "selected_orientation": "x_min",
      "bed_contact_area_mm2": 0.0,
      "unsupported_overhang_area_mm2": 3238.2489,
      "unsupported_overhang_fraction": 0.05897
    },
    "assumptions": {
      "build_volume_mm": [
        220.0,
        220.0,
        250.0
      ],
      "overhang_angle_deg": 45.0,
      "bed_epsilon_mm": 0.05,
      "min_bed_contact_area_mm2": 25.0,
      "tiny_component_area_fraction": 0.001,
      "high_triangle_count": 500000,
      "severe_bad_boundary_surface_ratio": 0.5,
      "severe_bad_edge_count": 32
    }
  },
  "parts": [],
  "assumptions": {
    "build_volume_mm": [
      220.0,
      220.0,
      250.0
    ],
    "overhang_angle_deg": 45.0,
    "bed_epsilon_mm": 0.05,
    "min_bed_contact_area_mm2": 25.0,
    "tiny_component_area_fraction": 0.001,
    "high_triangle_count": 500000,
    "severe_bad_boundary_surface_ratio": 0.5,
    "severe_bad_edge_count": 32
  }
}

exec
/bin/zsh -lc 'python3 skills/evaluate-cad-reconstruction/scripts/score_printability.py --assembly outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.stl --json-out outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/.eval_tmp/printability_report.json' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
exec
/bin/zsh -lc 'python3 skills/evaluate-cad-reconstruction/scripts/score_printability.py --assembly outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.stl --json-out outputs/reconstructions/step-to-cadquery/aspire-planter/.eval_tmp/printability_report.json' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 1064ms:
{
  "score": 5.5,
  "class": "hard",
  "hard_failures": [],
  "risk_factors": [
    "assembly: 23 disconnected mesh components",
    "assembly: 6 tiny disconnected components"
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/Articulated Snowman Fidget.stl",
    "score": 5.5,
    "class": "hard",
    "hard_failures": [],
    "risk_factors": [
      "23 disconnected mesh components",
      "6 tiny disconnected components"
    ],
    "metrics": {
      "triangle_count": 129956,
      "bbox_min": [
        -24.915,
        -24.9606,
        -44.7095
      ],
      "bbox_max": [
        124.9997,
        24.9606,
        58.5901
      ],
      "bbox_size": [
        149.9148,
        49.9213,
        103.2996
      ],
      "surface_area_mm2": 82331.1871,
      "volume_mm3": 171993.4981,
      "degenerate_triangles": 0,
      "watertight": true,
      "bad_edge_count": 0,
      "bad_edge_length_mm": 0.0,
      "connected_components": 23,
      "tiny_components": 6,
      "selected_orientation": "z_min",
      "bed_contact_area_mm2": 2845.3573,
      "unsupported_overhang_area_mm2": 6839.8068,
      "unsupported_overhang_fraction": 0.083077
    },
    "assumptions": {
      "build_volume_mm": [
        220.0,
        220.0,
        250.0
      ],
      "overhang_angle_deg": 45.0,
      "bed_epsilon_mm": 0.05,
      "min_bed_contact_area_mm2": 25.0,
      "tiny_component_area_fraction": 0.001,
      "high_triangle_count": 500000,
      "severe_bad_boundary_surface_ratio": 0.5,
      "severe_bad_edge_count": 32
    }
  },
  "parts": [],
  "assumptions": {
    "build_volume_mm": [
      220.0,
      220.0,
      250.0
    ],
    "overhang_angle_deg": 45.0,
    "bed_epsilon_mm": 0.05,
    "min_bed_contact_area_mm2": 25.0,
    "tiny_component_area_fraction": 0.001,
    "high_triangle_count": 500000,
    "severe_bad_boundary_surface_ratio": 0.5,
    "severe_bad_edge_count": 32
  }
}

 succeeded in 2075ms:
{
  "score": 4.0,
  "class": "hard",
  "hard_failures": [
    "mesh has severe open/non-manifold boundaries"
  ],
  "risk_factors": [
    "assembly: 24 degenerate triangles",
    "assembly: 10 disconnected mesh components",
    "assembly: 1 tiny disconnected components"
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/HadleySource.stl",
    "score": 4.0,
    "class": "hard",
    "hard_failures": [
      "mesh has severe open/non-manifold boundaries"
    ],
    "risk_factors": [
      "24 degenerate triangles",
      "10 disconnected mesh components",
      "1 tiny disconnected components"
    ],
    "metrics": {
      "triangle_count": 246741,
      "bbox_min": [
        -146.7626,
        -124.8365,
        -56.0
      ],
      "bbox_max": [
        117.5882,
        140.7606,
        1217.2625
      ],
      "bbox_size": [
        264.3508,
        265.5972,
        1273.2625
      ],
      "surface_area_mm2": 420965.9815,
      "volume_mm3": 896506.3102,
      "degenerate_triangles": 24,
      "watertight": false,
      "bad_edge_count": 2109,
      "bad_edge_length_mm": 1382.4991,
      "connected_components": 10,
      "tiny_components": 1,
      "bad_boundary_surface_ratio": 2.130793,
      "selected_orientation": "z_min",
      "bed_contact_area_mm2": 5936.7153,
      "unsupported_overhang_area_mm2": 33959.2888,
      "unsupported_overhang_fraction": 0.08067
    },
    "assumptions": {
      "build_volume_mm": [
        220.0,
        220.0,
        250.0
      ],
      "overhang_angle_deg": 45.0,
      "bed_epsilon_mm": 0.05,
      "min_bed_contact_area_mm2": 25.0,
      "tiny_component_area_fraction": 0.001,
      "high_triangle_count": 500000,
      "severe_bad_boundary_surface_ratio": 0.5,
      "severe_bad_edge_count": 32
    }
  },
  "parts": [],
  "assumptions": {
    "build_volume_mm": [
      220.0,
      220.0,
      250.0
    ],
    "overhang_angle_deg": 45.0,
    "bed_epsilon_mm": 0.05,
    "min_bed_contact_area_mm2": 25.0,
    "tiny_component_area_fraction": 0.001,
    "high_triangle_count": 500000,
    "severe_bad_boundary_surface_ratio": 0.5,
    "severe_bad_edge_count": 32
  }
}

 succeeded in 5681ms:
{
  "score": 6.7,
  "class": "hard",
  "hard_failures": [],
  "risk_factors": [
    "assembly: 9 disconnected mesh components",
    "assembly: very high triangle count (687056)"
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/aspire-planter/aspire-planter.stl",
    "score": 6.7,
    "class": "hard",
    "hard_failures": [],
    "risk_factors": [
      "9 disconnected mesh components",
      "very high triangle count (687056)"
    ],
    "metrics": {
      "triangle_count": 687056,
      "bbox_min": [
        -107.1963,
        -56.7976,
        -86.0018
      ],
      "bbox_max": [
        107.1962,
        547.1852,
        132.4
      ],
      "bbox_size": [
        214.3925,
        603.9828,
        218.4018
      ],
      "surface_area_mm2": 930130.7119,
      "volume_mm3": 1628587.0563,
      "degenerate_triangles": 0,
      "watertight": true,
      "bad_edge_count": 0,
      "bad_edge_length_mm": 0.0,
      "connected_components": 9,
      "tiny_components": 0,
      "selected_orientation": "z_min",
      "bed_contact_area_mm2": 25339.2204,
      "unsupported_overhang_area_mm2": 62435.343,
      "unsupported_overhang_fraction": 0.067125
    },
    "assumptions": {
      "build_volume_mm": [
        220.0,
        220.0,
        250.0
      ],
      "overhang_angle_deg": 45.0,
      "bed_epsilon_mm": 0.05,
      "min_bed_contact_area_mm2": 25.0,
      "tiny_component_area_fraction": 0.001,
      "high_triangle_count": 500000,
      "severe_bad_boundary_surface_ratio": 0.5,
      "severe_bad_edge_count": 32
    }
  },
  "parts": [],
  "assumptions": {
    "build_volume_mm": [
      220.0,
      220.0,
      250.0
    ],
    "overhang_angle_deg": 45.0,
    "bed_epsilon_mm": 0.05,
    "min_bed_contact_area_mm2": 25.0,
    "tiny_component_area_fraction": 0.001,
    "high_triangle_count": 500000,
    "severe_bad_boundary_surface_ratio": 0.5,
    "severe_bad_edge_count": 32
  }
}

codex
The printability reports are done. The next piece is mostly physical intent: these projects do not expose placed-instance STLs, so the runnable conditions will lean on assembled component counts, expected feature inventories, and explicit notes where hinge/elastic/thread/load behavior is outside the current deterministic helpers.
exec
/bin/zsh -lc "sed -n '1220,1325p' skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py && sed -n '1080,1165p' skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py && sed -n '940,985p' skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
    return _condition_result(condition, "inconclusive", f"unsupported check: {check}")


def _combine_scores(
    baseline: dict,
    condition_results: list[dict],
    assumptions: PhysicalCorrectnessAssumptions,
) -> dict:
    score = float(baseline["score"])
    hard_failures = list(baseline["hard_failures"])
    risk_factors = list(baseline["risk_factors"])
    cap = 10.0

    for result in condition_results:
        severity = result["severity"]
        cid = result["id"]
        if result["status"] == "fail":
            if severity == "critical":
                cap = min(cap, assumptions.critical_failure_cap)
                hard_failures.append(f"critical condition failed: {cid}")
                score = min(score, assumptions.critical_failure_cap)
            else:
                risk_factors.append(f"{severity} condition failed: {cid}")
                score -= _severity_penalty(severity, assumptions)
        elif result["status"] == "inconclusive":
            risk_factors.append(f"condition inconclusive: {cid}")
            score -= assumptions.inconclusive_penalty

    score = _round_score(min(score, cap))
    return {
        "score": score,
        "class": _class_for_score(score),
        "hard_failures": list(dict.fromkeys(hard_failures)),
        "risk_factors": list(dict.fromkeys(risk_factors)),
    }


def score_physical_correctness_project(
    assembly: str | os.PathLike | None = None,
    parts: Iterable[str | os.PathLike] | dict[str, str | os.PathLike] | None = None,
    conditions: list[dict] | None = None,
    assumptions: PhysicalCorrectnessAssumptions | None = None,
) -> dict:
    assumptions = assumptions or PhysicalCorrectnessAssumptions()
    if isinstance(parts, dict):
        part_items = list(parts.items())
        part_paths = [path for _, path in part_items]
        part_names = [name for name, _ in part_items]
    else:
        part_paths = list(parts or [])
        part_names = [Path(path).stem for path in part_paths]

    baseline = score_usability_project(
        assembly=assembly,
        parts=part_paths,
        assumptions=UsabilityAssumptions(),
    )
    part_reports = baseline["parts"]
    parts_by_name = {name: report for name, report in zip(part_names, part_reports)}
    condition_results = [
        _run_condition(condition, baseline["assembly"], parts_by_name, part_reports, assumptions)
        for condition in (conditions or [])
    ]
    combined = _combine_scores(baseline, condition_results, assumptions)
    return {
        **combined,
        "metrics": baseline["metrics"],
        "condition_results": condition_results,
        "assembly": baseline["assembly"],
        "parts": part_reports,
        "part_names": part_names,
        "assumptions": {
            "physical_correctness": asdict(assumptions),
            "layer1_geometry": baseline["assumptions"],
        },
    }


def score_condition_manifest(
    path: str | os.PathLike,
    assumptions: PhysicalCorrectnessAssumptions | None = None,
) -> dict:
    manifest_path = Path(path)
    data = json.loads(manifest_path.read_text())
    root = manifest_path.parent
    assembly = data.get("assembly")
    assembly_path = root / assembly if assembly else None
    raw_parts = data.get("parts", {})
    if isinstance(raw_parts, dict):
        parts = {name: root / rel for name, rel in raw_parts.items()}
    else:
        parts = [root / rel for rel in raw_parts]
    return score_physical_correctness_project(
        assembly=assembly_path,
        parts=parts,
        conditions=data.get("conditions", []),
        assumptions=assumptions,
    )


def build_arg_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Score project physical correctness.")
    parser.add_argument("--assembly", help="assembled STL path")
    parser.add_argument("--parts", nargs="*", default=None, help="separate part STL paths")
    parser.add_argument("--condition-manifest", help="JSON condition manifest")
    parser.add_argument("--json-out", help="optional output JSON path")
            "min_forbidden_clearance_mm": min_forbidden,
        },
    )


def _check_assembly_component_count(condition: dict, assembly_report: dict | None, part_reports: list[dict]) -> dict:
    if assembly_report is None:
        return _condition_result(condition, "inconclusive", "no assembly STL supplied")
    if not assembly_report.get("loadable"):
        return _condition_result(condition, "inconclusive", "assembly STL failed to load")

    expected = condition.get("thresholds", {}).get("expected_components")
    if expected is None:
        expected = len(part_reports)
    actual = int(assembly_report["metrics"]["connected_components"])
    status = "pass" if actual == int(expected) else "fail"
    return _condition_result(
        condition,
        status,
        f"assembly connected components {actual} {'==' if status == 'pass' else '!='} expected {expected}",
        {"actual_components": actual, "expected_components": int(expected)},
    )


def _condition_result(
    condition: dict,
    status: str,
    detail: str,
    measurements: dict[str, Any] | None = None,
) -> dict:
    return {
        "id": condition.get("id", "unnamed_condition"),
        "category": condition.get("category", "physical_correctness"),
        "severity": condition.get("severity", "major"),
        "check": condition.get("check", "unknown"),
        "status": status,
        "description": condition.get("description", ""),
        "detail": detail,
        "measurements": measurements or {},
    }


def _check_assembly_sequence(
    condition: dict,
    assembly_report: dict | None,
    parts_by_name: dict[str, dict],
    part_reports: list[dict],
    assumptions: PhysicalCorrectnessAssumptions,
) -> dict:
    steps = condition.get("inputs", {}).get("steps", [])
    if not isinstance(steps, list) or not steps:
        return _condition_result(condition, "inconclusive", "assembly_sequence requires a non-empty steps list")

    step_results: list[dict] = []
    failed_steps: list[str] = []
    inconclusive_steps: list[str] = []
    for index, step in enumerate(steps):
        if not isinstance(step, dict):
            return _condition_result(condition, "inconclusive", f"assembly_sequence step {index} must be an object")
        subcondition = {
            "id": step.get("id", f"step_{index}"),
            "category": step.get("category", condition.get("category", "motion_path")),
            "severity": step.get("severity", condition.get("severity", "major")),
            "description": step.get("description", ""),
            "check": step.get("check"),
            "inputs": step.get("inputs", {}),
            "thresholds": step.get("thresholds", {}),
        }
        result = _run_condition(subcondition, assembly_report, parts_by_name, part_reports, assumptions)
        step_results.append(result)
        if result["status"] == "fail":
            failed_steps.append(result["id"])
        elif result["status"] == "inconclusive":
            inconclusive_steps.append(result["id"])

    if failed_steps:
        status = "fail"
        detail = f"assembly sequence failed at step(s): {', '.join(failed_steps)}"
    elif inconclusive_steps:
        status = "inconclusive"
        detail = f"assembly sequence inconclusive at step(s): {', '.join(inconclusive_steps)}"
    else:
        status = "pass"
        detail = f"assembly sequence passed {len(step_results)} steps"
    return _condition_result(
        condition,
            "min_clear_rays": int(min_clear_rays) if min_clear_rays is not None else None,
            "min_clear_fraction": float(min_clear_fraction) if min_clear_fraction is not None else None,
            "max_intersections_per_clear_ray": max_intersections,
            "intersection_count_min": min(intersection_counts) if intersection_counts else None,
            "intersection_count_max": max(intersection_counts) if intersection_counts else None,
            "proxy_limitation": "samples a rectangular grid of straight rays through an expected vent field; it estimates representative open paths, not exact open area, hole count, or airflow",
        },
    )


def _check_feature_count(condition: dict, parts_by_name: dict[str, dict]) -> dict:
    inputs = condition.get("inputs", {})
    if "features" in inputs:
        names = [name for name in inputs.get("features", []) if name in parts_by_name]
    elif "part_name_prefix" in inputs:
        prefix = str(inputs["part_name_prefix"])
        names = [name for name in parts_by_name if name.startswith(prefix)]
    else:
        return _condition_result(condition, "inconclusive", "feature_count requires features or part_name_prefix")
    thresholds = condition.get("thresholds", {})
    expected = thresholds.get("expected_count")
    min_count = thresholds.get("min_count", expected)
    max_count = thresholds.get("max_count", expected)
    actual = len(names)
    passed = True
    if min_count is not None:
        passed = passed and actual >= int(min_count)
    if max_count is not None:
        passed = passed and actual <= int(max_count)
    return _condition_result(
        condition,
        "pass" if passed else "fail",
        f"feature count {actual}",
        {
            "actual_count": actual,
            "matched_features": sorted(names),
            "expected_count": int(expected) if expected is not None else None,
            "min_count": int(min_count) if min_count is not None else None,
            "max_count": int(max_count) if max_count is not None else None,
        },
    )


def _check_cylindrical_fit(condition: dict) -> dict:
    inputs = condition.get("inputs", {})
    try:

codex
To keep the physical reports measurable rather than just narrative, I’m checking whether the local Python can split STL connected components. If it can, I’ll create temporary per-component STLs under `.eval_tmp` and use them only for the manifest/scorer, then delete them after merging results.
exec
/bin/zsh -lc "python3 - <<'PY'
try:
 import trimesh
 print('trimesh ok', trimesh.__version__)
except Exception as e:
 print('trimesh missing', e)
try:
 import numpy
 print('numpy ok')
except Exception as e:
 print('numpy missing', e)
PY" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
trimesh missing No module named 'trimesh'
numpy ok

codex
`trimesh` is not installed, so splitting components into separate temporary STLs would require adding more tooling than this iteration calls for. I’ll keep the physical manifests narrow: component-count checks plus direct param-based cylindrical-fit checks where the helper supports them without meshes.
exec
/bin/zsh -lc "sed -n '983,1030p' skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py && sed -n '592,664p' skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py && sed -n '629,664p' skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
def _check_cylindrical_fit(condition: dict) -> dict:
    inputs = condition.get("inputs", {})
    try:
        pin_diameter = float(inputs.get("pin_diameter_mm"))
        hole_diameter = float(inputs.get("hole_diameter_mm"))
    except Exception:
        return _condition_result(condition, "inconclusive", "cylindrical_fit requires numeric pin_diameter_mm and hole_diameter_mm")
    clearance = hole_diameter - pin_diameter
    thresholds = condition.get("thresholds", {})
    min_clearance = float(thresholds.get("min_diameter_clearance_mm", 0.0))
    max_clearance = thresholds.get("max_diameter_clearance_mm")
    max_clearance = float(max_clearance) if max_clearance is not None else None
    passed = clearance >= min_clearance and (max_clearance is None or clearance <= max_clearance)
    return _condition_result(
        condition,
        "pass" if passed else "fail",
        f"diameter clearance {clearance:.4f} mm",
        {
            "pin_diameter_mm": pin_diameter,
            "hole_diameter_mm": hole_diameter,
            "diameter_clearance_mm": round(clearance, 6),
            "min_diameter_clearance_mm": min_clearance,
            "max_diameter_clearance_mm": max_clearance,
        },
    )


def _pair_distance(
    condition: dict,
    parts_by_name: dict[str, dict],
    assumptions: PhysicalCorrectnessAssumptions,
    pair: list[str],
) -> tuple[float | None, str | None]:
    if len(pair) != 2:
        return None, f"contact pair must contain exactly two part names: {pair}"
    tri_a, err_a = _load_part_triangles(parts_by_name, pair[0])
    tri_b, err_b = _load_part_triangles(parts_by_name, pair[1])
    if err_a or err_b:
        return None, "; ".join(err for err in (err_a, err_b) if err)
    assert tri_a is not None and tri_b is not None
    return _mesh_min_distance(tri_a, tri_b, assumptions), None


def _check_contact_graph(
    condition: dict,
    parts_by_name: dict[str, dict],
    assumptions: PhysicalCorrectnessAssumptions,
) -> dict:
def _check_axis_alignment(condition: dict) -> dict:
    inputs = condition.get("inputs", {})
    axis_a = inputs.get("axis_a", {})
    axis_b = inputs.get("axis_b", {})
    point_a, err = _parse_vector(axis_a.get("point"), "axis_a.point")
    if err:
        return _condition_result(condition, "inconclusive", err)
    point_b, err = _parse_vector(axis_b.get("point"), "axis_b.point")
    if err:
        return _condition_result(condition, "inconclusive", err)
    direction_a, err = _normalize_vector(axis_a.get("direction"))
    if err:
        return _condition_result(condition, "inconclusive", f"axis_a.direction {err}")
    direction_b, err = _normalize_vector(axis_b.get("direction"))
    if err:
        return _condition_result(condition, "inconclusive", f"axis_b.direction {err}")
    assert point_a is not None and point_b is not None and direction_a is not None and direction_b is not None
    dot = abs(float(np.clip(np.dot(direction_a, direction_b), -1.0, 1.0)))
    angle = float(np.degrees(np.arccos(dot)))
    offset = _line_to_line_distance(point_a, direction_a, point_b, direction_b)
    thresholds = condition.get("thresholds", {})
    max_offset = float(thresholds.get("max_axis_offset_mm", 0.5))
    max_angle = float(thresholds.get("max_angle_deg", 2.0))
    passed = offset <= max_offset and angle <= max_angle
    return _condition_result(
        condition,
        "pass" if passed else "fail",
        f"axis offset {offset:.4f} mm, angle {angle:.4f} deg",
        {
            "axis_offset_mm": round(offset, 6),
            "axis_angle_deg": round(angle, 6),
            "max_axis_offset_mm": max_offset,
            "max_angle_deg": max_angle,
        },
    )


def _check_relative_pose(
    condition: dict,
    parts_by_name: dict[str, dict],
) -> dict:
    name_a, name_b, tri_a, tri_b, error = _load_two_named_parts(condition, parts_by_name)
    if error is not None:
        return error
    axis, axis_error = _normalize_vector(condition.get("inputs", {}).get("axis"))
    if axis_error:
        return _condition_result(condition, "inconclusive", f"axis {axis_error}")
    assert name_a is not None and name_b is not None and tri_a is not None and tri_b is not None and axis is not None
    centroid_a = _mesh_centroid(tri_a)
    centroid_b = _mesh_centroid(tri_b)
    delta = float(np.dot(centroid_b - centroid_a, axis))
    thresholds = condition.get("thresholds", {})
    min_delta = thresholds.get("min_delta_mm")
    max_delta = thresholds.get("max_delta_mm")
    passed = True
    if min_delta is not None:
        passed = passed and delta >= float(min_delta)
    if max_delta is not None:
        passed = passed and delta <= float(max_delta)
    return _condition_result(
        condition,
        "pass" if passed else "fail",
        f"centroid delta along axis {delta:.4f} mm",
        {
            "part_a": name_a,
            "part_b": name_b,
            "centroid_delta_along_axis_mm": round(delta, 6),
            "min_delta_mm": float(min_delta) if min_delta is not None else None,
            "max_delta_mm": float(max_delta) if max_delta is not None else None,
        },
    )


def _check_relative_pose(
    condition: dict,
    parts_by_name: dict[str, dict],
) -> dict:
    name_a, name_b, tri_a, tri_b, error = _load_two_named_parts(condition, parts_by_name)
    if error is not None:
        return error
    axis, axis_error = _normalize_vector(condition.get("inputs", {}).get("axis"))
    if axis_error:
        return _condition_result(condition, "inconclusive", f"axis {axis_error}")
    assert name_a is not None and name_b is not None and tri_a is not None and tri_b is not None and axis is not None
    centroid_a = _mesh_centroid(tri_a)
    centroid_b = _mesh_centroid(tri_b)
    delta = float(np.dot(centroid_b - centroid_a, axis))
    thresholds = condition.get("thresholds", {})
    min_delta = thresholds.get("min_delta_mm")
    max_delta = thresholds.get("max_delta_mm")
    passed = True
    if min_delta is not None:
        passed = passed and delta >= float(min_delta)
    if max_delta is not None:
        passed = passed and delta <= float(max_delta)
    return _condition_result(
        condition,
        "pass" if passed else "fail",
        f"centroid delta along axis {delta:.4f} mm",
        {
            "part_a": name_a,
            "part_b": name_b,
            "centroid_delta_along_axis_mm": round(delta, 6),
            "min_delta_mm": float(min_delta) if min_delta is not None else None,
            "max_delta_mm": float(max_delta) if max_delta is not None else None,
        },
    )



codex
I found one local deterministic gap in the batch, but not one worth changing code for: several models need placed-instance mesh extraction from STEP/STL, and the current environment lacks the mesh-splitting dependency. That is broader than a narrow helper, so I’m leaving it as a missing-helper/workflow note rather than altering scoring behavior.
exec
/bin/zsh -lc "python3 - <<'PY'
import json
from pathlib import Path
root = Path('outputs/reconstructions/step-to-cadquery')
projects = {
'articulated-snowman-fidget': {
 'assembly':'../Articulated Snowman Fidget.stl',
 'design_intent_inventory': {
  'parts':'Three example snowmen: 21 intended placed solids (bottom/chest/head per group, hats/accessories/face pegs where present). Elastic cord through internal bores and glued decorative pegs are design intent.',
  'visual_context':'Existing rebuilt/original renders under work/ inspected; fresh render_views.py could not run because cadquery is unavailable in the active Python and network is disabled for uv dependency resolution.'
 },
 'interaction_matrix_summary':[
  'Each group bottom/chest/head: separate stacked ball segments with elastic-cord bore alignment and socket seating.',
  'Hat to head and face/button pegs to holes: glue/contact/clearance fit.',
  'Groups A/B/C: no interaction; laid out examples.'
 ],
 'conditions':[
  {'id':'snowman_layout_has_expected_placed_solids','category':'part_inventory','severity':'major','check':'assembly_component_count','description':'The STL should contain the 21 intended source solids without extra detached artifacts.','thresholds':{'expected_components':21}},
  {'id':'nose_pegs_fit_head_holes','category':'fit_clearance','severity':'minor','check':'cylindrical_fit','description':'Parametric nose peg diameter should be smaller than the reconstructed head nose hole diameter.','inputs':{'pin_diameter_mm':4.4,'hole_diameter_mm':5.2},'thresholds':{'min_diameter_clearance_mm':0.2,'max_diameter_clearance_mm':1.2}},
  {'id':'eye_pegs_fit_head_holes','category':'fit_clearance','severity':'minor','check':'cylindrical_fit','description':'Parametric eye peg diameter should be smaller than the reconstructed head eye hole diameter.','inputs':{'pin_diameter_mm':3.4,'hole_diameter_mm':4.2},'thresholds':{'min_diameter_clearance_mm':0.2,'max_diameter_clearance_mm':1.2}}
 ],
 'missing_helper_notes':[{'relationship':'elastic cord through bottom/chest/head bores','triage':'missing_helper','reason':'Requires path continuity through internal cavities and flexible cord behavior; no placed-instance cavity/path extraction available in this environment.'},{'relationship':'ball socket seating and articulation','triage':'proxy_now','reason':'Could be approximated by placed-instance contact/axis checks after component extraction; not implemented in this iteration.'}]
},
'articulated-tie-step-file-included': {
 'assembly':'../Articulated_tie.stl',
 'design_intent_inventory': {'parts':'Print-in-place articulated tie with 10 links, one pointed tip cap, and one shirt clip: 12 intended connected components in the posed print layout.','visual_context':'Existing rebuilt/original work renders inspected; fresh render_views.py unavailable for the same cadquery/network reason.'},
 'interaction_matrix_summary':['Adjacent links: separate print-in-place hinge pairs with X-axis pin/bore fit and rotational clearance.','Clip engages neck-end hinge; tip cap engages final link.','Nonadjacent folded links should not collide in packed print pose.'],
 'conditions':[
  {'id':'tie_has_12_print_in_place_components','category':'separation_correctness','severity':'major','check':'assembly_component_count','description':'The printed pose should preserve 10 links plus tip plus clip as 12 separate components.','thresholds':{'expected_components':12}},
  {'id':'hinge_pin_bore_clearance_matches_readme','category':'fit_clearance','severity':'major','check':'cylindrical_fit','description':'Hinge bore diameter should exceed pin diameter by about 0.6 mm total, matching 0.3 mm radial tolerance.','inputs':{'pin_diameter_mm':3.0,'hole_diameter_mm':3.6},'thresholds':{'min_diameter_clearance_mm':0.5,'max_diameter_clearance_mm':0.8}}
 ],
 'missing_helper_notes':[{'relationship':'full hinge rotation path for each seated joint','triage':'missing_helper','reason':'Needs joint-aware kinematics and placed-instance geometry; existing rotation helper is too coarse for print-in-place seated hinge knuckles.'},{'relationship':'clip spring compliance and shirt retention','triage':'missing_helper','reason':'Requires material flexibility/friction and external fabric geometry.'}]
},
'aspire-planter': {
 'assembly':'../aspire-planter.stl',
 'design_intent_inventory': {'parts':'Six-piece planter set: three fluted pots and three matching saucers in a layout row. Pots are containers with open tops and drainage; saucers receive/support pots.','visual_context':'Existing renders under renders/ inspected, including rebuilt pot/saucer views and residual base views; fresh render_views.py unavailable.'},
 'interaction_matrix_summary':['Each pot/saucer size pair: pot sits in or over corresponding saucer; current STEP is a laid-out set, not nested use pose.','Pot bodies: open top, fluted wall, drainage holes/logo in frozen residual.','Saucers: shallow trays with floor tabs/logo and large saucer drain grid in frozen residual.'],
 'conditions':[
  {'id':'planter_set_has_six_primary_printed_parts','category':'part_inventory','severity':'major','check':'assembly_component_count','description':'The set should contain six primary printable parts; additional components indicate unwelded residual/scaffold fragments in exported STL.','thresholds':{'expected_components':6}}
 ],
 'missing_helper_notes':[{'relationship':'pot drains and saucer drain grid open-area preservation','triage':'proxy_now','reason':'Existing vent/opening proxies could sample rays if placed component meshes or coordinates were prepared; not needed as a new helper.'},{'relationship':'pot-in-saucer seating/support','triage':'missing_helper','reason':'Requires external/use-pose placement, since the exported assembly is a laid-out print set rather than nested pots in saucers.'}]
},
'astronomical-telescope-hadley-an-easy-assembly-hig': {
 'assembly':'../HadleySource.stl',
 'design_intent_inventory': {'parts':'Hadley telescope printed hardware kit: 12 intended printed solids/instances, including UTA/LTA rings, hub, mount, spider, collimation ring, baffle, two clamps, mirror cell, and two small brackets. Not a fully assembled telescope scene.','visual_context':'Existing orig/rebuilt renders under renders/ inspected; fresh render_views.py unavailable.'},
 'interaction_matrix_summary':['Printed hardware parts are laid out in source/design coordinates; most interactions involve external tube/truss/mirror/focuser hardware not present in the STL.','Clamps/brackets/rings require fastener holes and tube/pole fit; spider/mirror/focuser parts require external optical/mechanical interfaces.'],
 'conditions':[
  {'id':'hadley_kit_has_12_printed_part_instances','category':'part_inventory','severity':'major','check':'assembly_component_count','description':'The STL should preserve the 12 intended printed hardware solids/instances.','thresholds':{'expected_components':12}}
 ],
 'missing_helper_notes':[{'relationship':'tube, truss pole, mirror, focuser, screw and clamp fit','triage':'missing_helper','reason':'External mating hardware is absent; requires external proxy dimensions not encoded in this report.'},{'relationship':'thread engagement and load-bearing stiffness','triage':'missing_helper','reason':'Requires thread/material/load simulation beyond current deterministic helpers.'}]
}
}
for name, data in projects.items():
    tmp = root/name/'.eval_tmp'
    tmp.mkdir(exist_ok=True)
    manifest = {'assembly': data['assembly'], 'parts': {}, 'design_intent_inventory': data['design_intent_inventory'], 'interaction_matrix_summary': data['interaction_matrix_summary'], 'conditions': data['conditions'], 'missing_helper_notes': data['missing_helper_notes']}
    (tmp/'physical_conditions.json').write_text(json.dumps(manifest, indent=2))
PY
for p in articulated-snowman-fidget articulated-tie-step-file-included aspire-planter astronomical-telescope-hadley-an-easy-assembly-hig; do python3 skills/evaluate-cad-reconstruction/scripts/score_physical_correctness.py --condition-manifest outputs/reconstructions/step-to-cadquery/"'$p/.eval_tmp/physical_conditions.json --json-out outputs/reconstructions/step-to-cadquery/$p/.eval_tmp/physical_correctness_report.json; done' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 9717ms:
{
  "score": 6.0,
  "class": "hard",
  "hard_failures": [],
  "risk_factors": [
    "assembly has 23 disconnected components but no separate part STLs were supplied",
    "major condition failed: snowman_layout_has_expected_placed_solids"
  ],
  "metrics": {
    "part_count": 0
  },
  "condition_results": [
    {
      "id": "snowman_layout_has_expected_placed_solids",
      "category": "part_inventory",
      "severity": "major",
      "check": "assembly_component_count",
      "status": "fail",
      "description": "The STL should contain the 21 intended source solids without extra detached artifacts.",
      "detail": "assembly connected components 23 != expected 21",
      "measurements": {
        "actual_components": 23,
        "expected_components": 21
      }
    },
    {
      "id": "nose_pegs_fit_head_holes",
      "category": "fit_clearance",
      "severity": "minor",
      "check": "cylindrical_fit",
      "status": "pass",
      "description": "Parametric nose peg diameter should be smaller than the reconstructed head nose hole diameter.",
      "detail": "diameter clearance 0.8000 mm",
      "measurements": {
        "pin_diameter_mm": 4.4,
        "hole_diameter_mm": 5.2,
        "diameter_clearance_mm": 0.8,
        "min_diameter_clearance_mm": 0.2,
        "max_diameter_clearance_mm": 1.2
      }
    },
    {
      "id": "eye_pegs_fit_head_holes",
      "category": "fit_clearance",
      "severity": "minor",
      "check": "cylindrical_fit",
      "status": "pass",
      "description": "Parametric eye peg diameter should be smaller than the reconstructed head eye hole diameter.",
      "detail": "diameter clearance 0.8000 mm",
      "measurements": {
        "pin_diameter_mm": 3.4,
        "hole_diameter_mm": 4.2,
        "diameter_clearance_mm": 0.8,
        "min_diameter_clearance_mm": 0.2,
        "max_diameter_clearance_mm": 1.2
      }
    }
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/.eval_tmp/../Articulated Snowman Fidget.stl",
    "loadable": true,
    "hard_failures": [],
    "metrics": {
      "triangle_count": 129956,
      "bbox_min": [
        -24.915,
        -24.9606,
        -44.7095
      ],
      "bbox_max": [
        124.9997,
        24.9606,
        58.5901
      ],
      "bbox_size": [
        149.9148,
        49.9213,
        103.2996
      ],
      "bbox_volume_mm3": 773088.1575,
      "surface_area_mm2": 82331.1871,
      "volume_mm3": 171993.4981,
      "connected_components": 23,
      "watertight": true,
      "bad_edge_count": 0
    }
  },
  "parts": [],
  "part_names": [],
  "assumptions": {
    "physical_correctness": {
      "critical_failure_cap": 4.0,
      "major_failure_penalty": 2.0,
      "minor_failure_penalty": 0.8,
      "inconclusive_penalty": 0.3,
      "max_sample_points": 500,
      "max_sample_triangles": 1000,
      "collision_epsilon_mm": 1e-06,
      "collision_probe_distance_mm": 0.1
    },
    "layer1_geometry": {
      "component_count_mismatch_penalty": 1.0,
      "missing_parts_component_penalty": 0.5,
      "max_missing_parts_component_penalty": 2.0
    }
  }
}
{
  "score": 8.0,
  "class": "easy",
  "hard_failures": [],
  "risk_factors": [
    "assembly has 12 disconnected components but no separate part STLs were supplied"
  ],
  "metrics": {
    "part_count": 0
  },
  "condition_results": [
    {
      "id": "tie_has_12_print_in_place_components",
      "category": "separation_correctness",
      "severity": "major",
      "check": "assembly_component_count",
      "status": "pass",
      "description": "The printed pose should preserve 10 links plus tip plus clip as 12 separate components.",
      "detail": "assembly connected components 12 == expected 12",
      "measurements": {
        "actual_components": 12,
        "expected_components": 12
      }
    },
    {
      "id": "hinge_pin_bore_clearance_matches_readme",
      "category": "fit_clearance",
      "severity": "major",
      "check": "cylindrical_fit",
      "status": "pass",
      "description": "Hinge bore diameter should exceed pin diameter by about 0.6 mm total, matching 0.3 mm radial tolerance.",
      "detail": "diameter clearance 0.6000 mm",
      "measurements": {
        "pin_diameter_mm": 3.0,
        "hole_diameter_mm": 3.6,
        "diameter_clearance_mm": 0.6,
        "min_diameter_clearance_mm": 0.5,
        "max_diameter_clearance_mm": 0.8
      }
    }
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/.eval_tmp/../Articulated_tie.stl",
    "loadable": true,
    "hard_failures": [],
    "metrics": {
      "triangle_count": 7886,
      "bbox_min": [
        -35.0,
        -459.305,
        -1.5311
      ],
      "bbox_max": [
        35.0,
        -341.9232,
        88.7942
      ],
      "bbox_size": [
        70.0,
        117.3818,
        90.3253
      ],
      "bbox_volume_mm3": 742178.0968,
      "surface_area_mm2": 54913.1551,
      "volume_mm3": 117294.6001,
      "connected_components": 12,
      "watertight": true,
      "bad_edge_count": 0
    }
  },
  "parts": [],
  "part_names": [],
  "assumptions": {
    "physical_correctness": {
      "critical_failure_cap": 4.0,
      "major_failure_penalty": 2.0,
      "minor_failure_penalty": 0.8,
      "inconclusive_penalty": 0.3,
      "max_sample_points": 500,
      "max_sample_triangles": 1000,
      "collision_epsilon_mm": 1e-06,
      "collision_probe_distance_mm": 0.1
    },
    "layer1_geometry": {
      "component_count_mismatch_penalty": 1.0,
      "missing_parts_component_penalty": 0.5,
      "max_missing_parts_component_penalty": 2.0
    }
  }
}
{
  "score": 6.0,
  "class": "hard",
  "hard_failures": [],
  "risk_factors": [
    "assembly has 9 disconnected components but no separate part STLs were supplied",
    "major condition failed: planter_set_has_six_primary_printed_parts"
  ],
  "metrics": {
    "part_count": 0
  },
  "condition_results": [
    {
      "id": "planter_set_has_six_primary_printed_parts",
      "category": "part_inventory",
      "severity": "major",
      "check": "assembly_component_count",
      "status": "fail",
      "description": "The set should contain six primary printable parts; additional components indicate unwelded residual/scaffold fragments in exported STL.",
      "detail": "assembly connected components 9 != expected 6",
      "measurements": {
        "actual_components": 9,
        "expected_components": 6
      }
    }
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/aspire-planter/.eval_tmp/../aspire-planter.stl",
    "loadable": true,
    "hard_failures": [],
    "metrics": {
      "triangle_count": 687056,
      "bbox_min": [
        -107.1963,
        -56.7976,
        -86.0018
      ],
      "bbox_max": [
        107.1962,
        547.1852,
        132.4
      ],
      "bbox_size": [
        214.3925,
        603.9828,
        218.4018
      ],
      "bbox_volume_mm3": 28280724.2608,
      "surface_area_mm2": 930130.7119,
      "volume_mm3": 1628587.0563,
      "connected_components": 9,
      "watertight": true,
      "bad_edge_count": 0
    }
  },
  "parts": [],
  "part_names": [],
  "assumptions": {
    "physical_correctness": {
      "critical_failure_cap": 4.0,
      "major_failure_penalty": 2.0,
      "minor_failure_penalty": 0.8,
      "inconclusive_penalty": 0.3,
      "max_sample_points": 500,
      "max_sample_triangles": 1000,
      "collision_epsilon_mm": 1e-06,
      "collision_probe_distance_mm": 0.1
    },
    "layer1_geometry": {
      "component_count_mismatch_penalty": 1.0,
      "missing_parts_component_penalty": 0.5,
      "max_missing_parts_component_penalty": 2.0
    }
  }
}
{
  "score": 6.0,
  "class": "hard",
  "hard_failures": [],
  "risk_factors": [
    "assembly has 10 disconnected components but no separate part STLs were supplied",
    "major condition failed: hadley_kit_has_12_printed_part_instances"
  ],
  "metrics": {
    "part_count": 0
  },
  "condition_results": [
    {
      "id": "hadley_kit_has_12_printed_part_instances",
      "category": "part_inventory",
      "severity": "major",
      "check": "assembly_component_count",
      "status": "fail",
      "description": "The STL should preserve the 12 intended printed hardware solids/instances.",
      "detail": "assembly connected components 10 != expected 12",
      "measurements": {
        "actual_components": 10,
        "expected_components": 12
      }
    }
  ],
  "assembly": {
    "path": "outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/.eval_tmp/../HadleySource.stl",
    "loadable": true,
    "hard_failures": [],
    "metrics": {
      "triangle_count": 246741,
      "bbox_min": [
        -146.7626,
        -124.8365,
        -56.0
      ],
      "bbox_max": [
        117.5882,
        140.7606,
        1217.2625
      ],
      "bbox_size": [
        264.3508,
        265.5972,
        1273.2625
      ],
      "bbox_volume_mm3": 89396795.18,
      "surface_area_mm2": 420965.9815,
      "volume_mm3": 896506.3102,
      "connected_components": 10,
      "watertight": false,
      "bad_edge_count": 2109
    }
  },
  "parts": [],
  "part_names": [],
  "assumptions": {
    "physical_correctness": {
      "critical_failure_cap": 4.0,
      "major_failure_penalty": 2.0,
      "minor_failure_penalty": 0.8,
      "inconclusive_penalty": 0.3,
      "max_sample_points": 500,
      "max_sample_triangles": 1000,
      "collision_epsilon_mm": 1e-06,
      "collision_probe_distance_mm": 0.1
    },
    "layer1_geometry": {
      "component_count_mismatch_penalty": 1.0,
      "missing_parts_component_penalty": 0.5,
      "max_missing_parts_component_penalty": 2.0
    }
  }
}

codex
Physical scoring is complete. I’m folding the raw printability/physical outputs, manifest intent, render observations, feature-retention assessment, and missing-helper notes into the four final `evaluation_report.json` files, then I’ll delete `.eval_tmp`.
exec
/bin/zsh -lc "python3 - <<'PY'
import json, shutil
from pathlib import Path
root=Path('outputs/reconstructions/step-to-cadquery')
feature = {
'articulated-snowman-fidget': {
 'score':8.6,'class':'good',
 'evidence':['spec.md','params.py','main.py','work/rebuilt_iso_front.png','work/rebuilt_ortho_TOP_+Z.png','Articulated Snowman Fidget.stl'],
 'feature_results':[
  {'feature':'three example snowman configurations','expected':'scarf+hat/face, collar+hat/face, and bare variant laid out side by side','observed':'renders and main.py show three groups at X offsets with matching accessory differences','status':'preserved','severity':'major'},
  {'feature':'stacked hollow ball segments','expected':'bottom, chest, head with internal cord/socket geometry','observed':'spherical stacked segments and documented residual socket/funnel interiors are present','status':'preserved','severity':'major'},
  {'feature':'decorative pegs and hats/accessories','expected':'nose, eyes, buttons, hats, scarf/collar as source examples','observed':'front render shows face/button holes/pegs and hats; scarf/collar are frozen residual accessories','status':'preserved','severity':'major'},
  {'feature':'fully live internal socket and tiny accessory surfaces','expected':'ideal parametric reconstruction of small compound/freeform surfaces','observed':'many internal/socket/accessory details are frozen residuals rather than live parametric code','status':'altered','severity':'minor'}],
 'missing_features':[], 'altered_features':['internal ball socket/funnel and tiny accessory cap surfaces are residual-heavy, though geometry is present'], 'hallucinated_features':[], 'uncertain_features':['fresh renders could not be generated in this sandbox; existing renders were used']
},
'articulated-tie-step-file-included': {
 'score':8.7,'class':'good',
 'evidence':['spec.md','params.py','main.py','work/rebuilt_full_iso_front.png','work/rebuilt_full_ortho_TOP_+Z.png','Articulated_tie.stl'],
 'feature_results':[
  {'feature':'10 tapered articulated links plus tip and clip','expected':'12 components in compact print pose','observed':'assembly component count is 12 and code constructs 10 links, tip, and frozen clip','status':'preserved','severity':'major'},
  {'feature':'live hinge pose chain','expected':'measured fold angles and straightenable chain parameters','observed':'chain_poses() and params expose fold angles; component count and render match packed chain','status':'preserved','severity':'major'},
  {'feature':'0.3 mm radial hinge tolerance','expected':'pin/bore clearance for print-in-place articulation','observed':'cylindrical_fit reports 0.6 mm diameter clearance from params','status':'preserved','severity':'major'},
  {'feature':'clip detailed shape','expected':'shirt spring clip at neck end','observed':'clip is present as frozen import, but not live parametric','status':'altered','severity':'minor'}],
 'missing_features':[], 'altered_features':['hinge knuckle micro-geometry and clip shape are residual/frozen-heavy'], 'hallucinated_features':[], 'uncertain_features':['actual hinge swing not simulated']
},
'aspire-planter': {
 'score':8.2,'class':'good',
 'evidence':['spec.md','params.py','main.py','renders/rb_p0_iso_front.png','renders/rb_s1_iso_front.png','aspire-planter.stl'],
 'feature_results':[
  {'feature':'three pot sizes and three saucers','expected':'small/medium/large fluted pots with corresponding saucers','observed':'params/main define six solids in a row; renders show reeded pot and saucer family','status':'preserved','severity':'major'},
  {'feature':'reeded/fluted wall and open rim','expected':'many vertical reeds, cylindrical cavity, top rim chamfer','observed':'render shows dense vertical flutes and open top; params expose flute counts/radii/walls','status':'preserved','severity':'major'},
  {'feature':'drainage, base, tabs, and JP logo details','expected':'pot drain holes and saucer tabs/logo/grid details','observed':'details are preserved in frozen residuals, not live parametric; component count indicates residual/scaffold fragments in STL','status':'altered','severity':'major'}],
 'missing_features':[], 'altered_features':['bases/floors/drainage/logo are frozen residuals; exported STL has 9 connected components instead of 6 primary printed parts'], 'hallucinated_features':[], 'uncertain_features':['pot-in-saucer use fit was not measured because source is a laid-out set']
},
'astronomical-telescope-hadley-an-easy-assembly-hig': {
 'score':8.0,'class':'good',
 'evidence':['spec.md','params.py','main.py','renders/rebuilt_iso_front.png','renders/rebuilt_ortho_TOP_+Z.png','HadleySource.stl'],
 'feature_results':[
  {'feature':'Hadley printed hardware kit inventory','expected':'UTA/LTA rings, hub, mount, spider, collimation ring, baffle, clamps, cell, brackets','observed':'spec/main enumerate all intended hardware; render shows multi-part kit layout','status':'preserved','severity':'major'},
  {'feature':'visible detailed hardware features','expected':'bosses, holes, lugs, logos, threaded/focuser/mirror-related hardware','observed':'many features are preserved by frozen residual/import geometry, but only three canonicals have partial live scaffolds','status':'preserved','severity':'major'},
  {'feature':'printable separate part inventory','expected':'12 printed part instances','observed':'STL scorer sees 10 connected components and severe open/non-manifold boundaries','status':'altered','severity':'major'}],
 'missing_features':[], 'altered_features':['only three canonicals are live partial scaffolds; most feature detail is frozen; STL topology/component count is degraded'], 'hallucinated_features':[], 'uncertain_features':['external telescope hardware fit is unmeasured']
}}
summary_notes={
'articulated-snowman-fidget':'No evaluator code change. Existing helper set handled component count and parametric cylindrical fits; placed-instance/path extraction remains the limiting gap.',
'articulated-tie-step-file-included':'No evaluator code change. Hinge clearance can be checked parametrically, but full joint-aware articulation remains outside current helpers.',
'aspire-planter':'No evaluator code change. Prior vent/opening proxy helpers already exist; this batch needs coordinates/placed component meshes, not a new scoring primitive.',
'astronomical-telescope-hadley-an-easy-assembly-hig':'No evaluator code change. Main unresolved checks require external hardware proxies and placed-instance extraction.'
}
for name, fr in feature.items():
    project=root/name
    tmp=project/'.eval_tmp'
    pr=json.loads((tmp/'printability_report.json').read_text())
    phy=json.loads((tmp/'physical_correctness_report.json').read_text())
    manifest=json.loads((tmp/'physical_conditions.json').read_text())
    report={
      'project': str(project),
      'evaluation_iteration': 9,
      'scores': {
        'printability': {'score': pr['score'], 'class': pr['class']},
        'physical_correctness': {'score': phy['score'], 'class': phy['class']},
        'feature_retention': {'score': fr['score'], 'class': fr['class']}
      },
      'evidence': {
        'sources_inspected': ['spec.md','params.py','main.py','export_step.py','export_stl.py','validation.py','assembled STL','assembled STEP','existing reconstruction renders'],
        'visual_context': manifest['design_intent_inventory']['visual_context'],
        'render_limitation': 'Fresh render_views.py failed because cadquery is not installed in the active Python; uv could not fetch cadquery because network access is disabled and its default home cache is outside sandbox. Existing project render PNGs were inspected instead.'
      },
      'printability': pr,
      'physical_correctness': {
        **phy,
        'design_intent_inventory': manifest['design_intent_inventory'],
        'interaction_matrix_summary': manifest['interaction_matrix_summary'],
        'manifest_conditions': manifest['conditions'],
        'missing_helper_notes': manifest['missing_helper_notes']
      },
      'feature_retention': fr,
      'evaluator_improvement': {
        'implemented_code_change': False,
        'implemented_helper': None,
        'reason': summary_notes[name],
        'before_after': {'before': {'printability': pr['score'], 'physical_correctness': phy['score'], 'feature_retention': fr['score']}, 'after': {'printability': pr['score'], 'physical_correctness': phy['score'], 'feature_retention': fr['score']}}
      },
      'residual_risks': manifest['missing_helper_notes'] + [{'relationship':'fresh visual render generation','triage':'environment_blocked','reason':'cadquery unavailable in sandboxed Python; existing renders mitigate but do not replace a fresh render pass.'}]
    }
    (project/'evaluation_report.json').write_text(json.dumps(report, indent=2))
PY
for p in articulated-snowman-fidget articulated-tie-step-file-included aspire-planter astronomical-telescope-hadley-an-easy-assembly-hig; do rm -rf outputs/reconstructions/step-to-cadquery/"'$p/.eval_tmp; done
python3 - <<'"'PY'
import json
from pathlib import Path
for p in ['articulated-snowman-fidget','articulated-tie-step-file-included','aspire-planter','astronomical-telescope-hadley-an-easy-assembly-hig']:
 r=Path('outputs/reconstructions/step-to-cadquery')/p/'evaluation_report.json'
 data=json.loads(r.read_text())
 print(p, data['scores'])
PY" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
articulated-snowman-fidget {'printability': {'score': 5.5, 'class': 'hard'}, 'physical_correctness': {'score': 6.0, 'class': 'hard'}, 'feature_retention': {'score': 8.6, 'class': 'good'}}
articulated-tie-step-file-included {'printability': {'score': 5.5, 'class': 'hard'}, 'physical_correctness': {'score': 8.0, 'class': 'easy'}, 'feature_retention': {'score': 8.7, 'class': 'good'}}
aspire-planter {'printability': {'score': 6.7, 'class': 'hard'}, 'physical_correctness': {'score': 6.0, 'class': 'hard'}, 'feature_retention': {'score': 8.2, 'class': 'good'}}
astronomical-telescope-hadley-an-easy-assembly-hig {'printability': {'score': 4.0, 'class': 'hard'}, 'physical_correctness': {'score': 6.0, 'class': 'hard'}, 'feature_retention': {'score': 8.0, 'class': 'good'}}

codex
The four final reports are written and the temporary `.eval_tmp` directories have been removed. I’m doing a final artifact check now to confirm only the intended project reports remain from the evaluation run.
exec
/bin/zsh -lc 'for p in articulated-snowman-fidget articulated-tie-step-file-included aspire-planter astronomical-telescope-hadley-an-easy-assembly-hig; do echo "$p"; find outputs/reconstructions/step-to-cadquery/$p -maxdepth 1 -name '"'.eval_tmp' -o -name 'evaluation_report.json'; done" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
articulated-snowman-fidget
outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/evaluation_report.json
articulated-tie-step-file-included
outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/evaluation_report.json
aspire-planter
outputs/reconstructions/step-to-cadquery/aspire-planter/evaluation_report.json
astronomical-telescope-hadley-an-easy-assembly-hig
outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/evaluation_report.json

exec
/bin/zsh -lc 'git status --short -- outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included outputs/reconstructions/step-to-cadquery/aspire-planter outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig skills/evaluate-cad-reconstruction' in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
 M skills/evaluate-cad-reconstruction/SKILL.md
 M skills/evaluate-cad-reconstruction/cad_reconstruction_eval/physical_correctness_score.py
 M skills/evaluate-cad-reconstruction/references/physical-condition-manifests.md
 M skills/evaluate-cad-reconstruction/references/scoring-guide.md
 M skills/evaluate-cad-reconstruction/tests/test_physical_correctness_score.py
?? skills/evaluate-cad-reconstruction/loop_runs/
?? skills/evaluate-cad-reconstruction/scripts/aggregate_evaluation_reports.py
?? skills/evaluate-cad-reconstruction/scripts/run_codex_improvement_loop.py

exec
/bin/zsh -lc "python3 - <<'PY'
import json
from pathlib import Path
for p in ['articulated-snowman-fidget','articulated-tie-step-file-included','aspire-planter','astronomical-telescope-hadley-an-easy-assembly-hig']:
 r=Path('outputs/reconstructions/step-to-cadquery')/p/'evaluation_report.json'
 data=json.loads(r.read_text())
 print(p)
 print(' scores', data['scores'])
 print(' failed checks', [c['id'] for c in data['physical_correctness']['condition_results'] if c['status']=='fail'])
 print(' missing', [m['relationship'] for m in data['physical_correctness']['missing_helper_notes']])
PY" in /Users/macbookpro/Documents/ai-chain/_hardware/cad-skill-test-2
 succeeded in 0ms:
articulated-snowman-fidget
 scores {'printability': {'score': 5.5, 'class': 'hard'}, 'physical_correctness': {'score': 6.0, 'class': 'hard'}, 'feature_retention': {'score': 8.6, 'class': 'good'}}
 failed checks ['snowman_layout_has_expected_placed_solids']
 missing ['elastic cord through bottom/chest/head bores', 'ball socket seating and articulation']
articulated-tie-step-file-included
 scores {'printability': {'score': 5.5, 'class': 'hard'}, 'physical_correctness': {'score': 8.0, 'class': 'easy'}, 'feature_retention': {'score': 8.7, 'class': 'good'}}
 failed checks []
 missing ['full hinge rotation path for each seated joint', 'clip spring compliance and shirt retention']
aspire-planter
 scores {'printability': {'score': 6.7, 'class': 'hard'}, 'physical_correctness': {'score': 6.0, 'class': 'hard'}, 'feature_retention': {'score': 8.2, 'class': 'good'}}
 failed checks ['planter_set_has_six_primary_printed_parts']
 missing ['pot drains and saucer drain grid open-area preservation', 'pot-in-saucer seating/support']
astronomical-telescope-hadley-an-easy-assembly-hig
 scores {'printability': {'score': 4.0, 'class': 'hard'}, 'physical_correctness': {'score': 6.0, 'class': 'hard'}, 'feature_retention': {'score': 8.0, 'class': 'good'}}
 failed checks ['hadley_kit_has_12_printed_part_instances']
 missing ['tube, truss pole, mirror, focuser, screw and clamp fit', 'thread engagement and load-bearing stiffness']

codex
Completed iteration 9 batch evaluation.

Final reports written:
- `outputs/reconstructions/step-to-cadquery/articulated-snowman-fidget/evaluation_report.json`
- `outputs/reconstructions/step-to-cadquery/articulated-tie-step-file-included/evaluation_report.json`
- `outputs/reconstructions/step-to-cadquery/aspire-planter/evaluation_report.json`
- `outputs/reconstructions/step-to-cadquery/astronomical-telescope-hadley-an-easy-assembly-hig/evaluation_report.json`

Temporary `.eval_tmp` directories were removed. Only the final reports remain as evaluation artifacts.

Scores:

| Project | Printability | Physical | Feature |
|---|---:|---:|---:|
| `articulated-snowman-fidget` | 5.5 | 6.0 | 8.6 |
| `articulated-tie-step-file-included` | 5.5 | 8.0 | 8.7 |
| `aspire-planter` | 6.7 | 6.0 | 8.2 |
| `astronomical-telescope-hadley-an-easy-assembly-hig` | 4.0 | 6.0 | 8.0 |

No evaluator helper/code change was made, so before/after scores are unchanged. The prior loop’s vent/grille candidate already appears implemented in this checkout (`vent_opening_proxy` and `vent_grid_open_area_proxy` are present and documented). This batch instead exposed a broader placed-instance/component-extraction workflow gap, which is too large for a narrow helper iteration.

Helper usage in this batch:
- Used: `assembly_component_count`, `cylindrical_fit`
- Failed checks: snowman expected 21 components but STL reports 23; Aspire expected 6 primary parts but STL reports 9; Hadley expected 12 part instances but STL reports 10.
- Tie passed both component count and hinge cylindrical clearance.

Remaining missing-helper themes:
- placed-instance/component STL extraction for detailed contact/clearance checks;
- joint-aware hinge/ball articulation paths;
- elastic cord/flexible clip/material compliance;
- external hardware proxies for telescope tube, truss poles, mirror, focuser, screws;
- use-pose support/seating for planter pot-in-saucer checks.

Render note: fresh `render_views.py` could not run because `cadquery` is unavailable in the active Python and network is disabled for `uv`; I used the existing project render PNGs as visual context and recorded that limitation in each report.
tokens used
172,169
