# PAIN POINTS — Pipeline Execution Log

### Turn 11

**Ideator:**
- The `differentiation` field verification step for named comparables (e.g., checking Marjapussi's actual trump-declaration rule, or the pottery/blacksmith theme's existing shipped titles) required multiple follow-up searches beyond the single mechanism-phrase query the `prior_art_search.sh` script generates — the script's printed queries are a good starting checklist but for mechanics with a real historical precedent (e.g., dynamic trump in card games) I still had to do an extra targeted search to characterize *how* the precedent's rule works before I could write an honest, specific differentiation claim rather than just confirming absence/presence.
- No other significant friction this turn; the schema and heuristics were clear enough to apply directly.

**Evaluator:**
- **No directory-listing tool available, and the exact `manifest.json` path had to be reverse-engineered from source, not documented in my own instructions.** My tool set this turn is Read/Write/Edit/WebSearch/WebFetch only — no Bash/Glob/LS — and `Read` errors with `EISDIR` on a directory with no listing fallback. My own instructions say to read `board-game/history/turn-<N>/cad-builds/<idea-slug>/manifest.json` but never define `<idea-slug>`'s exact format. I had to guess several plausible slugs (`cargo-hold`, `idea-6`, `6-cargo-hold`, etc.), all of which failed, before reading `board-game/tools/generate_cad_builds.py`'s `_slugify()` function and discovering the real, undocumented format is `idea-{id:02d}-{slugify(title)}` (e.g. `idea-06-cargo-hold`). This should either be stated explicitly in my own agent file (`.claude/agents/board-game-evaluator.md`), or I should be given a directory-listing capability so I'm not dependent on knowing the generator script's internal naming convention by chance.
- **No manifest.json or build directory exists at all for a parked (`awaiting_questions`) pick** — `generate_cad_builds.py` returns early before ever creating `out_dir` for that outcome, so "the file is missing" (per my instructions' first bullet under "CAD Reality Check") is the *only* signal available for a park; there is no `status` field to read at all, and no way for me to see what clarifying question the job actually asked. My instructions say to "state the `status` and, if present, the `error`/park reason in your notes," but for a park there is no artifact anywhere containing that reason — I could only report "parked (awaiting_questions), no manifest/build artifacts produced" based on the orchestrator's own summary, not anything I could independently verify by reading a file. If diagnosing *why* a build parked is meant to be part of my job, I'd need either the job's raw API response or the orchestrator to persist it somewhere I can read.
- Minor: the rubric's SCORES.md template table for "Built ideas — Total /100" doesn't have a column for build status, and the CAD Reality Check table format says to show status/error "in place of the score columns" for non-done builds — but the harsh 0/40 and 0/10 gate elsewhere implies numeric scores should still appear in the *Total* table. I resolved this by adding an extra "Build status" column to the Total table and keeping the status-in-place-of-scores behavior only in the detailed CAD Reality Check table, but this required interpretation rather than being spelled out.

**Triage:**
- [AUTO-FIXED] No documented manifest.json slug format, no directory-listing tool — `.claude/agents/board-game-evaluator.md` now spells out the exact `idea-{id:02d}-{slugified-title}` format with a worked example, and the evaluator's tool set gained `Glob` so it can search for the real path instead of guessing.
- [AUTO-FIXED] Parked builds have no manifest.json and no recorded park reason anywhere — `.claude/agents/board-game-evaluator.md` now states explicitly that a missing build directory *is* the complete signal for a park, with the exact note-wording to use, so the evaluator stops searching for a reason file that will never exist.
- [AUTO-FIXED] SCORES.md Total table lacked a build-status column, forcing the evaluator to improvise a format — `.claude/agents/board-game-evaluator.md`'s template now includes a "Build status" column in the Total table (matching what the evaluator already did this turn) with an explicit note that only the separate CAD Reality Check detail table swaps status/error in place of score columns.
- [NOT-ACTIONABLE] Ideator's extra follow-up searches needed to characterize *how* a named precedent's rule actually works (beyond `prior_art_search.sh`'s single generated query) — this is inherent to writing an honest, specific differentiation claim once prior art is found, not a fixable tooling gap; `prior_art_search.sh` is meant as a starting checklist, not a complete verification pipeline.

**Ideator (revise pass):**
- none

### Turn 12

**Ideator:**
- The `Write` tool's "must `Read` an existing file before overwriting it" requirement is in direct tension with generate mode's explicit instruction not to read `board-game/IDEAS.json` — I had to do a token `Read` (just the first 5 lines, unused) purely to satisfy the tool mechanically. Worth clarifying in the pipeline instructions whether a metadata-only stat/peek is the intended workaround, or whether generate mode should just always use a fresh filename/overwrite mechanism that doesn't require reading prior content.
- No other friction this turn — `prior_art_search.sh`'s guidance (search the idea's own literal wording, check alternate modes of named comparables, verify supporting comparables too) worked well and caught three real near-collisions before they were finalized.

**Evaluator:**
- `IDEAS.json` for this turn is large enough (131 lines, ~28K tokens) that a single unpaginated `Read` triggered an automatic truncation/pagination warning partway through the file, silently cutting off ideas 9 and 10 plus `cad_build_picks` on the first read. The system reminder that flagged this was easy to catch and recover from with a follow-up `Read(offset=99)`, but it's worth noting explicitly: for a 10-idea batch with this much per-idea text (full `rules`/`cad_prompt` strings), one `Read` call is not reliably enough, and an evaluator that doesn't notice the truncation warning would silently score against incomplete data (missing exactly the ideas/picks needed for the CAD Reality Check section).
- The orchestrator's turn-start message this turn pre-supplied the build status (failed/parked/done) and even the photo-inspection finding for the one completed build, which was a helpful shortcut, but it meant the only way to independently confirm "no manifest/build directory for ideas 1 and 9" was a `Glob` across the whole `cad-builds/` tree rather than a targeted per-idea check — worth keeping in mind that `Glob` on the parent directory remains the right verification step even when the orchestrator's summary already states the outcome, since the rubric asks for independently-verified status, not just a restated claim.
- No other friction this turn — the `idea-{id:02d}-{slugified-title}` manifest path format documented in `.claude/agents/board-game-evaluator.md` since the Turn 11 pain-points triage worked exactly as specified on the first try.

**Triage:**
- [AUTO-FIXED] `Write` tool's Read-before-overwrite requirement was in tension with generate mode's "don't read prior turn content" hard rule — `.claude/agents/board-game-ideator.md` now states explicitly that a token/metadata-only `Read` purely to satisfy the tool precondition doesn't violate the rule, since the rule is about not *using* prior content, not about the mechanical tool call.
- [AUTO-FIXED] `IDEAS.json` can silently truncate on a single unpaginated `Read`, dropping the last ideas/`cad_build_picks` from scoring — `.claude/agents/board-game-evaluator.md` now explicitly warns about this and instructs following up with additional `Read` calls at the right offset when a truncation warning appears.
- [NOT-ACTIONABLE] Evaluator independently re-verifying build status via `Glob` even when the orchestrator's turn-start message already stated it — this is correct behavior (independent verification, not a restated claim), not a defect to fix.

**Ideator (revise pass):**
- BOARD.md's Turn 12 entry names the CAD-pipeline park/timeout failure as diagnostically invisible ("no manifest, no error, no clarifying-question text survives") for the third turn running — this is a pipeline gap in the evaluator/build harness, not something the ideator can fix through better prompts, and it's now clearly the dominant score-limiting factor (worth flagging again for `/goal`'s pain-point triage, since two consecutive revise passes have had to write around it rather than fix it).
- No other friction this pass — BOARD.md's Turn 12 section was clear and specific enough to act on directly.

### Turn 13

**Ideator:**
- none

**Evaluator:**
- `IDEAS.json` this turn (131 lines) again triggered the automatic truncation warning partway through a single unpaginated `Read` (cut off after idea 9's `prompt` field, before its `cad_prompt`, idea 10, and `cad_build_picks`) — the same failure Turn 12's pain points already flagged and my own instructions now explicitly warn about. The documented recovery (follow-up `Read` at the right `offset`) worked correctly on the first try, so this isn't a new gap, just a confirmation the existing warning/instruction is necessary and is being hit essentially every turn now that batches run ~130 lines; worth considering whether the ideator could be asked to keep `IDEAS.json` under the single-`Read` threshold, or whether the evaluator's instructions should just assume a second `Read` call is always required rather than framing it as a contingency.
- The rubric asks me to "cross-check `cad_prompt` against `components`" as part of Producibility for built ideas, but for the two parked picks (Cipher Row, Threshing Floor) there's no way to know whether their `cad_prompt`s were actually the proximate cause of the park — the park could be anything from an ambiguous dimension to a totally unrelated pipeline hiccup, and I have zero artifact to distinguish those. I ended up not attempting a `cad_prompt`-quality diagnosis for the parked picks beyond noting the hard-gate score, since anything more would be speculation dressed as analysis — worth confirming this is the intended scope (score the gate, don't speculate about cause) rather than something I should be doing more of.
- Determining the CAD-build fidelity score for Foghorn required cross-referencing four separate sources (the orchestrator's turn-start message, `photo.jpg`, `assembled.png`, and `qa.png`) to build confidence in what had and hadn't fused/dropped — this worked fine here because the orchestrator's message pre-flagged the two key defects, but for a turn without that pre-flagging I'd want to know whether checking all three image artifacts (not just `photo_file` as the rubric names) is expected as standard practice, since `qa.png`'s multi-angle orthographic views were what let me count the visible part clusters precisely enough to notice the missing trick collection tray.

**Triage:**
- [AUTO-FIXED] `IDEAS.json` truncates on a single `Read` essentially every turn now (3 for 3) — `.claude/agents/board-game-evaluator.md` now frames a second `Read` at the right offset as the normal expected way to read this file, not a contingency to watch for.
- [AUTO-FIXED] Unclear whether the evaluator should attempt to diagnose a parked pick's `cad_prompt` as the park's proximate cause — `.claude/agents/board-game-evaluator.md` now states explicitly not to, since no artifact exists to support that claim; score the hard gate and stop.
- [AUTO-FIXED] Unclear whether checking `assembled.png`/`qa.png` alongside `photo_file` is standard practice or only for orchestrator-flagged turns — `.claude/agents/board-game-evaluator.md` now states explicitly to view all three on every built idea as standard practice, calling out why `qa.png`'s orthographic views matter for counting parts.
- [ASKED-DECLINED] Recurring (3-turn) pattern of parked/failed CAD builds leaving zero diagnostic artifact (no manifest, no error, no park reason) was raised as a controversial pipeline-architecture fix (persisting the raw job/park response in `generate_cad_builds.py`) via `AskUserQuestion`. User chose to investigate `generate_cad_builds.py`/the job API themselves rather than have this session make the change — no code was touched.

### Turn 14

**Ideator:**
- The `must_survive[].geometric` schema example only shows `check`/`category`/`severity`/`thresholds`, but several check types (`part_contact`, `part_clearance`, `axis_alignment`, `cylindrical_fit`) clearly need to reference specific named bodies to be meaningful. Added an ad-hoc `"parts": [...]` key alongside `thresholds` for those entries since the spec doesn't define where part names belong — worth clarifying in the schema whether `parts` is the expected convention or if part names should live inside `thresholds` instead.
- No mechanism in the instructions to verify a `differentiation_path: twist` idea's "genuine rule-level change" bar other than self-assessment — resolved ambiguity by making the twist change both board topology (stacked layers) and win condition (single-player puzzle to two-player adversarial), but there's no checklist for how much change counts as "genuine" versus a minor variant.

**CAD-writer:**
- The agent wrote its trailing `PAIN_POINTS:` text directly into `board-game/CAD_PROMPTS.json` after the closing `}`, making the file invalid JSON (caught and manually truncated back to valid JSON by the orchestrator before back-translation/build could run). This would have silently broken every downstream consumer of the file. The agent's own instructions apparently don't make clear that `PAIN_POINTS:` belongs only in the response text, never appended inside the JSON artifact itself.
- `CAD_QUESTIONS.md` carries no recorded questions yet (this loop's build-history tracking was only just rebuilt), so there were no recurring question categories to specifically pre-answer beyond the standing Learned Heuristics.
- Idea 2 (Twin Deck Solitaire): spec gives internally inconsistent peg diameters — 10mm in `components`/`art_direction.scale` vs. 11.4mm in the `must_survive` rank-5 fit-check thresholds against a 12mm hole. Followed the fit-check's numbers since those are what get mechanically validated.
- Idea 2 also gives posts as 60mm tall against a required 30mm air gap between boards, with no stated insertion depth to reconcile the two numbers — inferred 15mm embedded per side. This kind of gap should be closed at the ideation stage with an explicit insertion-depth field.
- Idea 3 (Eclipse): disc diameter stated as 24mm (equal to the socket) in three spec fields, but the must_survive rank-4 clearance check requires the disc to be measurably smaller than the socket — shrunk disc to 23.4mm to satisfy the clearance check.
- Idea 1 (Keyhold): spec never states which of a tile's four edges are tabs vs. notches — assumed an alternating tab/notch/tab/notch pattern as the most functionally conservative reading; this is a gameplay-relevant gap (affects which rotations produce a legal mate) the ideator should settle explicitly, not just a CAD-prompt wording issue.

**CAD-pilot:**
- Idea 1 (Keyhold): design service returned `llm_error` ("couldn't produce a plan") immediately after concept selection, before reaching any question or plan-approval park. Fully-spec'd prompt (5/5 must_survive ranks covered), no signal beforehand that plan generation would fail — a pipeline-side failure, not a park requiring a pilot decision.
- Idea 3 (Eclipse): concept-phase worker failed outright ~35 minutes into round-2 style generation with `worker_error`: "Claude's response exceeded the 32000 output token maximum." Zero recoverable artifacts. A pipeline capacity/config limitation (`CLAUDE_CODE_MAX_OUTPUT_TOKENS`), not a prompt or spec issue.
- Idea 2 (Twin Deck Solitaire) reached `done` and its geometry was independently verified faithful (30.0mm air gap, matching cross_positions() on both boards, PEG_D=11.4 vs HOLE_D=12.0), but `score_build.py`'s `compile_conditions()` (lines ~88-129) only ever reads `geometric.get("inputs")` from `IDEAS.json` to populate the check functions' input dict — this turn's `IDEAS.json` geometric blocks use `"parts"` (a list) and `"thresholds"` instead, never `"inputs"`, so `part_clearance`/`opening_presence`/`axis_alignment`/`cylindrical_fit` are structurally unable to resolve regardless of `bindings.json` correctness. This is systemic across this turn's schema, not specific to one idea — 4 of Idea 2's 5 must_survive ranks came back "inconclusive" purely from this wiring gap, not from any geometry defect.
- Idea 2 also found a real (unscored) spec deviation: the cad_prompt asked for blind holes (15mm deep) at the post insertion points, but the CAD agent silently built through-bores instead, so insertion pins poke 3mm past each board's outer face.

**Evaluator:**
- `score_build.py`'s `compile_conditions()`/`inputs`-key mismatch (documented above under CAD-pilot) meant `geometric_fidelity` for idea 2 had to be taken as-reported (0.6) while the actual verification work — confirming the 4 "inconclusive" ranks were really satisfied — had to happen by hand-reading `project/main.py` and cross-checking against `must_survive` thresholds, since no `evaluation_report.json` field distinguishes "inconclusive because the tool is broken" from "inconclusive because the geometry is ambiguous." A `detail` string ("part_clearance requires part_a and part_b") is enough to diagnose the former by inspection, but it would be faster and more auditable if `evaluation_report.json` carried a machine-readable `reason: "missing_inputs"` vs `reason: "ambiguous_geometry"` distinction so the evaluator doesn't have to re-derive that from the error string.
- No `renders/` directory content existed for idea 2 (`cadquery unusable on this host and no docker compose file found`) — the rubric's instruction to "look at the ortho renders first" had to fall back to `project/main_review/*.png` (per-part QA renders) and `assembled.png`/`qa.png` instead, which worked fine here because they happened to include labeled ortho views (top/front/back/left/right) and cross-section cuts, but the rubric doesn't name `main_review/` as a fallback location — worth adding explicitly, since `renders/` being empty is apparently a known host-config issue, not a one-off.
- For ideas 1 and 3 (pre-build pipeline failures), the rubric's Fidelity/Reliability/Ambition/Total structure has no explicit guidance for "zero build artifacts exist at all" — I inferred Fidelity=0 and Reliability=0 (both technically unmeasurable, treated as zero rather than null-and-excluded) and excluded both from `average_total`/`average_first_shot_survival` by analogy to Turn 13's precedent (parked picks with no manifest were dropped from the averaged subset even though the rubric text at the time didn't say to). Worth confirming this reading is the intended one, or adding an explicit rule for "job failed before any build artifact existed" as distinct from "job parked/failed after producing a manifest."

**User-identified (post-turn review of Twin Deck Solitaire):**
- **The built object is physically unplayable, and every layer of the pipeline certified it.** Pegs are 20mm tall seated in 8mm-deep holes, so 12mm protrudes into a 30mm inter-board gap, leaving 18mm; freeing a peg costs 8mm of lift, leaving 10mm for fingers that need 50-70mm to pinch and lift. Off by 3-4x. Separately, both boards are identical 180x180mm footprints in exact vertical alignment, so the lower deck is 100% occluded from above and visible only edge-on through a 30mm slot — in a game whose rules require playing on that deck. The critical failure is systemic, not a one-off: `must_survive` rank 1 (highest weight, severity `critical`) *encodes the 30mm gap as a pass condition* (thresholds 25-35mm). The build passed it, the evaluator confirmed it "numerically satisfied," the auditor verified the evaluator, and the idea scored the turn's best 80.68/100. **`must_survive` validates that the object matches the spec; nothing in the pipeline validates that the spec describes a usable object.** A perfectly-executed build of an unusable design scores at the top of the board.
- **Vision Ambition rewarded the same flaw.** Ambition was judged 12/15 from the vision render — which shows the occlusion plainly — and cited "a genuine two-story assembly with an air gap" as a strength. The ambition pass has no playability lens either.
- **No theme/character in the form.** The ideator filled its identity fields with nothing: `surface_treatment: "no other surface texture"`, `silhouette: "like a small equipment rack"`, and a `part_vocabulary` that is a bare parts list ("distinguished purely by which body they are"). This is not a consequence of the no-colour constraint — the ideator's own brief asks for identity in pure geometry and gives a rich worked example ("chunky chamfered slabs with deep chiselled channels, everything reading as carved stone"). The result reads as a test fixture rather than a product. Note for future triage: the orchestrator initially mis-attributed this to the colour ban when asked, i.e. rationalized the output instead of judging it — the colour ban removes one channel, it does not license shapeless design.

**Triage:**
- [AUTO-FIXED] Playability blind spot — `.claude/agents/board-game-ideator.md` gains a "hand gate" section (main body, alongside the colour gate): reach budget (seating depth + protrusion + ~60mm finger room) for any playable position under another layer, an occlusion rule for stacked footprints, a seated-player posture assumption, the Twin Deck numbers as a worked example, and an explicit statement that `must_survive` structurally cannot catch this so it must be gated at design time. Added as checklist item 6 and folded into `art_direction.scale`'s field description.
- [AUTO-FIXED] Empty identity fields — `.claude/agents/board-game-ideator.md` gains a "No colour is not no character" section naming the three specific non-answers observed this turn, plus tightened field descriptions for `silhouette`/`part_vocabulary`/`surface_treatment` ("none" explicitly invalid) and checklist item 7.
- [ASKED-APPROVED] Playability enforcement in the rubric — user chose the evaluator detection layer over a hard zeroing gate or ideation-gate-only. `.claude/agents/board-game-evaluator.md` score mode now runs a reach/sight playability check before computing Fidelity, zeroing the credit of any `must_survive` rank that certifies an unusable dimension (flat −10 if no rank certifies it), with the finding stated in `SCORES.md` and attributed to the ideator as a vision failure. Applied to Twin Deck retroactively this would move rank 1 to 0 credit, Fidelity 48.0 → ~32.0, Total 80.68 → ~64.7. Scores already written for turn 14 were NOT restated — the rule takes effect next turn, per the standing rule that triage edits apply to the next invocation.
- [ASKED-APPROVED] Character in Vision Ambition — user approved folding formal character into the existing Ambition axis rather than leaving it to the ideation gate or also raising the 8/15 floor. `.claude/agents/board-game-evaluator.md` ambition mode now names the three specific non-answers (`surface_treatment: "none"`, bare-parts-list `part_vocabulary`, hardware `silhouette`) and instructs capping such ideas in single digits, with an explicit note that specificity is not ambition.
- [AUTO-FIXED] Root cause of this turn's `compile_conditions()` scoring breakage: the ideator's own schema docs never stated *where* part/axis/dimension references belong inside a `geometric` block (`physical-condition-manifests.md` documents the exact `inputs` contract per check type, but the ideator's only worked example — `assembly_component_count` — doesn't need one, and nothing told it to read the reference before writing a check that does). `.claude/agents/board-game-ideator.md` now states the contract explicitly (`inputs.part_a`/`part_b` for part_clearance/part_contact/part_collision, `inputs.axis_a`/`axis_b` for axis_alignment, `inputs.pin_diameter_mm`/`hole_diameter_mm` for cylindrical_fit, etc.) and adds a second worked example (`part_clearance` with a populated `inputs` block) alongside the count-only one. No `score_build.py` change was needed — it was already correctly built for this contract; the gap was purely in what the ideator was told to write.
- [AUTO-FIXED] CAD-writer wrote its trailing `PAIN_POINTS:` section directly into `board-game/CAD_PROMPTS.json`, breaking JSON validity for every downstream consumer (back-translation, cad-pilot) — caught and manually repaired this turn before it could propagate. `.claude/agents/board-game-cad-writer.md` now states explicitly that `PAIN_POINTS:` belongs in reply text only, matching the "(outside the JSON)" clarity the ideator's own instructions already had (which is exactly why the ideator didn't make this mistake this turn).
- [AUTO-FIXED] Evaluator's rubric named only `renders/`/`qa.png`/`assembled.png` as visual-check sources, with no documented fallback for the known cadquery/docker-unavailable host issue that leaves `renders/` empty — `.claude/agents/board-game-evaluator.md` now names `project/main_review/*.png` as the explicit fallback.
- [DEFERRED] Recurring (2 of 3 ideas this turn) pattern of internally inconsistent numeric dimensions between prose fields (`components`/`art_direction.scale`) and `must_survive` threshold values — flagged by the CAD-writer and independently raised by the judged auditor's AMBER finding as not yet reflected in `CAD_GRAMMAR.md`. Real and recurring, but the 3-auto-fix cap was reached this turn; highest-priority carryover for next turn's triage (likely fix: require the ideator to single-source each physical dimension rather than restating it in both prose and thresholds).
- [DEFERRED] Ideator: no checklist for what counts as a `twist` idea's "genuine rule-level change" beyond self-assessment. Carryover.
- [DEFERRED] Idea 1 (Keyhold)'s spec never specified which tile edges are tabs vs. notches, a gameplay-relevant ambiguity the cad-writer had to guess at. Carryover — could bundle with the dimension-consistency fix above as one broader "no unresolved geometric ambiguity leaves `must_survive`-adjacent fields" pass.
- [DEFERRED] `evaluation_report.json` has no machine-readable distinction between "inconclusive because tooling is broken" and "inconclusive because geometry is ambiguous" — evaluator had to re-derive this from a free-text `detail` string. A `score_build.py` enhancement (adding a `reason` field), related to but distinct in scope from this turn's ideator-schema fix.
- [DEFERRED] No explicit rubric rule for "zero build artifacts exist at all" (pre-build pipeline failure) as distinct from "parked with a manifest" — evaluator inferred Fidelity=0/Reliability=0/excluded-from-average by analogy to Turn 13 precedent. Worth codifying explicitly in `board-game-evaluator.md`.
- [NOT-ACTIONABLE] CAD_QUESTIONS.md currently has no recorded history since this loop's tracking was only just rebuilt — self-resolving as builds accumulate real question data.
- [NOT-ACTIONABLE] Idea 2's CAD agent silently substituted through-bores for the prompt's specified blind holes — external design-service behavior, not controllable from this repo's agent/tool files; not covered by any `must_survive` rank so it didn't affect scoring.
- [NOT-ACTIONABLE] Judged auditor's AMBER finding that the canary control has never produced a `history/turn-*/builds/canary/` in 14 turns: the canary rule is new as of the loop revamp (commit `ab91436`), turns 1-13 predate it entirely, and turn 14 (14 mod 4 = 2) isn't a checkpoint turn. Not a violation — watch at turn 16, the first canary turn under the revamped loop.
- [NOT-ACTIONABLE] Two of three ideas failed this turn on pipeline/design-service errors upstream of any build artifact (idea 1: `llm_error` at plan-generation; idea 3: `worker_error`, 32000-output-token overflow at concept-phase generation) — both are external CAD-service failures, not something fixable from this repo's agent/tool/command files. If this pattern recurs across turns, it should be raised to the user as a controversial infra question (per Turn 13's precedent of a declined related fix), not auto-fixed.

### Turn 15

**Ideator:**
- The `must_survive[].geometric` schema example only shows `check`/`category`/`severity`/`thresholds`/`inputs` (per Turn 14's fix), but no reference doc yet shows a full worked example for `feature_count`'s threshold key name or for `opening_presence`'s threshold key(s)/inputs beyond "define a line segment." Inferred `thresholds.expected_count` for `feature_count` (by analogy with `assembly_component_count`'s `expected_components`) and `thresholds.min_clear_fraction` for `opening_presence` (by analogy with sibling `clear_path_proxy`/`vent_opening_proxy` helpers). Both are reasonable but unverified guesses — worth adding one canonical JSON example for each of these two checks to `physical-condition-manifests.md`.
- No other ambiguity encountered; the `new`/`twist`/`reskin` schema and the hand-gate arithmetic were straightforward to apply once dimensions were chosen up front.

**CAD-writer:**
- Idea 1 (Lockstep Canals): the spec never specifies which board corner hosts the draw bin, nor the spacing between a font-stone's 1-4 owner holes. Picked an unspecified corner (left unassigned in the prompt) and 6mm hole spacing as a placeholder, flagged in `unstated_in_spec` rather than silently invented.
- Idea 2 (Sluice Row): seed-weight top/bottom edge chamfer size is absent from the spec; added a 1mm chamfer purely to avoid a default sharp edge, flagged as unstated.
- Idea 3 (Tidepool Stones): spec describes the disc as "biconvex-profile" but gives no curvature radius. Described the profile qualitatively without inventing a number since no `must_survive` check depends on it, flagged the gap.
- General: `CAD_QUESTIONS.md` is currently empty of real question history (a known gap since turns 11-13), so this turn's prompts are built purely from the Learned Heuristics baseline rather than any fresh pre-answered categories.

**Evaluator:**
- Idea 2 (Sluice Row) rank 3's geometric check (`feature_count`, `part_name_prefix: "seed_small"`, expected 16) came back `"no geometry could be bound to the named part(s) — component appears absent"` in `evaluation_report.json`, but `content.json`/the STEP manifest both list `seed_small_01` through `seed_small_16` as 16 correctly-named, separate top-level assembly parts, and they're clearly distinguishable in every render examined. This looks like a scorer/binder defect (possibly the binder for `feature_count` expecting sub-features *within* one part rather than a count of same-prefixed top-level parts, which is what this check actually needed) rather than an absent feature — but the rubric requires consuming the reported status as-is, so it was scored `fail`/0 credit despite the visual axis independently passing. Worth confirming with `score_build.py`'s author whether `feature_count` is meant to bind across multiple same-prefix top-level parts at all, since every other `feature_count` check this turn (capacity_ring, store_pit) was checking sub-features within a single part, a structurally different case that the binder may be conflating.
- Same idea, rank 5's geometric `part_clearance` check measured `min_distance_mm: 0.0` (a press fit) between `seed_large_sample`/`pit_wall_sample`, but `project/main.py` has an explicit `validate()` assertion (`gap = SMALL_WELL_D/2 - max(WEIGHT_DIAS)/2; assert gap >= 3.0`) that must have passed for the build to have completed at all, and every render of a filled well shows a clean, unbroken gap around the seated seed. Unable to determine from the artifacts available (no renders/ directory, per the known host issue) exactly which two solids the checker actually sampled to get 0.0mm — scored as reported per rubric, but this is now two clearance-adjacent discrepancies in two turns (Turn 14 had a structurally different `inputs`-key wiring bug) and might be worth a dedicated look at how `part_clearance` selects its sample instances.
- The rubric's fallback instruction ("if `renders/` is empty... fall back to `project/main_review/*.png`") worked as documented again this turn, but the two source images needed different crop/zoom treatment to be legible at all — `main_review/trough_board.png`'s top-down well close-ups needed ~3x upscaling before an engraved 0.5mm ring would even plausibly be checkable, and the low-angle iso panel in the same image set was actively misleading (recessed seed-weights read as empty holes from that angle due to occlusion, while the same wells read correctly as filled from the top-down orthographic panel in the same file). Worth noting for future evaluator passes: when a piece sits recessed below a rim, prefer the top-down ortho panel over the iso panel for presence/gap judgments, since the iso angle's foreshortening can hide a seated piece that the top view shows clearly.
- Two of three ideas this turn failed with a bare `terminal: failed` event and literally zero diagnostic text in `session.json` (no error string, unlike Turn 14's `llm_error`/`worker_error` messages) — there was nothing beyond timestamps to distinguish "the CAD generation crashed" from "the CAD generation was killed for taking too long" from any other failure mode. If `session.json`'s terminal event schema could carry whatever error/reason text the underlying job actually returned (mirroring what Turn 14's two failures had), it would let the evaluator attribute failures more precisely instead of defaulting to "pipeline limitation, cause unknown" both times.

**Pipeline (`/bg` driver, not agent-generated):**
- `board-game/tools/pipeline_queue.py`'s `PRIORITY` list (used by `cmd_next` to decide what to run) omitted the `"drafted"` state entirely, even though `PIPELINE["drafted"]` defines a real action (`owner_gate_1` → `awaiting_owner`). Since `cmd_next`'s `waiting_on_owner` check also only looks at `awaiting_owner`/`awaiting_ship`, any idea sitting in `drafted` was invisible to the scheduler — `next` reported "the queue has nothing to advance" and silently defaulted to `propose`, which would have started a second idea while Armillary sat stuck with its owner-gate render never sent. Caught before it happened (Armillary's `next` call returned `propose` right after `draft` completed, which was the tell). Fixed with the owner's approval: added `"drafted"` to `PRIORITY` between `"approved"` and `"briefed"`, matching the closest-to-shipping-first ordering. This would have silently stalled every future idea at the same point, not just this one.
