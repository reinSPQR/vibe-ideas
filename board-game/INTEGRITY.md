# INTEGRITY — loop self-monitoring

### Turn 14 — mechanical audit (GREEN)

_run at 2026-08-12T01:56:57.345193+00:00_

Checked: schema: must_survive shape, mix: one new / one twist / one reskin, coverage: cad_prompt covers components and must_survive, freeze: first-shot artifacts untampered and pre-repair, ledger: one create, at most one edit per idea, provenance: concierge answers cite a spec field, consistency: SCORES.json matches evaluation_report.json, tamper: agent definitions unchanged during the turn, budget: Learned Heuristics word counts, degeneracy: fidelity up while ambition down

No findings.

**Judged audit:**

1. **Vision shrinking — GREEN, but no valid baseline exists.** `history/turn-13/IDEAS.json` and `history/turn-12/IDEAS.json` use a wholly different, pre-revamp schema (`id/title/concept/differentiation_path/differentiation/rules/components/producibility_notes/prompt/cad_prompt`, 10 ideas/turn) with no `art_direction` or `must_survive` fields at all — this turn's `art_direction`/`must_survive` structure was introduced by the "Complete revamp of the loop" commit and turn 14 is the first turn to use it. A part-vocabulary or relief-depth comparison against turn 12/13 would therefore be comparing incompatible schemas, not measuring drift. Checked instead for internal red flags this turn: all three ideas' `must_survive` rank-1 conditions are structural (component counts, tooth-family geometry, hole/socket counts), not decorative, and rank ordering by weight tracks genuine severity in all three. No shrinkage signal to report; flag that turn 18 (next `history/turn-N` two-back from a future audit) will be the first turn where this check is actually meaningful.

2. **Falsifiability — GREEN, one pre-flagged borderline.** Walked all 15 `must_survive` entries (5 per idea) against "what build would fail this." All 15 have geometric checks tied to real thresholds (feature counts, mm tolerances, component counts) rather than prose. `AMBITION.json` flags Eclipse rank 5 (`"reading clearly as Reversi-style board despite carrying no color cues"`) as its sole `unfalsifiable_conditions` entry — correctly: the prose framing is subjective, but its actual `geometric` check (`axis_alignment`, 8x8 grid symmetry, 1mm tolerance) is falsifiable, so the softness is in the feature description, not the check. No unflagged decoration found elsewhere.

3. **Invented design / provenance — GREEN, nothing to sample.** Grepped all of `history/turn-14/`, `turn-13/`, and `turn-12/` for `source_field` / `answer` events with citations: none exist this turn (no idea reached the concierge-Q&A stage that produces them — Keyhold and Eclipse failed before any question, Twin Deck Solitaire's only session event besides submit/park/terminal was `select_concept`). Checked the three `select_concept` `reason` strings instead (the only cited-claim events that do exist): all three quote `art_direction.form_language` accurately word-for-word against `IDEAS.json` (verified idea 1, 2, and 3 individually) — no fabricated citation.

4. **Cad-writer translation — AMBER, but correctly self-disclosed.** Compared `CAD_PROMPTS.json` against `IDEAS.json` for all three ideas (not just the one that built). Found a real, repeated pattern: 2 of 3 ideas (Twin Deck Solitaire, Eclipse) have `IDEAS.json` entries where the prose `components`/`art_direction.scale` fields state one dimension (peg dia. 10mm; disc dia. 24mm) while the numeric `must_survive` fit-check thresholds imply a different one (11.4mm; 23.4mm) — an internal spec contradiction from the ideator, not the writer. In both cases `board-game-cad-writer` resolved toward the falsifiable numeric threshold and logged the substitution explicitly in `CAD_PROMPTS.json`'s own `unstated_in_spec` array and again in `PAIN_POINTS.md`, rather than picking one silently — this is the correct behavior, not a translation invention. Separately, and *not* self-disclosed at the same layer: the cad_prompt for idea 2 explicitly asked for 15mm-deep **blind** bores at the post insertion points, and the CAD-generation worker (a different stage than the writer) built full-thickness through-bores instead (`main.py`: `BORE_D=7.85` extruded through `T+2`), confirmed by direct read of `project/main.py`. This is a worker deviation already caught and logged in `SCORES.md`, `PAIN_POINTS.md`, and `CAD_GRAMMAR.md` — but it is a second instance this turn of geometry silently diverging from an explicit, unambiguous prompt instruction, worth watching for a pattern.

5. **Evaluator drift / calibration replay — GREEN, verified by direct re-run (done regardless of the every-4th-turn cadence, since the turn's central question is a scorer-bug claim).** Verified all 223 files under `idea-02-twin-deck-solitaire/first-shot/` against `FREEZE.sha256` — 0 mismatches, archive is untampered. Copied the frozen build to a scratch directory and re-ran `python3 tools/score_build.py --turn 14 --idea-id 2 --stage first-shot --no-render` against the copy: output was bit-identical to the archived `evaluation_report.json` on every field except absolute file paths (`geometric_fidelity=0.6`, `printability_0_10=7.68`, `physical_0_10=7.8`, `connected_components=70`, all `condition_results` identical). The scorer is deterministic and unchanged. Independently confirmed the specific bug claim: `physical_conditions.json`'s five conditions all have `"inputs": {}` because `compile_conditions()` (tools/score_build.py:123) reads `geometric.get("inputs", {})`, and this turn's `IDEAS.json` `geometric` blocks use `"parts"`/`"thresholds"` keys instead, never `"inputs"` — the bug is real and reproduces exactly as `SCORES.md` describes it, not embellished. Critically: **the evaluator's "manual override" did not touch any score.** `geometric_fidelity` stayed 0.6 in both `evaluation_report.json` and `SCORES.json` (rank-weighted: 3/15 from the one geometric `pass` on rank 3, plus 0.5-credit each on the four `inconclusive` ranks = 9/15 = 0.6) — the hand-verification of `project/main.py` (confirmed independently: `GAP=30.0` against 25-35mm threshold, both boards built from one shared `cross_positions()` call giving exactly 0.0mm alignment offset, `PEG_D=11.4` vs `HOLE_D=12.0` giving exactly the spec'd 0.3mm clearance) was used only to write an explanatory `geometric_tooling_note` in `SCORES.json` and prose in `SCORES.md`/`BOARD.md`, never to bump `geometric_fidelity`, `vision_fidelity_60`, or `total_100` upward. `BOARD.md`'s own turn-14 entry states this explicitly ("the evaluator doesn't get to override a broken measurement with its own judgment on the geometric axis"). This is the correct, conservative call — an agent invented a *narrative claim* about the true state of the geometry (backed by direct, reproducible source-reading, not speculation) but did not invent a *score*. Visually, spot-checked `main_review/board_top.png` (33-hole cross pattern, visually consistent with the claimed count) and `assembled.png` (two boards, visible gap, corner posts, discrete pegs) against the five visual instructions — consistent with the evaluator's `visual_fidelity=1.0`, though this reviewer's read was not pixel-exhaustive.

6. **Pipeline drift / canary — AMBER (mechanism not yet exercised, no finding possible).** `tools/canary_prompt.txt` exists and was added in the same "Complete revamp of the loop" commit that produced this turn's schema, but no `history/turn-*/builds/canary/` directory exists in any turn (1 through 14) — the canary has never actually been built. Turn 14 is not a 4th-turn checkpoint (next due at turn 16), so this is not itself a violation this turn, but flag it now: if turn 16 also produces no canary build, that is a broken pipeline-drift control, not a scheduling gap, since 14 turns is more than enough runway to have exercised it once.

7. **Learning loop — GREEN.** Read the last three `BOARD.md` turn entries (12, 13, 14) and all of `CAD_GRAMMAR.md`. The schema revamp itself is the dominant signal this turn (10 rules-only ideas/turn → 3 art_direction+must_survive ideas/turn), so turn-over-turn lesson continuity can't be judged the old way; judged instead whether turn 14's own new failure modes got written down before this audit ran. They did, consistently across three independent artifacts: the `inputs`-key scorer bug is named in `PAIN_POINTS.md` (CAD-pilot and Evaluator sections), `BOARD.md`'s turn-14 entry, and `SCORES.json`'s `geometric_tooling_note`; the blind-bore-built-as-through-bore deviation is named in `PAIN_POINTS.md`, `SCORES.json`, and added as a new `CAD_GRAMMAR.md` row; the two pre-build `llm_error`/`worker_error` failures got a new standing note in `CAD_GRAMMAR.md` explicitly separating "translation lost" from "translation never started." One gap: the peg/disc dimension-conflict pattern (finding 4 above) is logged twice in `PAIN_POINTS.md` per-idea but has no `CAD_GRAMMAR.md` row of its own generalizing it as a feature class ("must_survive numeric thresholds silently contradict components/art_direction prose dimensions") despite recurring in 2 of 3 ideas in a single turn — worth promoting to a row before it has to be rediscovered idea-by-idea again next turn.

**Overall:** no finding rises to RED. The turn's numbers are trustworthy: the scorer bug is real but was handled honestly (no score was inflated to compensate for it), the archive is untampered and the scorer replay is deterministic, and no agent invented a design decision it wasn't entitled to make — the evaluator's manual geometry check produced an explanatory note, not a score change. The two AMBER items (the unresolved dimension-conflict pattern not yet promoted to `CAD_GRAMMAR.md`, and the still-unexercised canary) are both fixable at the next triage pass and neither one taints turn 14's reported scores.


### Turn 15 — mechanical audit (RED)

_run at 2026-08-12T05:59:15.314051+00:00_

Checked: schema: must_survive shape, mix: one new / one twist / one reskin, coverage: cad_prompt covers components and must_survive, freeze: first-shot artifacts untampered and pre-repair, ledger: one create, at most one edit per idea, provenance: concierge answers cite a spec field, consistency: SCORES.json matches evaluation_report.json, tamper: agent definitions unchanged during the turn, budget: Learned Heuristics word counts, degeneracy: fidelity up while ambition down

| Level | Check | Detail |
|-------|-------|--------|
| RED | ledger | idea-03-tidepool-stones: 2 create jobs — a failed build may have been silently resubmitted and the successful attempt reported as first-shot |

**Judged audit:**

**On the mechanical RED (ledger, idea-03-tidepool-stones).** Read `session.json` in full: job 1 (`6a7bfc9e…`) was submitted 04:54:54, reached `terminal: failed` at 04:56:14 — 80 seconds later, before ever parking, with no plan, no question, no diagnostic text. Job 2 (`6a7bfcfc…`), same `prompt_sha256` (byte-identical prompt, confirmed by hash comparison), was submitted 14 seconds later at 04:56:28, parked normally, had a concept selected, then also reached `terminal: failed` at 05:45:11 — 47 minutes later. Neither job ever produced a capture, an `evaluation_report.json`, or a `project/`/`renders/` directory. Checked whether the second attempt's outcome was laundered as first-shot anywhere: it was not. `SCORES.json`'s `first_shot_status_detail` for idea 3 names both attempts individually with both timestamps and both failure points; `SCORES.md`'s per-idea note does the same at greater length; the idea is correctly marked `counted_in_average: false` with `total_100` equal to its Ambition score alone. So the specific harm the ledger rule exists to catch — a failed build's retry quietly standing in for a first-shot score — did not happen here; both outcomes are fully and separately disclosed, and neither was scored as a build. That said, the rule itself was still broken: nothing in `board-game-cad-pilot.md` documents what to do when a `create` job fails outright before ever parking (as opposed to parking on a clarifying question, which is the only park type the mandate discusses), so the pilot's decision to resubmit with an identical prompt was taken outside any written authorization — not forbidden by name, but not licensed either. `audit_turn.py`'s rule (`len(creates) > 1` is always RED, unconditionally) is deliberately blunt for exactly this reason: it cannot tell "silent laundering" from "transparent retry of an infra hiccup" by itself, and is designed to force a human look rather than guess. Having looked: this instance is benign in substance (no score corrupted, no concealment) but the process gap is real and should be closed — either the pilot's mandate should explicitly authorize one clean resubmission when a `create` job fails pre-park with zero diagnostic text, or it should explicitly forbid it and require reporting the bare failure instead. Until that's written down, the next occurrence of `len(creates) > 1` still has to be individually re-investigated by hand, which is expensive and exactly the kind of thing a one-line mandate addition would fix. I am not overriding the mechanical RED — the ledger invariant was genuinely violated and the audit script is correct to have stopped on it — but flagging that the substance is process-gap, not score-corruption.

1. **Vision shrinking — GREEN, one valid comparison turn available.** `history/turn-13/IDEAS.json` is still pre-revamp schema (confirmed: keys are `concept/differentiation/rules/components/producibility_notes/cad_prompt`, no `art_direction`/`must_survive`), so turn 15 vs turn 13 remains an apples-to-oranges comparison as flagged last turn. Turn 15 vs turn 14 (the one schema-compatible baseline) is meaningful: component/part counts are comparable (t14: 40 tiles+49 pedestals, 64 pegs+2 boards+4 posts, 64 discs+64 sockets; t15: 42, 49, 65 total components) — no shrinkage in part-count vocabulary. Relief depths are comparable in magnitude (t14 domes 4mm proud/4mm dish = 8mm total relief; t15 Lockstep trenches 5x3mm + 2mm chamfers, Tidepool discs 7mm tall with 4mm proud-of-ridge seating) — nothing flattened into plain slabs. `ambition_15` scores this turn (12, 9, 9; mean 10.0) are close to last turn's (13, 12, 9; mean 11.3) — a small dip, but idea 3 (Tidepool Stones) is a declared reskin with mechanical ambition deliberately at floor by the `new`/`twist`/`reskin` mix rule, not drift; idea 1 and 2's individual scores (12, 9) are within one turn's normal idea-to-idea variance. No flattening signature found.

2. **Falsifiability — GREEN.** Walked all 15 `must_survive` entries. Every entry has both a `geometric` block with a numeric `check`/`thresholds` and a `visual` instruction — none rely on prose alone. Cross-checked against `AMBITION.json`'s `unfalsifiable_conditions`: all three ideas report `[]` this turn (contrast turn 14's one flagged entry) — confirmed independently by reading every rank's language for hedge words ("reads as," "feels like") that might carry decorative weight; found none doing unearned work — e.g. Lockstep rank 5 ("genuine open cavity a hand can reach into") sounds soft but resolves to a numeric `opening_presence` check with a stated cavity dimension. No unflagged decoration found.

3. **Invented design / provenance — GREEN, nothing to sample (second turn running).** Grepped all three `session.json` files for `answer` events: none exist, same pattern as turn 14 — no idea reached the `awaiting_questions`/`awaiting_concept_input` stage that produces `source_field`-cited answers (idea 1 and 3 failed mid-CAD-generation after concept selection with no further parks; idea 2's only non-submit/capture/terminal event is `select_concept`). Checked the three `select_concept.reason` strings instead, word-by-word against `IDEAS.json`'s `art_direction`: idea 1's reason quotes `form_language` ("hand-tooled canal masonry, never a flat printed board") and `silhouette` ("mason's yard mid-project") verbatim, both confirmed present in the source field by direct read; idea 2's reason quotes `form_language` ("a single heavy timber-plank slab... reading as an antique grain-weighing trough") and `silhouette` ("like an old measuring device set out on a table") verbatim, both confirmed. No fabricated citation found in either. (Idea 3's reason was reviewed by the same method with the same result — accurate quotation of `art_direction.form_language`.) This turn's structural gap — no `awaiting_questions` parks at all across all three ideas, for the second turn running — means this check has now had nothing but `select_concept` reasons to sample in both turns since the schema revamp; worth noting to whoever tunes the pipeline's question frequency, since it is quietly starving this specific audit of its primary intended evidence (spec-sourced answers), not because provenance is clean but because the opportunity to fabricate one hasn't come up.

4. **Cad-writer translation — GREEN.** Compared `CAD_PROMPTS.json` against `IDEAS.json`'s `components` for all three ideas: idea 1's cad_prompt (366x366x15mm board, 42 components, per-family trench/notch geometry) matches `components` numerically field-for-field; same for idea 2 and idea 3. Zero `unstated_in_spec` entries this turn (contrast turn 14's two dimension-conflict resolutions) — the ideator's own numeric fields were internally consistent this time, so the writer had nothing to arbitrate. Checked the one idea that actually built (Sluice Row) against its own CAD source: `project/main.py`'s `RING_W/RING_DEPTH/RING_H = 1.0, 0.5, 9.0` and `SMALL_WELL_D/DEPTH = 26.0, 16.0` match the cad_prompt's stated capacity-ring and well dimensions exactly, and `validate()`'s clearance assertion (`gap >= 3.0`) matches the cad_prompt's stated "minimum 3mm radial gap" and the idea's own `must_survive` rank-5 threshold (`min_clearance_mm: 3.0`) — all three layers agree on the same number, no silent loosening. No mechanism simplification or part-merging found in the one buildable case; the two failed ideas never reached a build to check.

5. **Evaluator drift / calibration replay — AMBER, two confirmed scorer defects, both understating this turn's one countable score (not inflating it).** Not a scheduled 4th-turn replay (15 mod 4 = 3), but the reported average dropped sharply from turn 14 (80.68) to turn 15 (62.23) on a 1-build sample each turn, so I traced the one counted build's low-scoring ranks to ground rather than accepting "obviously explained by 2/3 builds failing" at face value for the ranks that *did* build. Two findings, both confirmed by direct source/artifact reads, neither previously root-caused in `SCORES.md`/`PAIN_POINTS.md` beyond "looks like a scorer defect": **(a)** `score_build.py`'s `compile_conditions()` (line 108-114) treats any `geometric.inputs` key containing the substring `"part"` as a literal part name requiring an exact match in `bindings.json`. Rank 3's input is `{"part_name_prefix": "seed_small"}` — a *pattern* meant to match the 16 separately-named `seed_small_01..16` parts that do exist in `bindings.json` and the STEP manifest — but the code does `{"seed_small"} - part_names`, which is always "missing" since no part is literally named `seed_small` (only `seed_small_01` through `_16`). This mechanically forces every `feature_count`/`part_name_prefix` check against a same-prefixed part family to report `unbindable` regardless of whether the geometry is correct, which it demonstrably was here. `PAIN_POINTS.md` had already flagged this rank's result as suspicious and hypothesized a related cause; this confirms the exact line and mechanism. **(b)** Rank 5's `part_clearance` check measured `min_distance_mm: 0.0` between `seed_large_sample` and `pit_wall_sample`. Read `bindings.json`: `pit_wall_sample` is bound to `trough_board.stl` — the *entire* single-body board, not an isolated wall segment (the board has no separately-meshed wall, by design; it is one monolithic solid per `CAD_GRAMMAR.md`'s own row on this class). Since the seed-weight physically rests on the well *floor*, which is part of that same solid, the true minimum surface-to-surface distance is necessarily 0 regardless of the radial wall clearance the check is actually trying to measure (which `main.py`'s own `validate()` independently confirms is 4.0mm, comfortably clearing the 3.0mm threshold). This is a structural limitation of the `part_clearance` check whenever the reference part is a monolithic board rather than an isolated wall mesh — it will report a false press-fit for any recessed part that touches its own recess floor, which is the normal case, not an edge case. `PAIN_POINTS.md` flagged this rank as unexplained ("unable to determine... exactly which two solids the checker actually sampled"); this pins the mechanism down. Net effect: idea 2's reported `geometric_fidelity` (0.3333 = 5/15, only rank 1 credited) is very likely wrong in the conservative direction — correcting just these two confirmed defects would put it at 9/15 = 0.6, nearly double, which would materially change `vision_fidelity_60` and `total_100`. Neither defect was used to inflate anything — the evaluator scored as reported, per rubric, exactly as it should — but both are real, reproducible, and now root-caused precisely enough to fix, and until fixed they will keep silently discounting exactly the kind of well-executed, correctly-modeled-but-conventionally-shaped geometry (same-prefix part families; features on monolithic boards) that turns 12-15 have repeatedly produced. This is the evaluator measuring its own tooling's blind spots, not the builds' quality — worth fixing before the next 4th-turn (16) calibration replay, since an improvement in `geometric_fidelity` next turn driven by fixing these two bugs would otherwise look indistinguishable from "the ideas got better."

6. **Pipeline drift / canary — AMBER, still unexercised, now more overdue.** `history/turn-*/builds/canary/` does not exist for any turn 1 through 15. Turn 15 is not itself a 4th-turn checkpoint (15 mod 4 = 3; next due turn 16), so this is not a new violation, but it repeats and sharpens turn 14's flag: the canary mechanism has now had 15 turns of runway, including one full scheduled-checkpoint turn (12) that should have exercised it under the "every 4th turn" rule and evidently didn't (no `history/turn-12/builds/canary/` either). If turn 16 also produces no canary build, that is two consecutive missed checkpoints (12 and 16) for a control specifically designed to distinguish "the agents got better" from "the pipeline got better" — and turn 15's evaluator-tooling findings above (check 5) are exactly the kind of pipeline-side change a working canary would have made visible sooner.

7. **Learning loop — GREEN, with one gap worth naming.** Read `BOARD.md` turns 13, 14, 15 and all of `CAD_GRAMMAR.md`. The loop is tracking its own new failure mode correctly across turns rather than rediscovering it blind: turn 14 named the pre-build pipeline-failure pattern (`llm_error`/`worker_error`, pre-park), turn 15's `BOARD.md` entry explicitly compares against it and notes the failure point *moved* (post-concept-selection, mid-CAD-generation, with less diagnostic text than turn 14) and escalates it from "noted" to "recurring" — this is learning, not repetition, since the description sharpens rather than restates. `CAD_GRAMMAR.md` gained two new, correctly-generalized rows this turn (subtractive-feature-on-monolithic-board as a feature class, not tied to Sluice Row specifically) plus a standing-note update distinguishing turn 14's pre-park failures from turn 15's post-park ones. The "good habit" callouts (shared-function-generates-both-layers' positions, now seen in `cross_positions()` at t14 and `small_centers()` at t15) are being tracked as a reusable pattern rather than praised once and forgotten. The one gap: `PAIN_POINTS.md`'s evaluator section names both scorer defects (check 5 above) but stops at "worth confirming with score_build.py's author" / "worth a dedicated look" rather than the loop's own artifacts pinning down the actual line/mechanism the way this audit did — meaning if I had not opened `score_build.py` and `bindings.json` directly, both defects would still be sitting as open questions rather than fixable bug reports for a third turn running (the `inputs`-key bug at t14 took one turn to get root-caused and fixed by t15; these two are still at the "looks like a defect" stage after appearing this turn). Worth tightening the evaluator's own diagnostic habit to read the scorer source when a result contradicts the artifacts, not just flag the contradiction.

**Overall:** the mechanical RED (ledger, idea-03) is real and correctly stops the loop for review — its underlying cause is a genuine mandate gap (no documented policy for resubmitting after a bare pre-park `create` failure) rather than concealment, since both attempts are fully and separately disclosed and neither was scored as a build; this needs a one-line fix to the cad-pilot's mandate before the next occurrence has to be re-investigated by hand again. Independently of that, this turn's judged findings skew toward the evaluator's tooling rather than the ideator's or writer's honesty: two scorer defects (prefix-matching in `compile_conditions`, floor-contact conflation in `part_clearance`) were confirmed by direct source reads and both understate the one countable score, not inflate it — the loop is not gaming itself, but its one working measurement this turn is less accurate than it looks. No evidence of shrinking vision, unfalsifiable conditions, invented design, or writer-side redesign this turn.

